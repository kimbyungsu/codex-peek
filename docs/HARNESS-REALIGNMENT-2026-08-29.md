# 하네스 재정렬 최종본 — 수칙서의 목적 회복과 반복 문제 정리 (2026-08-29)

상태: 정본 v19(2026-08-31) · 설계 검증 통과(2026-08-29 3왕복·08-30 §4-B 4왕복+4왕복) · **사용자 구현 착수 승인 2026-08-30("다음 진행") · 개선 2
1단계(결정 장부·판단 관문·마감문 블록·서식 편집) 구현됨(대시보드 꼬리는 §7 4번으로) · 개선 1(되받아침 고리 A+B+F) 구현됨 2026-08-31 · §7 3-1(§4-B ①②
검증자 규약 1회 전달·전달 레코드·압축 감지·postflight) 구현됨 2026-08-31 · §7 3-2a(개선 4 (a)(b)(c)+§4-B ④ 자료 예산·선별 초과 격하·판당 행
상한) 구현됨 2026-08-31 · §7 3-2b(개선 4 (e)(d)+§4-B ③ 하네스 문장→훅 장부+Claude 쪽 ① 세션 1회 전달·SessionStart 훅·판정 꼬리 포인터·대시보드
'훅으로 옮긴 규칙') 구현됨 2026-08-31(부록 참조) · 다음=§7 4번(개선 3 보고 양식 검사+P6·P7·P9·P10 소형+대시보드 결정 장부 합류·서고 카드)** ·
이 문서가 2026-08-28 밤~30 세션의 모든 논의(수칙서 목적·재판단·결정 장부·보고 양식·프롬프트 비대·앵커 이탈)를 대체하는 정본이며,
[REJUDGE-AUTHORITY-DESIGN.md](REJUDGE-AUTHORITY-DESIGN.md) v6은 이 문서 §5의 범위로 **축소 적용**된다. 여러 번 덧댄 문서라 2026-08-30에
전체를 다시 읽어 상충 문구를 정리했다(정리 내역은 §8 마지막 항목).

## 0. 한 줄 결론

수칙서는 "지킬 규칙집"이 아니라 **채점자(검증자)가 미리 알고 채점하는 시험 범위표이자, 답안 쓴 사람(구현자)이 범위 밖
지적을 되받아칠 때 드는 근거**다. 오늘까지 하네스는 앞쪽(검증자에게 범위표 전달)만 있었고 뒤쪽(구현자가 그 범위표로
되받아치기)은 한 번도 작동하지 않았다(되받아친 건 706번 중 1번). 개선은 **뒤쪽 고리를 잇는 최소 구조**로 하고, 사용자에게
결정을 묻는 항목은 늘리지 않는다.

## 1. 수칙서란 무엇인가 — 최종 정의(이 문서로 확정)

상황으로 말하면 이렇다. 시험이 끝나면 채점자는 답안에서 무엇이든 지적할 수 있다. 그런데 **시험 범위표**가 있으면
"이건 범위 밖 문제"인 지적은 감점이 아니라 메모가 된다. 답안 쓴 사람은 범위표를 들고 "이 지적은 범위 밖"이라고 되받아칠
수 있고, 범위표는 선생님(사용자)이 정한다. 채점자는 되받아침에 반대하려면 "범위 안에서도 이 문제가 난다"는 근거를 내야 한다.

- **세 칸**: 전제(우리가 지키기로 한 세계 — 예: "혼자 쓰는 로컬 PC"), 금지(어떤 경우에도 안 되는 일 — 예: "남의 프로젝트 장부에
  기록되는 일"), 제외(안 다루는 상황 — 예: "서버 두 대에 동시에 배포").
- **두 사용처**: ① 검증자 인지 — 채점 전에 범위표 전달(지금도 됨). ② 구현자 재판단 — 지적이 도착한 자리에서 범위표를 보고
  "범위 밖" 되받아치기(지금 안 됨 → §4 개선 1).
- **누가 넣고 빼나**: 승인·빼기=사용자만(현행·앞으로도). 제안의 주체가 바뀐다 — 지금은 구현 담당(저)의 마감 제안+대화 약속 포착뿐인데,
  사용자님 지적대로 **자기 반박 근거가 될 경계를 답안 쓴 사람이 제안하는 것은 이해충돌**이다. 그래서 **독립 탐색 트랙(큐레이션)이 장부
  (무엇이 반복 강등됐나·무엇이 되받아침에 쓰였나·무엇이 한 번도 안 쓰였나)를 보고 유지/추가/삭제·항상/관련을 제안하고, 사용자는 승인·
  빼기만 한다** — 2차 범위(§5 C·D). 구현 방식은 검증 경로 안에 끼우는 것(마감 마커·curating 단계·5단 커밋 = 조각 6개)이 아니라, 3트랙
  정찰처럼 검증 경로 **밖**에서 도는 독립 작업이 후보를 제안함(기존 후보 장부·승인 흐름 재사용)으로 한다. 사용자 판단은 늘지 않는다
  (승인·빼기 표면 그대로).
- **항상/관련**: 1차는 현행 유지 — 첫 장(전제·금지·제외 19줄)은 매 검증 붙고, 보관 수칙은 작업마다 사서가 골라 싣는다. 19줄이
  "임의로 고정된 헌법"이었다는 지적은 맞다. 줄마다 항상/관련을 정하는 일은 위 큐레이션 트랙의 제안 항목으로 2차에 함께 간다(구현자가
  정하지 않는다).
- **되받아침의 근거는 '제외' 칸뿐**이다. 전제 칸은 "우리 세계 안"이라는 뜻이라 오히려 지적을 뒷받침하고, 금지 칸은 면제
  사유다. 그래서 **되받아칠 재료가 자라는 유일한 곳이 제외 칸인데, 제외 칸은 첫 장 12칸 상한뿐이고 보관 수칙 파일은 금지 칸만
  받는다** — 사용자님이 아직 짚지 않은 구조 공백이다(P12).

## 2. 오늘 세션의 사실 실측 (장부 전수 — 기준 시각: 선별 영수증 2026-08-29T05:34:23.981Z까지. 최신 장부는 처분 720행·지적 1068건으로 비율 동일. 재현은 부록)

| 관측 | 수치 | 뜻 |
|---|---|---|
| 지적에 대한 구현자 처분 706행 | 수용 619 · 보강 64 · 보관 22 · **되받아침 1** | 재판단이 실행에서 죽어 있음 |
| 검증자가 "범위 밖"이라고 스스로 표시 | **0 / 1057** | 채점자에게 자기 지적을 낮추라는 구조는 작동 안 함 |
| 범위표로 강등된 지적 | 0 | 범위표가 채점 기준으로만 쓰이고 재판단엔 안 쓰임 |
| 구현자에게 전달되는 범위표 | 금지 8줄만(전제·제외 없음) · 판정 도착 자리엔 아무것도 없음 | 되받아칠 재료가 되받아치는 사람에게 안 옴 |
| 보관 수칙 골라 싣기 | 62판 중 34판에서 1~11줄 실림(보관 23줄 시절) · 지금은 보관 0줄이라 0 | 실어 나르는 구조는 있음 |
| 상한(턴당 5회 왕복) 소진 턴 | **2턴**(입력칸 결속 08-28T14:49 · 재판단 설계 08-29T05:28). 경계 통일 작업의 4판(08-28T21:33)은 **다른 프로젝트(D:\codex-peek) 공책**에 기록됐고 그 폴더 계약엔 프로필·상한 설정이 없어 기본값(감사 프로필·상한 없음)으로 돌았다 — 상한 소진이 아니며 제 마감문 "5/5"는 제가 손으로 매긴 번호였다(P7·P10) | 상한은 턴+프로젝트 단위. 매 회차 새 지적을 전부 수용 → 마지막 수정이 미검증으로 남는 패턴 |
| 설계 검증 3~5회차 판정 | 검증자 기계 블록 1줄 손상 → '보류' 강등·지적 각 3·2·4건이 장부에 **미등록** | 재판단 강제 사슬이 끊김(P6) |
| 검증자에게 가는 글(마지막 ask 실측) | 전체 9,516자 = 하네스 머리 **8,690자** + 제 요청 826자. 머리 중 고정 산문 2,329자(예산 안: 1,704≤1,720·639≤700·213≤250), **데이터 6,353자**(경계 19줄 1,753 · 지도 조각 2,776 · 결합 확인 680 · 설계 경위 581 · 정찰 293 · 열린 지적 270) | 예산은 산문에만 있고 데이터엔 총량 상한이 없다(P11) |
| 8월 추이(검증자 스레드 실측) | 08-15 이후 스레드의 실제 요청 268건 **중앙값 8,562·최대 11,328**(1회차 검증자 재집계 — 제 첫 집계 8,527은 압축 사본 1,531건을 섞은 오류) · 08-13 스레드 중앙값 5,290 | 사용자 관측("8천 넘어 1만 근접")과 일치 |
| 상한(코드) | 사용자 계약 주입 4,000(차단) · 고정 산문 예산(시험) · 머리 총량 12,000은 **경고만(차단 아님)** | 총량이 12,000에 닿아도 검증은 그대로 나간다 |
| 구현자(Claude) 쪽 | 매 턴 주입 2,051자(검증 지시 1,107·경계 652·규약 197·경위 95) + 판정 도착 꼬리 1,863자(재판단 규약 1,402 포함) | 판정 자리에 범위표를 붙이는 개선 1-A는 이 꼬리를 키운다 → 총량 예산 필요(개선 4-d) |

## 3. 문제 목록 — 사용자 지적 6(P1~P5·P11) + 재정독 6(P6~P10·P12)

**P1. 재판단·되받아침이 작동하지 않음(사용자 지적)** — 원인: ①범위표가 되받아치는 쪽에 안 옴 ②강등 권한이 "채점자가 스스로
낮춰라"에 걸림 ③되받아치는 명령이 자유 메모뿐·근거 결속 없음·채점자 반증 의무 없음 ④수용은 한 줄, 되받아침은 실측 반례+동봉+
분쟁 위험 — 구조가 수용을 유도.

**P2. "상황예시로 설명" Claude 규칙 반복 위반(사용자 지적)** — 사실: 규칙은 매 프롬프트에 주입되고 설치본도 일치한다(훅에 걸려
있음). 그러나 **지키는지 보는 장치가 없다**(출력 검사 0). 비유: 출입문에 복장 규정이 붙어 있는데 경비원이 옷을 안 본다. 게다가
저는 설계 문서 문장을 사용자 보고에 그대로 옮겼다. "다음부터 조심"으로는 안 되고, 보고의 자리를 나누는 구조가 필요(§4 개선 3).

**P3. "사용자 결정은 스크립트가 출력하라" 미이행(사용자 지적)** — 사실: 결정 항목을 장부에 남기고 스크립트가 목록·선택지를
출력하는 기능은 없다. 있는 것은 ①상한 마감문의 결정 칸을 **제가 산문으로 쓰면** 형식만 검사 ②개요의 "지금 정할 것"은 경보·기한
지난 보관함·지도 선택만 합산(마감 결정 항목은 미포함). 몇 차례의 지시가 "작성 규약(문서 §8)"으로만 남았다.

**P4. 설계가 개정될수록 조각과 사용자 판단이 불어남(사용자 지적)** — 원인: 검증 왕복마다 검증자 지적을 "새 조각 추가"로 흡수했다
(P1의 619:1과 같은 습관이 설계 단계에서 재현). 재판단 설계는 5번 개정되며 부품 6개·새 내구 조각 6개·사용자 결정 4개가 됐다.
되받아칠 수 있어야 비대도 막힌다 — P1 개선이 P4의 구조적 처방이기도 하다.

**P5. 사용자에게 결정을 넘기는 압박(사용자 지적)** — 원인 두 가지. ①"[사용자 판단 필요]" 칸이 있으면 채우는 관성·설계 문서에
§8을 신설하는 습관 ②**하네스 자체가 결정을 제조**한다: 검증자 블록이 손상되면 마감문 규칙이 "EVIDENCE-UNAVAILABLE"을 사용자 판단
칸에 넣으라고 강제해, 실제로는 판독 실패인데 사용자에게 선택 1/2를 묻게 만든다(오늘 실제 발생).

**P6. (재정독) 검증자 출력 손상 시 지적이 장부에서 사라짐** — 설계 검증 3·4·5회차 모두 기계 블록 1줄 손상으로 판정이 '보류'로
강등됐고, 그 지적들(3·2·4건)은 **장부에 등록되지 않아** 재판단 강제·계보·상한 마감 근거가 전부 비었다. 저는 본문을 눈으로 읽어 반영했지만
하네스 입장에선 "지적 0건"이다. 손상은 fail-closed가 맞고 건수 경보("지적 블록 손상(n줄)")도 이미 남는다. 없는 것은 **다음 검증 요청에
"몇 번째 줄이 어떤 사유로 깨졌으니 서식을 다시 내라"를 자동으로 붙이는 처리**뿐이다. 손상 줄의 원문은 남기지 않는다 — 깨진 줄에 비밀값이
섞여 있을 수 있어 좌표·사유 키만 보존하는 것이 현행 계약(ab-7)이며 이 문서도 그것을 유지한다.

**P7. (재정독) 프로젝트 앵커 이탈 — 검증 4판이 다른 프로젝트의 공책·기본값 계약으로 감(ab-1 실사고)** — 경계 통일 작업의 판정
4건(08-28 21:36~22:16)은 워크스페이스가 `D:\codex-peek`으로 기록됐다. 경위: 21:33에 제가 코드 로드 시험으로 훅 스크립트를
`require()`했고, 훅 파일은 불러오는 순간 진짜 훅처럼 실행돼 "이 대화의 폴더"를 그때의 셸 위치(codex-peek)로 덮어썼다(입력 없음 →
`hook.cwd || process.cwd()` 폴백·세션 id는 환경변수에 살아 있어 이 세션의 앵커 파일을 갱신). 이후 `ask-start`는 앵커대로 codex-peek 폴더의
계약을 읽었고, 그 계약엔 프로필·상한이 없어 기본값(무결성=감사 프로필·상한 0=없음)으로 돌았다 — 그 프로필은 기계 판독을 건너뛰므로
(`machineFindingsLayer` 첫 줄) 지적은 장부에 없고, 왕복 줄 `[검증 왕복 N/M]`도 찍히지 않았다. 사용자 프롬프트가 23:07에 다시 오자 앵커가
원래 폴더로 돌아왔다. 하네스는 아무 경고도 내지 않았다("엉뚱 폴더 방어"는 앵커와 실행 위치가 같으면 무동작·설정 없는 폴더의 기본값 계약도
고지 없음). 상황으로 말하면, 채점표를 옆 반 교무실에 제출해서 우리 반 점수판엔 아무것도 안 올라갔는데 아무도 "옆 반이다"라고 말해 주지 않은 것.

**P8. (재정독) 대화 턴마다 검증 강제의 비용** — '항상' 모드라 질문·설명 턴에도 매번 검증이 돌았다. 이번 세션(08-28 14:00 KST 이후)
검증 작업 31건(별도 체크포인트 14건 제외) 중 9건이 코드 변경 없는 "제 답변 문장 대조" 목적이었고, 완료 작업의 소요는 평균 5.8분·최대 13.2분
(10분 이상 3건)이었다. 사용자님 설정이므로 바꾸지 않되, 이 비용이 대화 지연과 침묵의 원인이었음을 적어 둔다.

**P9. (재정독) "왜 이렇게 됐나" 조회가 비어 있음** — 오늘 두 번 조회한 설계 경위 색인이 0건이었다. 수칙서·재판단처럼 문서에
결정 이력이 있는 주제가 색인에 없어, 경위 질문에 문서를 손으로 뒤졌다.

**P12. (재정독) 되받아칠 재료가 자랄 자리가 없음** — 되받아침 근거는 제외 칸뿐인데(§1), 제외 칸은 첫 장 12칸 상한뿐이고 보관 수칙
파일은 금지 칸만 받는다. 새 제외 상황이 발견돼도 넣을 곳이 없다. 해소 경로는 §5 D(2차·큐레이션과 함께).

**P10. (재정독·사용자 지적) "몇 회차"를 제가 손으로 셈** — 상한은 하네스가 턴(캠페인)+프로젝트 단위로 세고 판정 답 하단에만 `[검증 왕복
N/M]`으로 알려 준다. 저는 요청문 제목에 "검증 5회차(최종)"처럼 번호를 직접 적고, 그 줄이 없어진 것(P7)을 알아채지 못한 채 마감문에 "5/5
소진"을 썼으며, 기억 파일에도 그대로 남겨 다음 세션이 사실로 읽었다. 마감문 검사기(`verify-cap-handoff.js`)는 절 위치·지적 키·중복·근거
형식·보관함 영수증·잔여 위험 양식까지 검사하지만, 마감문 속 "N/M"은 추출하지도 캠페인 장부와 대조하지도 않는다(Stop 훅이 진행도를
계산해 놓고도 검사기엔 넘기지 않음). 즉 회차의 권위는 장부에 있는데 산문이 장부를 덮어쓰는 길이 열려 있다.
**화면판(사용자 실보고 2026-08-30 "화면은 1/5인데 보고는 5/5")**: 대시보드 '회차' 배지는 장부가 아니라 진행 표시 파일의 값(`이전 값+1`)을
보여 줬는데, 작업자가 선별 단계마다 그 값을 0으로 되돌려 매 판 "1/5"로 보였다. 장부(턴당 계수)는 5/5가 맞았고 화면이 틀렸다 — 같은 숫자를
두 곳에서 세던 것. 봉합: 장부에 오른(tracked) 예약은 화면 회차=캠페인 장부의 예약 서수(단일 원천)이고, 미집계·무제한 예약은 회차 권위가
없으므로 종전의 '이전 값+1' 진행 표시만 유지한다(권위 아님 — 답 하단에 '미집계' 고지가 따로 붙음). `tests/round-judge.test.js` [12]·`p12-budget`.

**P11. (사용자 지적 5) 검증자에게 가는 글이 8,690자 — 산문 다이어트는 지켜졌지만 데이터 동봉이 비대** — 08-06 머리 다이어트는
"코드가 소유한 고정 산문"에만 예산을 걸었고(지금도 예산 안), 그 뒤 붙은 데이터(경계 19줄 전문·지도 조각·결합 확인·정찰·설계 경위)는
합쳐서 6,353자인데 총량 상한이 경고(12,000)뿐이라 차단이 없다. 원인 셋: ① 경계 19줄 전문이 매 ask마다 그대로 실린다(1,753자) ② 지도
조각 관문은 "요청문에 파일 이름이 있으면" 열리므로 **문서만 검증하는 요청에도 코드 지도 2,776자가 실렸다**(제 요청문이 코드 파일을
인용해서) ③ 결합·정찰·경위는 지도 관문에 편승한다. 판정: **위험한 상태로 가고 있다** — 중앙값 8.5천·최대 11.3천이면 경고선(12,000)까지
여유 700자뿐이고(실제 요청 268건 중앙값 8,562), 개선 1-A(판정 자리 범위표)를 예산 없이 붙이면 구현자 쪽도 같은 길을 간다. 상황으로 말하면, 편지 본문 길이는 규정으로
묶어 놓았는데 첨부 서류는 아무 제한 없이 끼워 넣어 봉투가 터지기 직전인 것.

## 4. 개선안 — 개선 4개(새 사용자 표면 0·프롬프트 문장 추가 0)

**개선 1. 되받아침 고리 잇기(P1·P4)** = 재판단 설계의 A+B만.
- 판정이 도착하는 자리에 **그 판에 실린 범위표 중 제외 칸의 번호+한 줄 제목**(≤200자 — 개선 4-d 예산 안: 재판단 규약의 명령 인자 나열과
  처리 안내의 규약 중복 문장을 줄여 상쇄, 개선 전 꼬리 대비 합계가 늘지 않음을 시험이 잰다)을 함께 붙이고, 전문(전제·금지·제외·보관 선별분)은
  되받아침 명령의 조회 옵션으로 연다. 판마다 동결(다른 판의 문안이 붙지 않게).
- 되받아침 명령 한 줄: "이 지적은 제외 n번 상황을 전제한다"(번호+상황 한 줄). 효력은 지금 있는 "범위 밖 강등"과 같다 — 실패
  사유에서 메모로 내려가고, 남은 지적이 전부 강등이면 **통과가 아니라 '보류'**(사용자가 범위 밖 수용/재심 중 선택 — 기존 규칙).
  삭제는 없고 기록에 남는다. 다음 검증 요청엔 하네스가 자동으로 "구현자가 이렇게 되받아쳤다"를 붙인다.
- 채점자가 같은 지적을 다시 올리려면 "범위 안에서도 이 문제가 난다"는 반증 한 줄이 필수. 없으면 강등, 있으면 복귀. 두 번 복귀하면
  "분쟁" 표시가 붙고 **구현자가 다시 정한다**(수용·재검증·범위표를 바꿔야 하는 방향 질문이면 `decisions raise`) — 하네스가 사용자 항목을
  자동으로 만들지 않는다(개선 2와 동일 원칙·2026-08-30 정정).
- 근거가 되는 번호는 **제외 칸만**(전제·금지는 근거 아님). 판단(어느 번호가 해당하는가)은 구현자가 한다 — 기계가 문안을 대조해
  추천하지 않는다(패턴 나열 금지).
- **구현됨(2026-08-31)**: 검증 시작 시 동결 레코드에 제외 칸(번호+제목)을 싣고(`ask-start`→`writeEnvelopeFreeze` oos), 판정 도착 자리에
  `[되받아침 재료 — 이 판의 제외 칸 · finding-judge <id> rebut --oos oos-n]` 한 줄(≤200자) · 되받아침 명령=`finding-judge <id> rebut --oos oos-n`
  (동결된 번호만 유효·처분 행에 oosId·장부 close 행 closeReason `implementer-oos` — 삭제 없음) · 다음 검증 요청에 `[구현자 되받아침(범위 밖)]`
  목록 자동 동봉(데이터 행·반증 규칙 문장은 이 목록 머리줄에만 — 되받아침 0이면 서식 안내는 origin 열거 값 추가 외 불변, 고정 산문 예산 ko 700·en 1,250 준수) · 재소환=`origin:"boundary-contest"`+`prevId`+`contest`(20~300자) — 입장 심사 규칙 2b(규칙 1 앞):
  없으면 [백로그] 강등(`contest-unproven`)·있으면 복귀(`contest-restored`)·같은 계보 두 번째 복귀=`contest-dispute`→판단 관문 마커
  `dispute:<id>`(구현자가 `round-judge`로 정함·사용자 항목 자동 생성 0). 확인 검증 반영: 되받아침 명령은 동결 askId가 이 작업공간의
  **마지막 검증 잡**과 같을 때만(이전 판 번호 거부)·종결 행을 **먼저** 놓은 한 번의 쓰기+쓴 뒤 다시 읽어 확인+장부 쓰기 전 손상 꼬리(개행
  없이 끊긴 조각)를 개행으로 격리(꼬리 판독이 권한·잠금 오류면 쓰지 않음)+되받아침 처분은 대응 종결 행이 실재할 때만 '판단됨'으로 계산
  (종결 없이 처분만 남은 장부=미판단 유지)+장부 자체를 못 읽으면 처분 관문이 검증 시작을 막음(경고만 내고 진행하던 종전 동작 폐지 —
  판독 오류 동안 '지적 0'으로 위장되는 통과 증명 차단)+관문 계산은 한 번 읽은 장부 스냅샷만 사용(재판독 0회 — 첫 판독 뒤 판독 실패 경합
  차단·계산 예외도 차단)·재료 줄이 200자를 넘으면 제목 대신 번호
  전량+`finding-judge --list-oos`(전문 조회 옵션 — 머리말 ≤125자라 축 상한 12칸도 ko/en 모두 절단 없음)·재소환 우회(같은 제목을 새 id·다른
  origin으로)는 정규화 제목 동일 키로 같은 지적으로 심사하되 **복귀는 세 필드 전부**(boundary-contest+되받아친 id를 prevId로+contest)일 때만·
  contest는 단일행 20~300자만(초과·다행=부재 취급, 잘라 살리지 않음). 남는 한계(정직 기록): 제목을 바꿔
  쓰고 origin을 거짓으로 단 재소환은 기계가 못 잡는다 — 이는 오늘 규칙 3(fix-induced 자기 신고)과 같은 검증자 신의 경계이며, 문안 유사도
  대조를 넣지 않는다(패턴 대조 금지). 시험 `tests/rebuttal.test.js` 10단언(CLI 실행·전 고리 e2e·예산·stale 동결·형식 반례).

**개선 2. 결정 장부(P3·P5)** = 결정이 "생기는 순간" 장부 항목으로 남기고, 화면과 명령줄이 **양식으로 출력**.
- **전제(사용자 역할)**: 사용자는 철학과 방향을 정하고, 개입은 승인·빼기 정도다. 그러므로 사용자 앞에 놓이는 것은 "골라 달라"는 목록이
  아니라 **방향 질문**뿐이어야 하고, 나머지는 구현자가 정한다. "기본값"이란 구현자가 스스로 고를 수 있는 합리적 선택이 있다는 뜻이다 —
  기본값이 있으면 그것은 사용자 결정이 아니다.
- **결정 행이 생기는 출처는 구현자의 판단 하나뿐(사용자 재확정 2026-08-30)**. 앞 판본(v4~v17)은 검증자 지적에 맞추다 "기계 상태(전량 강등
  보류·분쟁 보류)가 자동으로 사용자 결정 항목을 만든다"로 흘렀는데, 그것은 **구현자 판단을 빼 버린 설계**라 고치려던 병(P1·P5)의 재발이다 —
  검증자 지적이나 하네스 상태가 그대로 사용자에게 "정해 주세요"로 빠지면 쓸데없는 항목이 계속 사용자에게 흘러간다. 바른 그림: 검증자
  지적이 오면 **구현자가 먼저 정한다** — 수용·반박·보관함으로 이동·수칙서 후보로 상신·추가 검증 요청 중 무엇인지(이미 있는 처분 채널
  `finding-judge`·`backlog`·`rule-propose`·재검증). 그 판단 끝에 "이건 사용자가 방향을 정해야 하는 것"이 남을 때만 구현자가 결정 장부에
  올린다(`decisions raise`). 상황으로 말하면, 직원이 민원을 받으면 스스로 처리·보류·상부 보고를 가르고, 사장에게는 "회사 방향을 정해야 하는
  건"만 올린다 — 민원 창구가 사장 책상으로 직결되지 않는다.
- **구현자 상신의 구조 채널(산문 대신 필드)**: `decisions raise --kind <범위표|제품|위험|외부> --question --why --no-default "구현자가 스스로 정할 수
  없는 이유" --choice key=라벨[|고르면] (2개 이상) [--recommend key] [--ask askId] [--target-fp 승인 수칙서 지문]`. 하네스는 **형식만** 검사한다
  (종류·선택지 2개 이상·기본값 없는 이유 길이·권장 키) — "정말 기본값이 없는가"는 뜻을 읽어야 아는 문제라 기계가 가리지 않는다. 그 진위의
  통제는 ① 캠페인당 상신 수(§7 지표 — 많으면 제조된 결정 신호) ② 사용자의 "네가 정해라"(delegate) 선택 — 둘 다 장부에 남는다. 검증자가
  "자기신고라 기계가 진위를 못 가린다"고 두 번 지적했고 제가 두 번 기계 출처로 옮겼지만, 사용자 역할 모델(구현자=판단·실행)에서 그 자리는
  구현자 몫이다 — 이 지적은 앞으로 **설계 의도로 반박**한다.
- **판단은 프롬프트로 촉구하지 않고 관문으로 강제한다(장치화 — 사용자 재재지적 2026-08-30)**: "구현자가 정한다"는 문장은 흐려진다(실제로
  "그때만 올려라 한 줄"로 되돌아갔던 것이 그 증거). 그래서 ① 검증 판정이 구현자 판단이 필요한 상태(전량 강등 보류·앞으로 분쟁)가 되면
  하네스가 **마커**를 남기고 ② Claude·Codex 두 종료 훅이 그 마커가 있는 동안 **턴을 끝내지 못하게** 막으며(검증 불필요 턴도) ③ 구현자가
  `round-judge <askId> close-oos|re-verify|escalate --note "근거"`로 판단을 장부에 기록해야 풀린다. ④ **escalate(사용자 판단 필요)는 결정
  장부의 실존 항목 id(`decisions raise`로 만든 것)가 없으면 기록 자체가 거부**된다 — "사용자가 정해 주세요"를 산문으로 쓰고 지나가는 길이
  구조적으로 막힌다(스크립트 출력 강제). 지적 단위도 같다: `finding-judge <id> escalate --decision <id>`. 판단의 **내용**(어느 쪽을 고를지)은
  구현자 몫이고 하네스는 "기록됐는가"만 본다. 상황으로 말하면, 민원을 받은 직원이 처리 방향을 대장에 적기 전에는 퇴근 카드가 안 찍히고,
  "사장님 결재"로 올리려면 결재 서식이 반드시 붙어 있어야 하는 것 — 재판단 원칙(검증 설정의 규약 문안)은 그대로 두되 그것이 지켜지는지를
  이 관문이 본다.
- 검증자 블록 손상은 결정이 아니라 경보(아래).
- **마감문(상한 소진 보고)의 "사용자 판단 필요" 절 = 장부에서 렌더한 쉬운 말 블록만(2026-08-30 구현)**: 구현자가 산문으로 쓰지 않는다.
  정본 렌더(`contract-lib.renderDecisionBlock` — `decisions render`가 출력)가 장부 필드에서 조립한 블록("- 결정 <id>: 질문 / 왜 / 구현자가 못
  정하는 이유 / 선택 1·2 — 고르면 어떻게 / 권장 / 답하기")만 들어가고, 마감 검사기는 ① 블록 형식 ② 장부에 실존하는 열린 항목 id ③ **블록 전문이
  정본 렌더와 글자 단위(공백 정규화)로 같은지** ④ 그 절에 인용된 지적 키가 그 지적의 유일한 행선지인지를 검사한다 — 질문·선택지·권장을
  바꿔치기한 블록은 위조로 거부된다. 사용자는 번호가 아니라 **무엇이 왜 걸렸고 무엇을 고를 수 있는지**를 읽고 고른다. 검증자 답을 읽지 못한
  경우(EVIDENCE-UNAVAILABLE)는 사용자 판단이 아니라 [잔여 위험 판단] 절의 재검증 판단("즉시 재검증 — 이유: EVIDENCE-UNAVAILABLE…", '무시
  가능' 불가)이다. 구현자가 스스로 정한 것은 "정해서 진행함" 보고 행(처분 채널 기록)으로 남는다. 앞 판본의 "제안 행을 사용자가 승격"·
  "기계 상태만 결정"·"번호만 자동 삽입" 구조는 모두 철회.
- **단계별 기본 원칙 프롬프트(재판단 규약·판정 처리 안내)와의 연결(사용자 지적 2026-08-30)**: 규약 문안이 옛 방식("보류로 사용자에게
  선택을 넘겨라·분쟁/미해결/외부 3분류·산문 첨부")을 말하면 장치와 문안이 서로 다른 말을 한다. 그래서 하네스 기본 문안(재판단 규약의 해당
  항목·판정 '보류' 처리 안내, ko/en·핵심/무결성 4곳)을 장치의 절차("구현자가 먼저 정한다 → 방향 질문만 decisions raise → escalate
  --decision")로 바꿨다. §4-B ③ 규칙대로 하네스 소유 문장만 바꿨고(사용자가 대시보드에서 편집한 규약은 그대로), 지워진 문장의 강제는 판단
  관문 시험이 맡는다. 앞으로 원칙 문안은 "무엇을 판단하나"만 말하고, "지켜지는가"는 관문·검사기가 본다.
- **결정 블록 서식도 사용자 편집 계층(사용자 지적 2026-08-30 "서식을 코드에 박지 마라")**: 블록의 낱말과 배치(왜/구현자가 못 정하는 이유/선택/
  권장/답하기)는 코드 상수가 아니라 기본지침 파일(core·언어별)의 `decisionBlock` 서식에 살고, 렌더(`decisions render`·마감문)와 마감 검사기
  (서식 줄을 정규식으로 바꿔 머리말·라벨·정본 대조)가 같은 서식을 읽는다 — 사용자가 낱말을 고치거나 자리를 바꾸면 둘 다 함께 따라간다
  (`decisions template show|set|reset`, 대시보드 편집칸은 다음 단계). 줄마다 필수 자리표시자(id·질문·왜·못 정하는 이유·선택 키·라벨·**고르면
  어떻게**·권장)를 남겨야 저장되고(정보 무손실 — 빼면 저장 거부), 줄마다 자리표시자 앞 머리말이 있어야 하며 헤더 머리말이 다른 줄 머리말과
  겹치면 저장 거부된다(검사기가 줄 머리말로 헤더/본문을 가르므로 모호한 서식은 저장 단계에서 막는다). 파일을 직접 고쳐 자리표시자를 빼거나
  문법이 깨지거나 값이 객체가 아니면 기본 서식으로 렌더하며 경고를 남긴다(조용한 폴백 금지). 기본 원칙 세 축을 저장해도 서식 편집은 지워지지
  않는다.
- 출력: 개요 "지금 정할 것"에 합류 + 명령줄 `decisions list`(양식: 종류·질문·왜·구현자가 못 정하는 이유·선택지·권장·"네가 정해라") —
  사용자가 `choose`/`delegate`로 답하면 결과 행이 장부에 추가된다.
- **장부 계약(기존 후보 장부 규약 재사용)**: 항목마다 `decisionId`(멱등 키)·`wsKey`·`campaignId`·`sourceAsk`·대상 상태 지문(수칙서
  지문 등 그 결정이 전제한 상태)을 기록. 파일은 추가 전용(jsonl) — 생성 행과 결과 행을 분리하고 같은 id는 마지막 행이 이긴다(latest fold),
  결과는 원항목을 덮지 않는다. 선택 시 대상 지문을 재대조해 불일치면 거부하고 다시 출력(다른 창·다른 프로젝트의 항목을 종결하지 못함),
  기록 뒤 읽어 확인(read-back)한 것만 화면에 반영. 중단 후 재시작해도 결과 행이 없으면 항목은 열린 채로 남는다.
- **압박 차단 규칙(코드)**: 결정 행은 구현자의 구조 채널 밖에서는 생기지 않는다(장부 쓰기가 출처·종류·필드를 검사·하네스 자동 생산자 0 —
  시험이 소스를 핀으로 고정). 설계 문서에 새 사용자 결정용 §8을 신설하는 것은 금지 — 결정은 결정 장부로만. 캠페인당 상신 수와 "네가 정해라"
  응답 수가 §7 지표.
- **하네스 제조 결정 차단(P5②) — 구현됨(2026-08-30)**: 검증자 블록 손상(EVIDENCE-UNAVAILABLE)은 사용자 판단 칸에 들어갈 수 없고(검사기가
  거부), 마감문 [잔여 위험 판단] 절에 "즉시 재검증 — 이유: EVIDENCE-UNAVAILABLE(서식 재발급)"로만 둔다('무시 가능' 불가).

**개선 3. 사용자 보고 양식(P2)** = 파일을 바꾼 턴의 마지막 답에 세 칸 고정: "무엇이 바뀌었나 / 이런 상황이 이렇게 됨(예 1개) /
다음에 할 일". 종료 훅이 세 칸 제목 존재만 검사(마감문 검사기의 제목 위치 추출부만 재사용 — 절 내용·근거 형식 검사는 하지 않음).
기술 내용은 문서에 두고 링크만.
Claude 규칙 자체는 그대로 두되, 이 양식이 "지키는지 보는 경비원" 역할을 한다.
- **구현됨 2026-09-01(4-1)**: `verify-cap-handoff.js` REPORT_SECTIONS(ko/en 3제목·코드 소유 상수)·reportShapeCheck(줄 머리 앵커 — 마크다운 #·굵게·대괄호·글머리
  뒤 제목, 뒤에 글자·숫자가 바로 이어지면 불인정; 내용 무검사)·reportShapeInstruction(빠진 칸 목록+예시 1줄)·lastAssistantText/lastCodexAssistantText(마지막 답 1개).
  `verify-guard.js`(Claude Stop)·`codex-hook.js`(Codex 구현자 Stop) 양쪽: 파일을 바꾼 턴은 마지막 답에 세 제목이 있어야 종료(마지막 답이 없는 도구만 턴은 대상
  아님·검증 증명이 없으면 그 요구가 먼저), 상한 마감 턴은 마감문 7절 수락 조건에 세 제목을 추가(빠지면 그 칸만 요구). 반복 차단은 기존 상한(epoch에
  reportOk 포함). 사용자 규칙(고정 규약 JSON)은 그대로 — 경비원은 하네스 소유 양식만 본다. 시험 `tests/report-shape.test.js`(종료 훅 실행 반례 6종).
  **확인 검증 1·2회차 blocker 반영**: ①검증 모드 off라도 경비원은 적용(off는 검증·잔여 마커·판단 관문·상한을 보지 않고 세 칸만 검사 — 종전 조기 종료가
  경비원을 통째로 건너뛰던 결함; Claude 경로 verifyOff·Codex 구현자 경로 vmOff 동일 경계 — 2회차: C-C가 off에서도 잔여/판단 장부를 읽던 불일치 정렬) ②마감 턴의 세 칸도 일반 턴과 같이 '마지막 답'에서만 검사(누적 텍스트면 앞선 답의 제목으로 통과하던 결함 — verify-sequential
  Claude·Codex 양쪽 반례).

**같이 처리(코드 작은 것)**: (P6) 검증자 블록 손상 시 다음 요청에 "n번째 줄·사유 키 — 서식 재발급" 절을 자동 동봉(원문 비복사 유지) — **구현됨 2026-09-01**:
`contract-lib.js` writeReissue/readReissue/clearReissue(`verify-findings/<wsKey>.reissue.json`·좌표+사유 키 8개 이하·원문 없음)·reissueSectionFor(같은 캠페인일 때만·
사유 한글/영문 라벨) · `codex-bridge.js` machineFindingsLayer가 손상 판독 뒤 기록·정상 판독 뒤 소거, v2DynamicData가 절을 동봉 ·
(P7 앵커 이탈 3종) ⓐ 훅 스크립트는 진짜 훅 입력(stdin에 이벤트 이름·세션 id)이 있을 때만 앵커를 쓰고, 불러오기만 됐을 때는 아무것도 쓰지
않는다 ⓑ 세션 도중 앵커의 폴더가 바뀌면 `ask-start`가 "이 대화의 폴더가 A에서 B로 바뀌었다"를 알리고 시작하지 않는다(명시 플래그로만
진행) ⓒ 프로필·상한 설정이 없는 폴더에서 검증이 시작되면 "기본값(감사 프로필·상한 없음)으로 돈다"를 요청·판정 양쪽에 1줄 고지 ·
(P10) 마감문의 "N/M" 문구는 캠페인 장부와 대조해 다르면 마감 미수락(산문이 장부를 덮어쓰지 못함) — **구현됨 2026-09-01**: 두 종료 훅이 handoffCtx에 장부
roundCount/roundBudget을 실어 `validateCapHandoff`→`roundFigureMismatch`가 마감 머리부터 끝까지 "키워드(회차·round·상한·cap)가 숫자 바로 앞이거나 숫자 바로 뒤에
회차/round" 꼴의 N/M만 대조(키워드~숫자 사이 조사·콜론·굵게 6자 이내 허용 — "회차는 3/5입니다"·"round is 3/5"도 회차 문구; 영문 키워드는 단어 경계 필수 —
recap·background·roundtrip·pre_cap·round_trip은 키워드 아님(\w 경계); 다르면 미수락 `round-figure-mismatch`·숫자 없으면 검사 없음·"성공 확률 3/4" 같은 일반 분수는 회차 아님 — 1·2회차 blocker:
일곱 절 본문만 보면 머리의 숫자를 놓치고, 12자 근접 규칙은 일반 분수를 오인하고, 경계 없는 영문 키워드는 단어 안 cap/round를 오인), 안내문에 실제값 명시 · (P9) 설계 경위 색인에 오늘 문서
3편(검증 거버넌스·재판단 권위·이 문서)의 결정 이력을 등재.

**개선 4. 글 다이어트를 구조로(P11) — 프롬프트를 늘리는 항목은 0, 줄이는 항목만** (아래 (a)~(e)는 §4-B 원칙의 첫 적용분이다 —
"이번 프로젝트 글을 줄이는 일"이 아니라 "글이 자라지 않는 구조"의 1차분)
- (a) **경계 중 '제외' 본문만 파일 참조로**: 검증자가 채점 전에 알아야 하는 것은 전문 유지 — 절대 차단 8줄(~650자), **전제(우리가 지키기로
  한 세계 — 예: "여러 창을 동시에 쓰는 환경도 지원" 같은, 검증자가 어떤 상황을 재현해 볼지 정하는 근거)**, 보관 선별분(alwaysBlocker 권위),
  열린 지적. 파일 참조로 바꾸는 것은 **제외 칸(안 다루는 상황) 본문뿐**이며, 번호와 한 줄 제목은 인라인으로 남긴다("oos-3 서버 두 대 동시
  배포 — 본문: <동결 파일>#oos-3"). 이유: 제외 칸은 검증자가 실패 판정을 내리는 근거도, 탐색 범위를 정하는 근거도 아니고 구현자가
  되받아칠 때 드는 근거(§1·개선 1-B)라, 검증자가 본문을 열지 않아도 "안 읽어서 통과시키는" 경로가 생기지 않는다 — 안 읽으면 범위 밖 지적을
  더 올릴 뿐이고 그것은 구현자가 되받아친다. (전제까지 참조로 돌리면 검증자가 지원 환경을 모른 채 재현을 건너뛰어 결함을 놓칠 수 있다 —
  2회차 검증 지적으로 되돌림.) 절감은 약 400~500자로 작다; 다이어트의 본체는 (b)·(c)다.
- (b) **지도·결합·정찰 동봉을 요청문 뼈대의 [직접 범위] 절에 결속**: 직접 범위가 문서·설계뿐이면 셋 다 생략, 코드가 있을 때만 그 파일
  기준으로 고른다. 지금의 "요청문 어디든 파일 이름이 보이면" 방식(씨앗 추출)은 뼈대 절이 있을 때 쓰지 않는다 — 새 판정 규칙이 아니라
  이미 있는 절 구조를 쓰는 것.
- (c) **총량은 조각 상한의 합으로 파생하고, 절단은 참고 자료만·순서 고정**: 머리 총량 관측 목표 6,000자(**열린 지적은 이 총량에 넣지
  않는다** — §4-B ④: 예산 밖·캠페인 유계). 줄이는 순서는 **참고 자료만**(지도 ≤1,500 · 결합 ≤400 · 정찰 ≤200 · 경위 ≤300 → 초과 시 이 순서로 먼저 생략, 기존 "잘림
  고지"로 알림). **권위 자료는 절단 금지** — 절대 차단 8줄·전제·보관 선별분·경계 참조 1줄, 그리고 열린 지적(다음 판이 id로 재검사해야 하는
  것 — 잘리면 검증자가 못 본 채 통과하고 하네스가 자동 종결하는 길이 열림). **시작 거부는 두지 않는다**(2026-08-30 철회 — §4-B "넘칠 수
  없게 만든다"): 총량 상한은 각 조각 상한의 합으로 파생되므로 구성상 넘을 수 없고, 넘으면 코드 결함으로 시험이 잡는다. 열린 지적이 많다는
  이유로도 당연히 시작을 막지 않는다(그 지적을 닫는 길이 검증 자체이므로). 고정 산문 예산 시험과 같은 방식으로 총량·절단 순서 시험을 둔다.
- (d) **판정 도착 자리(구현자 쪽) 총량 예산 = 현재 1,863자 고정(1차)**: 개선 1-A의 범위표는 "제외 칸 번호+한 줄"(≈300자)만 싣고, 같은 분량을
  재판단 규약 산문에서 뺀다(전문은 되받아침 명령의 조회 옵션으로 연다). 늘리려면 같은 분량을 빼는 규율(08-06과 동일). §4-B ①이 끝나
  재판단 규약이 세션 1회 전달로 옮겨가면 이 예산은 "상태 줄+열린 지적+≈300"으로 다시 내려간다(§4-B 목표 수치).
- (e) **매 턴 검증 지시 1,107자 → 명령 줄만**: 종료 훅·예약기가 실제로 막는 규칙 문장(동시 1개·순차·상한·마감 형식 등)은 이미 그 자리에서
  알려 주므로 지시문에서 뺀다 — 단 §4-B ③의 조건대로(하네스 문장만·문장별로 실행 반례 훅 시험 id가 있을 때만·"구조로 옮김" 장부에 기록).
  남기는 것은 시작·대기 명령 2줄과 진행도 1줄(≈400자).
- 지표: 검증자 머리(열린 지적 제외 총량) 중앙값 8.5천 → 6,000 이하, 구현자 매 턴 주입 2.0천 → 1.5천 이하, 판정 꼬리 1,863 유지. 각각 시험이 숫자를 직접 잰다.
- **구현됨 (a)(b)(c) — 2026-08-31(3-2a)**: (a) `envelopeInjectionFor` 제외 칸만 60자 제목+`본문: <대상 저장소 절대 경로>/verify-envelope.json outOfScope[n]`
  참조(검증자 cwd가 대상과 달라도 같은 파일 — 확인 검증 blocker①; 전제·절대 차단·짧은 항목은 전문) · (b) `map-reader renderV2Slice`가 씨앗을 요청문
  `[직접 범위]` 절 본문(`askSectionBody`: 다음 뼈대 절·'제목 낱말' 대괄호(허용 문자 양성 열거: 유니코드 글자·공백·가운뎃점·하이픈 30자 이하 — 경로·색인·배열 `[a, b]`·식별자 `[a_b]`·마크다운 링크 대괄호는 본문)·마크다운
  제목에서 끝남, 절 없음=null→종전 전체, 절 있음·비어 있음=""→씨앗 0→지도 생략 — 전체로 폴백하지 않음)에서만 뽑음 · (c) 참고 자료
  조각별 예산 `REF_BUDGET`(경위 300·지도 1,500·결합 400·정찰 200)을 `applyReferenceBudgets`가 순서대로 적용(초과=절단+참고용 고지·권위 자료는 경유
  안 함 — 절단 고지·조각 사이 개행까지 포함한 참고 출력 전체가 상한 합 2,400을 넘지 않으며, 차감으로 상한이 고지보다 작아지면 짧은 표지 `…[절단]`
  또는 조각 생략[clipped.omitted]), 총량 `HEAD_SOFT_LIMIT=headBudgetTotal()`=조각 상한의 합(`HEAD_BUDGET`: 상태 줄 400·고정 산문
  5,200[=`FIXED_PROSE_CAPS` 언어별 합의 최댓값·tests/verifier-head와 단일 출처]·경계 3×12×200+600·선별 4,000+400·계약 4,000·참고 2,400 — 계약 상한
  `CONTRACT_INJ_MAX`도 같은 출처)이며 열린 지적(v2Data)은 측정에서 제외, 초과=stderr+무결성 경보 1건(차단 없음). 선별 초과 바이트는 '실제로 실리는
  문안'(en 판은 번역문) 기준. 열린 지적 캠페인 유계=판당 행 상한 `FINDINGS_ROWS_MAX` 40(초과=고유 사유 `too-many-rows`로 보류 강등+사유 문장 "40행 이하로
  나눠 다시 제출"+서식 안내에 상한 명시) × 회차 상한. 손상 줄 좌표·사유를 다음 요청에 자동 동봉하는 절(P6)은 §7 4번에서. 시험 `tests/head-budget.test.js` 7단언(구성 시험: 경계 3축 만재+계약 상한
  여유분 합산 ≤ 파생 상한·열린 지적 200행 전량 동봉·측정 밖). 6,000 중앙값은 운영 관측.
- **구현됨 (e)(d) — 2026-08-31(3-2b)**: (e) Claude 매 턴 검증 지시는 `buildVerifyDirectiveSlim`(머리 1줄+시작·대기 명령+진행도, ko ≈450·en ≈610자)만;
  규칙 문장·[원격 확인]·[전달 원칙]·[재판단] 한 줄은 `buildVerifyDirectiveStatic`으로 세션 1회 전달(§4-B ①). 문장 조각의 단일 출처
  `verifyDirectivePieces`(full=slim+static·종전 구성 동일). 지시문에서 뺄 문장은 §4-B ③ 장부 `DIRECTIVE_MOVED`(문장 ko/en·막는 훅·실행 반례 시험
  파일+제목)에만 — **현재 0건**: 후보였던 "사용자 판단 항목이 있을 때만 한 번에 묻고, 없으면 선택지를 만들지 마라"는 훅이 상한 마감문·escalate 경로만
  막고 평범한 턴의 선택지 생성은 막지 못하므로(확인 검증 1회차 지적) 정적 블록에 유지. 사용자 글(계약 규칙·전달 원칙)은 대상 아님·바이트 그대로. (d) 판정 꼬리의 재판단 규약: 이 Claude 세션에 전달된 문안과 같은 지문이면
  `rejudgeTailFor`가 포인터 1줄(세대·전달 턴 시각)로 바꿈(−1,000자 이상) — 압축·재개 뒤 첫 판정은 다시 전문.

## 4-B. 원칙 — 프로젝트가 오래·크게 갈수록 글을 넣을 필요가 없어지는 구조 (사용자 지시 2026-08-30)

사용자님 지적의 뜻: 개선 4는 이 프로젝트의 봉투를 한 번 비우는 일이 아니라, **이 하네스로 운영되는 어떤 프로젝트든 장기화·대규모화해도
주입 글이 자라지 않게 만드는 원칙**이어야 한다. 그래서 "글이 왜 존재하는가"를 다섯 성격으로 나누고, 성격마다 글 대신 놓을 구조를 정한다.
상황으로 말하면, 매일 아침 전 직원에게 사규 전문을 낭독하는 회사가 아니라 — 입사 때 한 번 읽고, 그날 필요한 것만 게시판에 붙고, 지켜야
할 것은 출입문이 막는 회사로 바꾸는 것.

**다섯 성격과 각각의 자리**

| 성격 | 지금 글로 있는 예 | 글이 자라는 축 | 글 대신 놓을 구조 | 남는 글 |
|---|---|---|---|---|
| ① 정적 지시(매번 같은 문장) | 검증 기본 원칙 948·응답 서식 526·경계 규칙 215·재판단 규약 1,402·검증 지시 1,107 | 패치마다 누적(시간) | **세션 1회 전달 + 세대 결속** — Claude: 세션 시작 훅(시작·재개·압축 후에 발화 — 압축 복원 채널로 이미 쓰는 통로) / Codex 검증자: 검증자 세션은 브릿지가 만들고 이어 쓰므로(resume) **세션을 만드는 첫 메시지에 1회**, 이후 이어 쓰기엔 상태 줄만. 전역 `AGENTS.md`는 쓰지 않는다(구현자 Codex도 같은 채점 규칙을 받게 돼 역할 혼합), `model_instructions_file`도 쓰지 않는다(내장 지시를 대체하는 키). 사용자가 편집하는 계약·프로필·재판단 규약은 "정적"이 아니라 **세대가 있는 지시** — ②의 세대 지문으로 결속하고, 세대가 바뀐 턴·압축이 감지된 턴에만 1회 재전송. Claude 쪽도 같은 규칙: 세션 앵커 레코드에 "전달한 G"를 적고, 프롬프트 훅이 현재 G와 다르면 그 턴 1회 재주입, 압축은 세션 시작 훅(source=compact)이 담당 | 0(세션·세대당 1회) |
| ② 동적 상태 | 캠페인 id·회차 N/M·앵커 폴더·수칙서 지문·진행 단계 | 없음(상수) | 한 줄 상태 표시("캠페인 …·회차 2/5·폴더 …·경계 지문 …·규약 세대 …") — 이 줄이 P7·P10의 신호이기도 하다. **전달·보존 증명은 검증자 자기신고가 아니라 호스트(브릿지) 기록으로**: 브릿지가 검증자 세션에 규약 전문을 보낼 때 "세대 지문 G를 메시지 k번째에 전송"을 연결 레코드에 남기고, 다음 ask 전에 ⓐ 현재 세대 ≠ 기록된 G(규약이 바뀜) 또는 ⓑ 브릿지가 이미 판독하는 검증자 스레드 기록(rollout)에 그 전송 이후 **압축 이벤트**가 있으면 전문을 1회 재전송하고 기록을 갱신한다. 압축은 ask **실행 도중**에도 일어나므로(스레드 기록 실물: 도구 호출 뒤 압축, 같은 턴에서 답 계속) 판정을 받아들이기 **직전에도 같은 검사(postflight)**를 한다 — 전달 레코드의 기록 위치 이후 새 압축이 있으면 그 판정은 권위 없음(보류)으로 두고 전문을 재전송한 재판만 수락한다. 검증자가 번호를 되뇌는 필드는 두지 않는다(상태 줄에서 베껴 쓸 수 있어 증명이 아님). 세대 지문 G = sha(기본 검증 원칙·응답 서식·경계 규칙·재판단 규약·프로필·계약의 검증자 지시)로 역할별 산출 | ≈200자 |
| ③ 강제 가능한 규칙 | "동시 1개·순차 회차·통과하면 멈춰라·상한 시 마감 형식·선택지 도구로 물어라" (전부 **하네스 코드가 만든 문장**) | 사고마다 문장 추가(시간) | **대상은 하네스 코드가 소유한 문장뿐** — 사용자가 쓴 글(계약의 규칙·체크리스트·수칙서·프로필 편집분)은 바이트 그대로 두고 자동 삭제·"등가 판정"의 대상에서 제외한다(사용자 지적 2026-08-30: "왜 내가 쓴 글이 사라졌지?"의 원천 차단·시험으로 내용 동일 고정 — 종전 주입 규약의 앞뒤 공백 정리·빈 항목 제거만 예외). 하네스 문장은 자동으로 지우지 않고 **설계 시 사람이 문장별로 결정**하되, 조건이 하나 있다 — "그 문장을 지워도 훅 시험이 그 위반을 실제로 막는다"는 **실행 반례 시험 id**가 있을 때만 지우고, 지운 문장은 "구조로 옮김" 장부(문장→훅→시험 id)에 남겨 대시보드 '하네스 주입 지침' 카드에서 "훅으로 옮긴 규칙 N개(보기)"로 열람할 수 있게 한다. 지표(하네스 문장 속 "…하라" 수, 목표 0)는 하네스 글에만 적용 | 0(하네스 글) · 사용자 글 불변 |
| ④ 판단 재료(데이터) | 수칙 경계 1,753·지도 조각 2,776·결합 680·정찰 293·경위 581·열린 지적 270 | 프로젝트 규모(파일·수칙 수)·이력(지적·경위) | **요청의 기계 판독 뼈대([직접 범위] 등)에 결속해 선별 + 조각별 예산 + 절단 순서(참고 자료만)**. 권위 자료(절대 차단·전제·선별)는 전문·절단 금지, 참고 자료는 참조+요청 시 조회. 총량 때문에 검증을 거부하는 일은 없다(아래 "넘칠 수 없게 만든다"). **열린 지적은 예산 밖**(현행 시험 계약 "열린 지적 데이터는 예산 밖·상한 금지"와 동일) — 총량 시작 거부의 대상이 아니다. 그 대신 캠페인으로 유계: 회차 ≤5 × 판당 지적 행 상한(파서에 신설 — 초과는 서식 손상으로 재발급). 그래야 "지적이 많아 문을 잠갔는데 그 지적을 닫으려면 문 안에서 통과받아야 하는" 순환 정지가 안 생긴다. 규모가 10배가 돼도 동봉은 예산 상수 + 캠페인 유계 | 예산 합(상수)+캠페인 유계 |
| ⑤ 서식 계약 | 지적 서식 v2 326·범위 확장 통로 200·MAP 라우팅 114 | 서식 개정마다(시간) | **파서+재발급 루프**(손상 줄 좌표·사유로 재요청 — 이미 있음)로 강제하고, 서식 설명은 ①과 같이 세션 1회 | 0(세션당 1회) |

**구현됨 3-2b(2026-08-31 · Claude 쪽 ①③)**: 세션 앵커 `active/<claudeSession>.json`의 `directive{gen,parts,sentAt}` 전달 기록 — 성분 7(검증 지시·원격
확인·전달 원칙 / 수칙 인지 ab 전문 / 설계 경위 / 재판단 규약 / 모드 / 프로필 / 언어)의 지문·세대 G. UserPromptSubmit(`contract-inject`)은 기록 없음·리셋·
세대 변경에만 정적 블록(`claudeStaticBlock`) 전문을 넣고, 매 턴은 사용자 규칙(그대로)+명령 줄(slim)+상태 줄 `[규약 전달 · Claude] 세대 G · …`+동적 신호
(이번 턴 선별 캐시·지도 상태)만 — 하네스 소유분 매 턴 예산 `CLAUDE_TURN_BUDGET`(slim 700·상태 줄 250·안내 550=1,500, 문서 지표)을 코드가 조립
'전체'에 지킨다: 고정 조각(자동 전환 고지·slim·상태 줄)+조각 사이 구분자를 먼저 세고 남은 자리에서 안내(지도 상태·부트스트랩)를 순서대로 절단·생략(고지
포함), 고정 조각은 코드 소유 문장이라 시험이 각 상한(slim ≤700 전 모드·언어·진행도 / 상태 ≤250 전 사유 / 전환 고지 ≤300)을 구성으로 보장하고 그래도
넘으면 overflow 표시(stderr)·안내 전량 생략. 사용자 규칙(사용자 글)·선별 캐시(권위 데이터)는 예산 밖(시험: 지도 없음 안내가 붙는 턴 ≤1,500·경계 입력·
전환 고지 동반 단위 반례). 전달 기록은
'출력이 실제로 전달된 뒤'(stdout write 콜백)에만 확정 — 출력 전 종료·파이프 실패면 기록이 없어 다음 턴이 전문(확인 검증 blocker②). 압축·재개·시작은 새
SessionStart 훅 `session-start.js`가 앵커 기록을 리셋해 다음 턴이 전문을 다시 보낸다(사유: 세션 compact 등 — 추측 아님·훅 이벤트 실물); 앵커 쓰기
실패·부재 아닌 판독 오류(잠금·손상·EISDIR)=마커 파일 폴백(옛 기록이 복구돼도 마커가 전문을 강제), 둘 다 실패=훅 비0 종료+stderr(조용한 fail-open
금지 — blocker③; 어떤 파일도 못 쓰는 환경의 잔여 위험은 훅 오류 표시로 드러남). 설치기·hook-setup 양쪽 목록에 등록(훅 6개). ③ 장부
`DIRECTIVE_MOVED`는 대시보드 'Claude 규칙' 카드 위 "훅으로 옮긴 규칙 N개(보기)" 접힘으로 열람(0건이면 접힘 숨김). 시험 `tests/claude-directive-delivery.test.js` 9단언
(훅 실행 e2e: 첫 턴 전문→둘째 턴 slim→기본 지침 편집·프로필 전환=세대 변경 전문→compact 리셋 전문→다른 세션=별도 앵커·판정 꼬리 포인터).

**구현됨 3-1(2026-08-31 · 검증자 쪽 ①②)**: 전달 레코드 `~/.codex-bridge/directive-delivery/<검증자 세션>.json`(`directive-delivery-v1`: 세대 G=성분별
지문 6개[기본 원칙+서식·경계 규칙·지적 서식 고정 산문·계약의 검증자 지시·프로필·언어]의 sha, sentAt, askId, rolloutFile, pending) — 새 세션 첫 메시지·
이어 쓰기 첫 판=전문(상태 줄 첫 줄 `[규약 전달] 세대 G · 이번 판 전문 전송(사유: …)`), 이후 판=상태 줄+데이터(경계 데이터·서고 선별·열린 지적·되받아침·지도)만.
재전송 트리거 4종 전부 기록 기반: 세대 변경(바뀐 성분 표기)·보낸 시각 뒤 rollout `compacted`·직전 전달 미확정(pending: 호출 실패/검증 도중 압축)·기록
판독 실패(안전 방향 — 레코드 손상/권한은 `record-unreadable`로 사유 구분). postflight: 답 수신 직후 "호출 시각 뒤 compacted"가 있거나 기록을 못 읽으면(압축
여부 미상) 판정 권위 없음 — 기계 판정 `compacted-mid-ask`/`postflight-unreadable` 보류 강등+**통과 증명(proof) 미기록**(종료 훅이 재검증 전 종료 차단)+판단
관문 마커(구현자 `round-judge <askId> re-verify` 또는 `escalate --decision` — close-oos 불가)+레코드 pending(다음 판 전문)+**실행 자체를 실패로 종결**(비0 종료
`HOLD_EXIT_CODE` — 내구 경로에서 job=failed라 ask-wait이 영수증 없이 출력을 돌려주고 같은 턴의 새 ask-start가 막히지 않음; 확인 검증 2회차 blocker ab-6 봉합:
succeeded인데 proof가 없어 `proof-missing`↔`먼저 ask-wait` 순환이 생기던 경로). (확인 검증 blocker① ab-3 봉합) 표시 3곳=상태 줄 문자열 하나에서 파생(머리 첫 줄=판정 하단 줄=대시보드
카드 제목 병기=개요 '최근 검증 결과' 배지). 사용자 글(계약 규칙)은 전문 판에 내용 그대로 — 유일한 정규화는 종전부터 있던 주입 규약(앞뒤 공백 정리·빈 항목
제거)뿐이고 문장은 한 글자도 바뀌지 않는다(확인 검증 blocker② 정정: "바이트 그대로"는 이 정규화를 제외한 뜻). 무상태 검증자(claude)는 계획 없음=종전 전문. 실측(시험
픽스처 ko·수칙서 1항×3축·규칙 2줄): 전문 판 대비 상태 줄 판 절감 ≥2,000자(`tests/directive-delivery.test.js` 10단언 — 트리거 4종·postflight 보류·
실행 중 편집 불변·표시 파생·배선 순서). Claude 쪽 ①(재판단 규약 꼬리·검증 지시 명령 줄만)과 ③④(하네스 문장 장부·규모 시험·선별 초과 전량 동봉)는 3-2.

**하네스 불변식으로 승격(시험이 지킨다)**: "한 턴에 주입되는 글 = 상수(②) + 예산 데이터(④, 열린 지적 제외) + 캠페인 유계 열린 지적" —
프로젝트의 파일 수·수칙 수·경위 수·캠페인 수를 픽스처에서 10배로 늘려도 예산 부분이 예산 안임을 시험이 재고, 열린 지적 부분은
"회차 상한 × 행 상한"이라는 유계를 시험이 확인한다(고정 산문 예산 시험과 같은 방식의 **규모 시험**). 유한 픽스처가 무한 성장을 증명하지는
못하므로, 성장 축마다 "상수·예산·유계 중 무엇으로 막는가"를 위 표에 명시하는 것이 증명의 본체다. 이 시험이 있어야 "다이어트를 한참
했는데 또 8천자"가 재발하지 않는다 — 08-06은 시험이 산문만 쟀기 때문에 데이터로 다시 불었다.

**압축을 어떻게 판정하고, 사용자는 무엇을 보는가(①·② 운영 규칙 — 사용자 질문 2026-08-30)**
- 판정 근거는 추측이 아니라 기록이다: 검증자 스레드 기록(rollout)에는 압축이 `type:"compacted"` 줄로 **시각과 함께** 남는다(실물:
  `2026-08-30T01:55:00.485Z compacted` — 앞 줄은 토큰 집계, 뒷줄은 새 세계 상태). 브릿지는 전문을 보낼 때 전달 레코드에 `{세대 지문 G,
  성분별 지문, 보낸 시각, 기록 파일}`을 적고, ask 직전과 판정 수락 직전에 "보낸 시각보다 뒤에 compacted 줄이 있는가"만 본다 — 예/아니오로
  갈리는 판정이라 오판 여지가 없다. 기록 파일을 못 읽으면 **안전한 쪽(전문 재전송)**으로 간다(재전송은 비용일 뿐 위험이 아니다).
- 사용자가 보는 곳 세 군데(새 화면 없음): ⓐ 검증 답 하단 상태 줄에 `[규약 전달] 세대 G · 이번 판 전문 재전송(사유: 압축 감지 01:55 / 규약
  변경: 재판단 규약)` 또는 `[규약 전달] 세대 G · 재전송 없음` 1줄 ⓑ 대화 카드의 기존 '🔒 하네스 주입 지침 N자' 접힘 제목에 같은 표시(펼치면
  보낸 전문) ⓒ 개요 '최근 판정' 줄의 작은 배지. 재전송은 **정보 행**이지 경보가 아니다(경보 남발 금지) — 단 postflight에서 "검증 도중
  압축"이 잡혀 판정을 보류로 내린 경우는 기존 '보류' 강등 경로(경보+재판)를 그대로 탄다.
- 지시가 중간에 바뀌면: 세대 지문 G는 ask 시작 시 **동결한 스냅샷**에서 계산한다(재판단 규약·계약·프로필은 이미 시작 시 동결해 완료 처리까지
  같은 문안을 쓰는 것이 현행 규칙) — 성분별 지문(기본 원칙·응답 서식·경계 규칙·재판단 규약·프로필·계약의 검증자 지시)을 따로 두어, 전달
  레코드와 다르면 그 ask에 전문을 다시 보내고 상태 줄에 **무엇이 바뀌었는지**(어느 성분)를 적는다. 검증이 도는 **도중**에 사용자가 편집한
  내용은 그 ask엔 적용되지 않고 다음 ask부터 적용된다 — 대시보드는 "저장됨 · 다음 검증부터 적용"을 보여 준다(수칙서 '승인 없이 바뀐 수칙'
  카드와 같은 문법). 상황으로 말하면, 시험지가 이미 배부된 뒤 바뀐 채점 기준은 다음 시험부터 적용되고, 답안지 표지에 "채점 기준 3판
  적용"이 찍혀 있어 어느 기준으로 채점됐는지 누구나 안다.

**"넘치면 시작 거부"는 철회 — 넘칠 수 없게 만든다(사용자 질문 2026-08-30)**
- 하네스가 스스로 붙이는 자료 때문에 하네스가 멈추는 설계는 잘못이다. 총량 상한은 마법 숫자가 아니라 **각 조각의 기존 상한의 합으로 정의**한다:
  고정 산문 예산(시험이 지킴) + 수칙서 상한(축당 12항×200자, 이미 코드) + 보관 선별분(아래 — 상한 초과 시에도 전량·서고 상한 96항이 절대 상한) + 참고 자료 예산(지도
  ≤1,500·결합 ≤400·정찰 ≤200·경위 ≤300) + 상태 줄. 참고 자료는 예산 안에서만 골라 싣고 잘린 수를 고지한다(기존 잘림 고지). 권위 자료는
  자기 상한을 이미 갖고 있으니 절단도 거부도 없다. 따라서 **구성상 총량을 넘을 수 없고**, 넘는다면 코드 결함이므로 시험이 잡고 실행 시엔 경보
  1건으로 알린다(멈추지 않는다).
  단, 지금 코드에 **반례가 하나 있다**: 보관 선별 합집합이 12항 또는 4,000바이트를 넘으면 `overflow`로 **검증 작업이 실패**한다(서고는 최대
  96항이라 "이번 작업 관련"이 13항이면 재현). 이것은 "권위 자료 때문에 하네스가 멈추는" 바로 그 설계라 §4-B에서 바꾼다 — 선별분은 사용자가
  승인한 절대 차단 수칙이므로 **한 항목도 빼지 않고 전량 싣는다**(12항·4,000바이트는 실패 조건이 아니라 "정상 범위" 표시로 격하). 넘친
  판에는 답 하단에 "관련 수칙 N항 — 정상 범위(12항) 초과, 전량 동봉" 정보 행을 남기고 대시보드 서고 카드에 "관련 수칙이 12항을 넘음 —
  서고 정리 권장"을 띄우며, 그 판의 참고 자료 예산을 넘친 만큼 줄인다(참고 자료는 잘려도 안전·권위 자료는 잘리면 위험). 선별기가 순위를
  내지 않으므로(관련 id만 반환·arc 번호 정렬) "상위만 싣기" 같은 순위 의존 규칙은 두지 않는다. 절대 상한은 서고 자체의 96항이며 그 서고를
  줄이는 것은 사용자의 정리 결정이다. 실패 없음·자동 삭제 없음·누락 없음.
  **구현됨 2026-08-31(3-2a)**: `selectorUnion`은 초과를 실패로 돌리지 않고 `over{items,bytes,count,bytesTotal,excessBytes}`로 표시(전량 선별), worker는 job을
  실패시키지 않고 `.err`에 정보 1줄, 브릿지는 서고 선별 절에 "(관련 수칙 N항 · M바이트 — 정상 범위 초과, 전량 동봉 — 누락 없음)" 정보 행+판정 하단
  `[서고 수칙] … 서고 정리 권장` 정보 행(경보 아님)+초과 바이트만큼 참고 자료 예산 차감(경위→지도→결합→정찰 순). 대시보드 서고 카드 표기는 3-2b의 작은
  항목으로 남김(판정 하단 줄은 대화 카드에서 보임).
  **범위 한정**: 이 절이 없애는 것은 "총량 때문에 생기는 추가 시작 거부"뿐이다. 지금 있는 무결성 관문 — 진행 중 작업, 손상된 job, 승인 없이
  바뀐 수칙, 서고 드리프트, 원문 스냅숏·미리보기 영수증 부재, 계약 규칙 4,000자 초과 — 는 검증 통과 위조를 막는 장치라 그대로 둔다(그 중
  어느 것도 다이어트의 삭제 대상이 아니다). "6,000"은 상한이 아니라 **관측 목표**(열린 지적 제외 머리 중앙값)이며, 상한 합계는 코드 상수에서
  파생해 문서에 적지 않는다(숫자 두 곳 관리 금지).

**사고·회귀 방지 시험(이 절의 계약)**
1. 재전송 트리거 3종 실행 시험: 세대 변경 → 재전송+상태 줄 성분 표기 / 보낸 시각 뒤 compacted → 재전송 / 검증 도중 compacted → postflight
   보류+재판. 기록 파일 판독 실패 → 재전송(fail-safe).
2. 사용자 글 불변 시험: 계약 규칙·체크리스트·수칙서·프로필 편집분이 어떤 다이어트 경로에서도 **내용 동일**하게 실린다(허용되는 유일한 차이=종전 주입
   규약의 앞뒤 공백 정리·빈 항목 제거 — 그 외 문장·순서·항목 수 변경 0. 3-1의 시험은 규칙 본문이 주입 JSON 안에 그대로 있음을 고정).
3. 하네스 문장 삭제 시험: "구조로 옮김" 장부의 각 문장에 훅 시험 id가 있고 그 시험이 실행 반례로 통과해야만 삭제가 유효(장부에 시험 id 없는
   삭제=시험 실패).
4. 총량 구성 시험: 각 조각을 상한까지 채운 픽스처에서 합계가 파생 상한과 같고, 참고 자료만 잘리며, **총량·선별 초과를 이유로 한** 시작 거부·
   작업 실패가 나오지 않는다(선별 13항 픽스처 → **13항 전량 동봉**+초과 정보 행 / 4,000바이트 초과 픽스처 → 전량 동봉+참고 자료 예산 축소
   — 어느 픽스처에서도 승인 수칙 누락 0). 기존 무결성 관문(진행 중 작업·손상 job·미승인 수칙·서고
   드리프트·영수증 부재·계약 4,000)은 같은 시험에서 **여전히 막는지**도 함께 확인한다(삭제 회귀 방지).
5. 표시 시험: 상태 줄·카드 제목·개요 배지가 같은 전달 레코드 한 곳에서 파생된다(세 곳이 다른 값을 보이는 일 없음).
6. 실행 중 편집 시험: ask가 도는 동안 기본 원칙·재판단 규약·프로필·계약을 편집해도 현재 job의 G·문안·판정 꼬리는 시작 시 동결값 그대로이고,
   다음 ask에서만 새 G로 전문 재전송+상태 줄에 바뀐 성분이 표기된다(실행 시나리오·파일 편집 실물).

**통로 확인(2026-08-30 실측)**: Claude 세션 시작 훅의 문맥 주입은 이 PC의 기억 훅이 이미 압축 복원에 쓰고 있어 동작이 확인됨 · Codex
바이너리는 둘이다 — 브릿지가 실제로 쓰는 것은 `codex-bin.txt`가 가리키는 VS Code 확장 동봉 **0.150.0-alpha.12.2**(`model_instructions_file`·
`AGENTS.override.md`·`project_doc_max_bytes` 문자열 실측), PATH의 npm CLI는 **0.118.0**. 전역 `~/.codex/AGENTS.md`는 0바이트. 브릿지는 호출마다
`-c model=…`·권한 키를 넘긴다(설정 전달 능력의 증거일 뿐 지시 파일의 안전성 증거는 아님). **통로 확정**: 파일 통로(전역 AGENTS·내장 지시
대체 키)는 쓰지 않고, 브릿지가 소유한 검증자 세션의 **첫 메시지 1회 + 브릿지 전달 레코드(세대 지문·순번) + 세대 변경·압축 감지 시 1회
재전송**으로 한다 — 압축이 지시를 지웠는지는 표식이나 검증자 자기신고가 아니라 **브릿지가 스레드 기록에서 압축 이벤트를 직접 읽어**
판정하며, ask 직전과 판정 수락 직전(postflight) 두 번 본다(이 스레드 기록엔 압축 이벤트가 103회 남아 있고 그중엔 ask 도중 압축도 있다;
브릿지는 이미 같은 기록에서 답·모델·토큰을 판독한다). 그래서 압축 100회 스레드에서도 매 턴이 아니라 "지워졌을 때만" 재전송된다.

**목표 수치(구조 완성 시)**: 구현자 매 턴 주입 2,051 → ≈200(상태 줄) + 세션·세대당 1회 · 판정 꼬리 1,863 → 열린 지적(캠페인 유계) + ≈300 ·
검증자 머리 8,690 → 상태 줄 + 권위 데이터(절대 차단 ≈650·전제·선별 예산) + 요청 결속 참고 자료 예산 ≈ **3,000 이하** + 열린 지적(캠페인
유계), 프로젝트 규모·나이와 무관.

**개선 4(a)~(e)와의 관계**: (a)=④ 권위/참고 분리, (b)=④ 뼈대 결속, (c)=④ 예산·절단(거부 없음), (d)=①+④ 판정 자리 예산, (e)=③ 하네스
문장 이관(사용자 글 불변). 빠져 있던 것은 ① "정적 지시를 세션 1회로" 와 **규모 시험** — 이 둘이 §4-B의 추가분이며 §7 순서 3에 포함한다.

## 5. 재판단 설계 v6의 범위 축소(확정)

| 부품 | 처리 |
|---|---|
| A 판정 자리에 범위표 동봉 | **1차 구현** |
| B 제외 번호 되받아침 채널·채점자 반증·2회=분쟁 보류 | **1차 구현** |
| F 재판단 안내 문안 1줄 | A·B와 함께(문구 1회) |
| C 독립 큐레이션 | **2차** — 단 v6의 형태(마감 마커·curating 단계·5단 커밋)는 폐기하고, 검증 경로 밖 독립 작업이 장부를 읽어 후보 장부에 제안(§1 "누가 넣고 빼나") |
| D 줄마다 항상/관련 속성·보관 파일 3칸 확장 | **2차(C와 함께)** — 큐레이션이 제안·사용자가 승인. §3-G(제외 칸 성장 경로 부재)의 해소 경로가 이것 |
| E 되받아침률 통계·노랑 | **보류** — 통계 탭에 캠페인당 되받아침 수 1줄만(개선 2에 편승) |
| §8 사용자 결정 4항 | **전부 기본값으로 닫음**: 구현자 제안 경로 현행 유지(2차 큐레이션 때 재론) / 반증 없는 재소환=강등 / 보관 파일 확장은 2차(D) / 큐레이션은 2차(C·형태 변경) |

## 6. 하지 않는 것(명시)
(1차에서) 큐레이션 트랙·항상/관련 속성·보관 파일 3칸 확장 — 2차로 미룸(§5) · 통계 탭 신설 · 수칙 문안 자동 대조/추천 · 새 사용자
결정 항목 · 설계 문서에 새 사용자 결정용 §8 신설(기존 §8은 기본값으로 닫은 상태 그대로 둔다) · **프롬프트에 문장을 더해 해결하는 항목**
(모든 개선은 장부·훅·검사·예산으로 만든다).

## 7. 순서와 검증
1. **개선 2(결정 장부·출력·압박 차단·EVIDENCE 처리)** — 먼저. 이후 모든 결정이 이 길로만 나간다.
2. **개선 1(A+B+F)** — 되받아침 고리(판정 자리 동봉은 개선 4-d 예산 안에서). **구현됨 2026-08-31.**
3. **개선 4 + §4-B(글이 자라지 않는 구조)** — (a)~(e)에 더해 정적 지시 세션 1회 전달(전달 레코드·압축 감지·postflight)·규모 시험. 측정
   있는 작업이라 1·2와 독립, 먼저 해도 됨. **분할(2026-08-31)**: 3-1=검증자 쪽 §4-B ①②(세션 1회 전달·전달 레코드·압축 감지·postflight·표시 3곳)
   **구현됨** / 3-2a=개선 4 (a)(b)(c)+§4-B ④(파생 총량·참고 예산·판당 행 상한·선별 초과 전량 동봉) **구현됨 2026-08-31** / 3-2b=개선 4 (e)+§4-B ③
   (하네스 문장→훅 장부·사용자 글 불변 시험)+Claude 쪽 ①(재판단 규약 꼬리·검증 지시 명령 줄만) **구현됨 2026-08-31** / 대시보드 서고 카드 초과 표기는
   4번 소형으로.
4. **개선 3(보고 양식 검사)** + P6·P7(앵커 이탈 3종)·P9·P10 소형. **분할(2026-09-01)**: 4-1=개선 3(보고 양식 경비원·Claude/Codex 구현자 양쪽·마감문 포함)+P6+P10
   **구현됨** / 4-2=P7 앵커 가드 3종+P9 경위 등재+대시보드 결정 장부 '지금 정할 것' 합류+서고 카드 초과 표기+**코덱스-코덱스 구현자 세션 1회 전달**(사용자
   결정 2026-09-01: 3-2b의 Claude 쪽과 같은 방식을 구현 Codex에도) — 다음.
5. (2차) 큐레이션 트랙 + 항상/관련(§5 C·D).

**같은 순서를 쉬운 말로(보고에는 이 문장을 쓴다 — 위 항목명을 그대로 옮기지 않는다, 사용자 지적 2026-08-30)**
1. 결정 장부 — ① 끝남(1단계): 검증자 지적은 제가 먼저 정합니다(받아들일지·되받아칠지·보관함으로 보낼지·수칙 후보로 올릴지·다시 검증받을지·
   **사용자 판단이 필요한지**). 그 끝에 "사용자가 방향을 정해야 하는 것"만 제가 장부에 올리고(질문·왜·제가 못 정하는 이유·선택지·권장을 빈칸
   없이), 사용자는 `decisions list`로 보고 명령 하나로 고르거나 "네가 정해라"로 돌려보냅니다. 하네스가 스스로 사용자에게 질문을 만드는 일은
   없습니다. **판단을 건너뛰지 못하게 하는 장치**: 검증자 지적이 전부 범위 밖이면 제 판단이 기록되기 전에는 턴이 끝나지 않고(두 종료 훅이 막음),
   "사용자 판단 필요"라는 판단은 장부 항목이 실제로 있어야만 기록됩니다(글로 "정해 주세요"라고 쓰고 지나갈 수 없음). 다섯 번 검증을 다 쓰고
   끝낼 때의 마감 보고서에서도 "정해 주세요" 칸엔 제 글이 아니라 장부에서 조립한 쉬운 말 블록(무엇을·왜·제가 못 정하는 이유·고르면 어떻게·권장·
   답하는 명령)만 들어가고, 검증자 답이 깨져 읽지 못한 경우는 "사용자가 정해 주세요"가 아니라 "답이 깨졌으니 다시 받아 검증"으로만 닫힙니다.
   기본 원칙 문안(재판단 규약)도 이 절차를 말하도록 바꿨습니다. ② 분쟁(두 번 복귀) 판단 관문은 2번에서 끝남. 남은 일 하나(대시보드 첫 화면
   "지금 정할 것"에 장부 항목 보이기)는 4번의 작은 안전장치 묶음으로 옮김.
2. 되받아치기 — 검증자 지적이 도착한 자리에 "우리가 안 다루기로 한 상황 목록(번호)"이 같이 오고, 제가 "이 지적은 n번 상황"이라고 한 줄로
   되받아칠 수 있게. 검증자가 같은 지적을 다시 올리려면 근거 한 줄이 필수이고, 근거를 달아 두 번 되살아나면 "둘이 다툰다"는 표시가 붙어
   제가 다시 정합니다(받아들일지·다시 검증받을지·범위표를 바꿔야 하는 방향 질문이면 그때만 사용자에게 올림 — 자동으로 사용자 항목이 되지
   않음). **끝남(2026-08-31)**: 상황예시 — 검증자가 "서버 두 대에 동시 배포하면 잠금이 겹친다"를 실패 사유로 올림 → 판정 밑에 "이 판의 제외
   칸: oos-1 서버 두 대 동시 배포 · oos-2 네트워크 끊김"이 같이 옴 → 제가 `finding-judge f-xxxx rebut --oos oos-1 --note "…"` 한 줄 → 그 지적은
   "구현자 되받아침" 사유로 닫히고(기록 보존), 다음 검증 요청에 "구현자가 oos-1 전제로 되받아쳤다"가 자동으로 붙음 → 검증자가 다시 올리려면
   "서버 한 대에서도 20회 중 3회 재현" 같은 근거 한 줄이 필수(없으면 보관 등급으로 내려감) → 근거를 달아 되살아나면 제가 다시 판단, 같은 지적이
   두 번째로 되살아나면 "둘이 다툰다" 표시와 함께 제 판단이 기록되기 전엔 턴이 끝나지 않음. 판정 밑 글 총량은 개선 전보다 늘지 않음(ko 1,894→
   1,692자 + 재료 ≤200).
3. 글이 안 자라는 구조 — 매번 보내던 안내문을 처음 한 번만 보내고, 검증자 기억이 압축됐거나 안내문이 바뀐 게 확인될 때만 다시 보내며(검증
   시작 전과 판정을 받아들이기 직전 두 번 확인), 붙이는 자료엔 상한을 두고, 규모를 10배로 늘려도 글이 늘지 않는지 시험이 재기.
   **앞부분 끝남(2026-08-31)**: 상황예시 — 검증자에게 매번 보내던 "채점 기본 원칙·답 쓰는 서식·경계 규칙·지적 서식·사용자가 적어 둔 검증 규칙"
   (약 2천 자 이상)을 이제 검증자 세션을 여는 첫 메시지에 한 번만 보내고, 그다음 검증부터는 맨 위에 "규약 전달 세대 xxxx · 재전송 없음" 한 줄과
   그 판의 자료(경계 항목·열린 지적·지도)만 보냅니다. 다시 전문을 보내는 경우는 네 가지뿐이고 전부 기록으로 판정합니다 — ①사용자가 규칙을
   고쳤을 때(어느 항목이 바뀌었는지 같이 표기) ②검증자 기록에 "기억 압축" 줄이 보낸 시각 뒤에 남았을 때 ③직전 전송이 확정되지 않았을 때(호출
   실패·검증 도중 압축) ④기록을 못 읽을 때(안전 쪽). 검증이 도는 도중에 압축이 잡히면 그 판정은 "권위 없음(보류)"으로 내려가고 제가 판단을 기록
   (다시 검증)하기 전엔 턴이 끝나지 않습니다. 이 상태는 검증 답 맨 아래 한 줄, 대시보드 대화 카드 제목, 첫 화면 '최근 검증 결과' 배지 세 곳에
   같은 문장으로 보입니다. **중간 부분도 끝남(2026-08-31, 3-2a)**: 상황예시 — 검증자에게 붙이는 자료 중 "참고용"(설계 경위·지도 조각·결합 확인·정찰
   신호)에만 각각 상한을 두고 순서대로 잘라 "잘렸다"고 표시하며, "권위 있는" 자료(절대 차단·전제·서고 수칙·열린 지적·사용자 규칙)는 한 글자도 자르지
   않습니다. 전체 상한은 따로 정한 숫자가 아니라 조각 상한을 더한 값이라 구성상 넘을 수 없고, 넘으면 코드 결함 신호로 경보 1건만 남기고 검증은 그대로
   진행합니다. 관련 보관 수칙이 12항을 넘어도 이제 검증이 실패하지 않고 전량 실린 채 "정상 범위 초과 — 서고 정리 권장" 한 줄이 붙습니다. 검증자가 한
   판에 지적을 40행 넘게 올리면 서식 손상으로 다시 받고, 지도 조각은 요청문의 "직접 범위" 절에 적힌 파일 기준으로만 고릅니다(문서만 고친 검증엔 지도가
   안 붙음). **뒷부분도 끝남(2026-08-31, 3-2b)**: 상황예시 — 제(Claude)가 매 턴 받던 안내문(약 2천 자: 검증 절차 규칙·원격 확인·전달 원칙·수칙 인지·
   재판단 규약)을 이제 세션 첫 턴에 한 번만 받고, 그다음 턴부터는 "시작·대기 명령 두 줄 + 진행도 + 규약 전달 상태 한 줄 + 사용자가 적은 규칙"만 받습니다
   (하네스가 쓰는 부분은 1,500자 예산 안 — 지도 상태 안내 같은 참고 신호가 넘치면 그 신호만 잘리고, 사용자가 적은 규칙은 사용자 몫이라 셈에서 뺌). 다시
   전문을 받는 경우는 세 가지뿐 — ①사용자가 기본 지침·프로필을 바꿨을 때(무엇이 바뀌었는지 표기) ②세션이 압축·재개·재시작됐을 때
   (Claude Code의 세션 시작 신호를 훅이 받아 기록을 지움) ③기록이 없을 때. 검증 답 아래 붙던 재판단 규약(1,400자)도 이 세션에서 이미 받은 문안이면
   "세대 xxxx — 이 세션 ○시 턴에 받은 것과 같음" 한 줄로 줄어듭니다. 하네스가 스스로 쓰던 "…하라" 문장을 글에서 빼려면 장치가 그 위반을 모든 경로에서
   실제로 막고 시험이 그걸 증명해야 하는데, 검증자가 "선택지를 만들지 마라"는 아직 일부 경로(마감·사용자 판단 올리기)만 막힌다고 잡아내어 그 문장은
   글에 남기고 장부는 0건으로 시작합니다(대시보드 'Claude 규칙' 카드의 "훅으로 옮긴 규칙 N개(보기)"는 행이 생기면 나타남). 사용자가 적은 글은 한 글자도
   건드리지 않습니다. 남은 것: 대시보드 서고 카드 초과 표기(4번).
4. 보고 양식 — 제 보고가 세 칸(무엇이 바뀌었나 / 상황 하나 / 다음 할 일) 제목을 갖췄는지 종료 시 검사 + 작은 안전장치들(검증자 답이 깨졌을 때
   **앞부분 끝남(2026-09-01, 4-1)**: 상황예시 — 제가 파일을 고친 턴의 마지막 답에 "무엇이 바뀌었나 / 이런 상황이 이렇게 됨 / 다음에 할 일" 세 제목이 없으면
   종료 장치가 "빠진 칸: …" 하고 돌려보내고, 검증 상한 마감 보고서에도 이 세 칸이 있어야 받아 줍니다(내용은 검사하지 않음 — 사용자가 적은 규칙은 그대로).
   코덱스-코덱스 모드의 구현 Codex도 같은 장치를 탑니다. 검증자 답의 지적 블록이 깨졌으면 다음 검증 요청에 "3번째 줄: JSON 형식 아님" 같은 좌표·사유가
   자동으로 붙고(원문은 옮기지 않음), 마감 보고서에 회차 숫자를 적으면 장부 실제값과 다를 때 받아 주지 않습니다. 남은 뒷부분(4-2):
   다시 요청·폴더가 바뀌면 검증 시작 안 함·"몇 회차" 손셈 방지(장부에 오른 검증의 화면 회차는 장부 숫자만 — 끝남; 장부에 못 오른 검증은
   권위 없는 진행 표시)·"왜 이렇게 됐나" 물으면 찾아볼 수 있게 이번 문서들의 결정 이력 등재·대시보드 첫 화면 "지금 정할 것"에 결정 장부
   항목 보이기(1번에서 넘어온 꼬리)).
5. (나중) 수칙 정리 담당 — 독립 작업이 장부를 보고 "이 수칙은 남기고, 이건 빼고, 이건 추가, 이건 항상 붙이고 저건 관련 작업에만" 후보를
   올리면 사용자는 승인·빼기만.

각각 독립 캠페인·독립 검증(사용자 승인 2026-08-30 — 1번부터 착수). 성공 지표: 캠페인당 되받아침 수(0→1 이상), 산문 결정 항목 0, 캠페인당 방향 질문 수(많을수록 제조된 결정 신호 — "네가
정해라" 응답 수 병기), 보고 양식 준수, 상한 소진 턴 감소.

## 7-1. 사용자 지적 5항에 대한 이 설계의 답(회귀 점검표)

| 지적 | 이 설계가 어떻게 답하나 | 프롬프트 증가 |
|---|---|---|
| 1. "상황예시" 규칙이 반복 안 지켜짐·훅이 안 걸린 것 아닌가 | 규칙은 매 턴 주입되지만 지키는지 보는 장치가 없었다(P2). 답=개선 3: 종료 훅이 보고의 세 칸 제목을 검사(마감문 검사기의 제목 위치 추출부 재사용). 프롬프트 문장 추가 없음 | 0 |
| 2. "사용자 결정 필요"를 스크립트로 출력하라고 했는데 안 됨 | 답=개선 2: 구현자가 판단 끝에 올리는 결정만 장부 행으로 생기고(`decisions raise` 구조 채널·산문 금지), `decisions list`와 개요가 양식으로 출력 | 0 (마감문 지시는 오히려 줄어듦) |
| 3. 개선안이 프롬프트 과대화에 기댄 수정 아닌가 | 개선 1·2·3·4 전부 장부·훅·검사·예산이며 지시문 추가 항목은 0. 유일하게 글이 늘 수 있는 개선 1-A는 4-d 예산(총량 고정)으로 상쇄 | 0(합계 감소) |
| 4. 수칙에 치우쳐 사용자 판단을 늘리는 흐름 | 결정 출처=구현자 판단 하나(기계·검증자가 사용자 질문을 만들지 않음)·§8 기본값으로 닫음·제안 행/승격·기계 출처 철회. 큐레이션도 승인·빼기 표면만 쓴다 | 0 |
| 5. 프롬프트가 8천~1만자 | 실측 8,690(머리)·최대 11,328·경고선 12,000(차단 없음) — 위험으로 가는 중(P11). 답=§4-B 원칙(정적 지시는 세션 1회·강제 규칙은 훅으로·데이터는 뼈대 결속+예산·서식은 파서) + 개선 4 1차분, 규모 시험으로 재발 차단. 목표: 규모·나이 무관 3,000 이하 | 감소·성장 0 |
| (사용자 추가) 일시적 다이어트가 아니라 하네스 완성 조건 | §4-B를 불변식으로 승격("주입 글 = 상수 + 예산 데이터")하고 규모 시험이 매 릴리스 잰다 | 성장 0 |

## 8. 오늘 세션이 남긴 교훈(한 줄씩)
- 지적을 전부 수용하는 습관은 코드 왕복에서도 설계 왕복에서도 같은 비대를 만든다 — 되받아칠 수 있어야 멈춘다.
- "훅에 걸려 있다"와 "지켜진다"는 다르다 — 주입만 있고 검사가 없으면 프롬프트 의존이다.
- 결정은 사람이 정할 수밖에 없는 것만, 그것도 양식으로 — 산문으로 만든 결정은 압박이 된다.
- 검증자 출력이 깨지면 하네스는 조용히 "지적 0"이 된다 — 깨짐 자체를 신호로 남겨야 한다.
- 이 문서의 1회차 검증(blocker 7·보완 3)에서 9건 수용(blocker 6·보완 3)·1건 반박(f-32d8d877: "기본값 없음"의 진위 기계 판정 요구 —
  뜻을 읽어야 아는 문제라 원리상 불가). 2회차에서 그 반박은 기각됐다 — 검증자의 대안(진위를 읽지 말고 구현자의 결정 **생성 권한**을 없애라)이
  더 단순하고 사용자 의도에 맞아 수용했다. 수용분은 사실 정정·장부 계약·권한 제한으로, 새 조각은 늘리지 않았다. 하네스는 2회차에서
  "보강 요구 수용 3건째 — 글로 정하는 단계가 수확 체감" 신호를 냈다.
- 그 2회차 수용(제안 행·사용자 승격)은 사용자 재점검에서 **철회**됐다 — 검증자 지적에 답을 맞추다 사용자 역할 전제(철학·방향만, 개입은
  승인·빼기)를 놓쳤다. 검증자 안이 "더 단순"해 보여도 사용자 앞에 놓이는 항목을 늘리면 그 자체가 P4·P5의 재현이다.
- 같은 실수를 한 번 더 했다(2026-08-30): "기계 상태가 결정을 만든다"로 옮긴 것도 구현자 판단을 뺀 설계였다. 사용자 재지적으로 되돌렸다 —
  결정의 유일한 출처는 구현자 판단이고, 검증자의 "자기신고 진위 불가" 지적은 앞으로 설계 의도로 반박한다. 구현까지 갔다가 되돌린 비용이
  들었다(코드·시험 재작성).
- "몇 회차"는 장부가 세는 것이다 — 산문·요청문 제목·기억 파일에 제가 적은 번호는 권위가 없고, 왕복 줄이 사라졌다면 그것이 신호다(P7·P10).
- 예산은 "내가 쓰는 글"에만 걸고 "붙이는 자료"에는 안 걸면 봉투는 자료로 터진다 — 총량으로 재야 한다(P11).
- 글은 다섯 성격뿐이다(정적 지시·동적 상태·강제 규칙·판단 재료·서식). 성격마다 글 아닌 자리가 있고, 그 자리에 놓으면 프로젝트가
  커져도 글은 안 자란다. "지키라"는 문장이 지시문에 남아 있으면 아직 구조가 아니라는 뜻이다(§4-B).
- 하네스가 자기 자료 때문에 멈추는 설계는 없다 — 상한은 마법 숫자가 아니라 조각 상한의 합이고, 사용자가 쓴 글은 어떤 다이어트에서도 건드리지
  않는다. 재전송·판정 기준은 답안지 표지처럼 눈에 보여야 한다(상태 줄 1줄).
- 사용자가 "그쪽이 낫다"고 한 방향(큐레이션)을 "조각이 많다"는 이유로 보류하는 것은 방향 변경이다 — 형태를 바꿔서라도 방향은 유지한다.
- 2026-08-30 전체 재정독 정리 내역: 머리말 상태 갱신 / §3-G·§3-F 옛 참조 → P12·P6 / P8·P9 순서 정렬·P12 신설 / §4 제목("조각 3개"→개선 4개) /
  개선 1-A "범위표 전문"과 4-d "제외 번호+한 줄" 상충 → 후자로 통일 / 4-d 예산의 §4-B 이후 값 명시 / 4-e에 §4-B ③ 조건 결속 / §4-B ①에 Claude 쪽
  세대 변경 규칙 추가 / §5 §8 행의 "큐레이션 보류"→2차 / §7의 "통로 파일럿" 문구 제거.

## 부록 — 구현 참고(파일·재현)
- 처분 집계: `~/.codex-bridge/verify-findings/*.jsonl`의 `disposition.choice`(619/64/22/1 — 기준 영수증 `01787981663981-0000015664-000000-cnnh.json`
  =2026-08-29T05:34:23.981Z 시점; 이후 720/…/1068) · `finding.supported===false` 0 · `demoted` 22(범위 밖 0).
- 세션 검증 작업: `~/.codex-bridge/ask-jobs/*.json` 중 `schema:"ask-job-v1"`·createdAt≥2026-08-28T05:00Z(14:00 KST) 31건(`*.checkpoint.json`
  14건 제외) · 경계 통일 캠페인 `cl:…:2026-08-28T21:33:35.614Z` 4건 profile=integrity·verifyBudget=0.
- 구현자 주입=`bridge/contract-lib.js implementerEnvelopeInject`(금지만) · 판정 렌더=`contract-lib.js formatForClaude`(범위표 없음) ·
  되받아침 명령=`bridge/codex-bridge.js finding-judge`(자유 메모 → 2026-08-31 `rebut --oos oos-n` 결속) · 범위 밖 강등 규칙=`contract-lib.js
  judgeAdmission` 규칙 2(+2026-08-31 규칙 2b 되받아침 복귀) · 전량 강등=보류 재산출=`codex-bridge.js machineFindingsLayer`.
- **개선 1 구현(2026-08-31)**: `contract-lib.js` FINDING_ORIGINS+`boundary-contest`·parseFindingsBlock `contest` 보존·`writeEnvelopeFreeze`/
  `readFrozenEnvelopeRec` oos 동결·`judgeAdmission(…, rebutted)` 규칙 2b·VERDICT_ACTION_CORE fail 축약(ko/en)·재판단 규약 절차문 축약(명령 인자
  나열→"명령이 안내한다", 기본값 해시 핀 갱신) · `codex-bridge.js` ask-start fx9.oos·`implementerRebuttalsFor`(close implementer-oos+계보 복귀 수)·
  `v2DirectiveFor` [구현자 되받아침] 목록·machineFindingsLayer 되받아침 재료 줄(≤200)·contest-* 영수증·분쟁→`addJudgeRequired(reason "dispute:<id>")`·
  `cmdFindingJudge --oos`(동결 번호 검증·disposition oosId·close 행) · 꼬리 실측: ko 1,894→1,692 · en 3,520→3,312(개선 전 HEAD 0e7b419 대비, `tests/rebuttal.test.js` [8]이 잰다).
- Claude 규칙 주입=`contract-lib.js`(고정 규약 블록)·`contract-inject.js`/`codex-hook.js` UserPromptSubmit; 출력 검사 없음(`verify-guard.js`·
  `verify-cap-handoff.js`).
- 결정 관련 현행(2026-08-30)=`verify-cap-handoff.js`([사용자 판단 필요] 절=결정 장부 정본 렌더 블록 대조·EVIDENCE-UNAVAILABLE은 [잔여 위험 판단]
  재검증 판단으로 강제)·`src/extension.ts` decideActs(개요 합산 — 결정 장부 합류는 남은 단계).
  **개선 2 구현 1단계(2026-08-30, 사용자 방향 재확정 반영)**: `contract-lib.js` 결정 장부(`decisions/<wsKey>.jsonl`·`decision-v1`·origin=`implementer`
  하나·kind 닫힌 열거(boundary|product|risk|external)·noDefault 10자 이상 필수·id=sha1("decision:"+wsKey|campaign|origin|ask|targetFp+"|"+sha1(question)[:16])[:16]·
  추가 전용/생성·결과 행 분리/latest fold/read-back/wsKey 결속·`resolveDecision`은 targetFp가 있으면 현재 지문 필수(fail-closed)) · `codex-bridge.js`
  `armScopeDemotedJudge`(전량 강등 보류=판단 관문 마커 장전·자동 생산자 없음) · 판단 관문=`contract-lib.js` `judge-required` 마커+`round-judge`
  CLI(close-oos|re-verify|escalate — escalate는 결정 장부 실존 id 필수)·`verify-guard.js`/`codex-hook.js` 종료 차단(검증 불필요 턴 포함·재시도
  카운터로 유계) · `finding-judge escalate --decision <id>` · CLI `decisions raise|list|choose|delegate|metrics` · `tests/decisions.test.js` 13단언·
  `tests/round-judge.test.js` 11단언(종료 훅 실행 반례·카운터 리셋·복구 멱등 포함) · 마감문 [사용자 판단 필요] 절=`renderDecisionBlock` 정본
  대조(`verify-cap-handoff.js decisionBlocksOk`·`decisions render`)·EVIDENCE-UNAVAILABLE=잔여 위험 절 재검증 판단·재판단 규약 4곳 문안 교체
  (`tests/verify-sequential.test.js`·`tests/p12-profile.test.js` 핀 갱신). 남은 단계: 개요 "지금 정할 것" 합류·분쟁(두 번 복귀) 마커.
- 검증자 블록 손상 처리=`codex-bridge.js machineFindingsLayer`('보류' 강등·지적 미등록) · 같은 함수 첫 줄이 무결성 프로필(`profileSnap!=="core"`)
  을 기계 판독 없이 반환 · 손상 좌표 계약=`contract-lib.js parseFindingsBlock` corrupt `{lineNo, reasonKey}`(원문 비복사).
- 앵커 이탈(P7) 재현: 세션 전사 2026-08-28T21:33:31Z Bash `node -e "require('./bridge/contract-inject.js')…"`(로드 시험) → `bridge/contract-inject.js:21`
  `ws = CLAUDE_PROJECT_DIR || hook.cwd || process.cwd()` → `~/.codex-bridge/active/<세션id>.json` workspace=codex-peek·앵커 21:33:35 →
  `contract-lib.js configWs()` 2순위(세션별 active) → ask-jobs 4건 workspace=`D:\codex-peek`·verifyProfile=integrity·verifyBudget=0·캠페인
  키 `5e0f4a38ea367c2e`(=wsKeyFor(D:/codex-peek)) · 그 폴더 계약(`~/.codex-bridge/contracts/5e0f4a38ea367c2e.json`)에 verifyProfile/verifyBudget
  없음 → `normVerifyProfile` 기본 "integrity"·`normVerifyBudget` 0=무제한 · "엉뚱 폴더 방어"(`codex-bridge.js` cmdAsk here/active 비교)는
  둘 다 codex-peek라 무동작 · 23:07 사용자 프롬프트 훅이 앵커를 원 폴더로 복귀.
- 글 길이 실측(P11): 검증자 스레드 `~/.codex/sessions/2026/08/15/rollout-…01a0023c….jsonl`에서 마지막 ask 사용자 메시지 9,516자(절별 길이는 §2 표) ·
  8월 중앙값은 같은 파일의 `response_item`(실제 요청)만 세고 `compacted`(압축 사본) 줄은 제외해야 한다(첫 집계는 이를 섞어 오류) · 구현자 쪽은 `contract-lib.js` buildInjection/buildVerifyDirective/
  implementerEnvelopeInject 산출 길이 합(2,051) · 판정 꼬리는 ask-wait 출력의 `---` 이후(1,863) · 상한 정의=`codex-bridge.js` CONTRACT_INJ_MAX 4000·
  HEAD_SOFT_LIMIT 12000(경고) · 고정 산문 예산=`tests/verifier-head.test.js` CAPS · 지도 관문=`bridge/map-reader.js renderV2Slice`(reqText 씨앗).
- **§7 3-1 구현(2026-08-31 · §4-B ①② 검증자 쪽)**: `contract-lib.js` DIRECTIVE_DELIVERY_DIR·directiveGenOf(성분 6·sha1 16자)·read/writeDirectiveDelivery·
  rolloutCompactedAfter(끝 16MiB·`type:"compacted"` timestamp>sentAt·시각 없음=참·판독 실패=unreadable)·readDirectiveDeliveryState(absent|ok|corrupt|unreadable)·
  deliveryPlanFor(first|no-record|record-unreadable|pending|gen-changed|compacted|rollout-unreadable|delivered)·deliveryStatusLine·machineReasonText `compacted-mid-ask`/
  `postflight-unreadable`·resolveJudgeRequired(보류 마커는 close-oos 거부 `choice-not-allowed`) · `codex-bridge.js` v2DirectiveFor=v2StaticDirective+v2DynamicData
  (바이트 무회귀)·envelopeSliceFor `envData/v2Static/v2Data`·withContract carrier.delivery→계획·slim/full 머리(상태 줄 첫 줄)·recordDeliveryBeforeCall(호출 직전
  pending)·postflightDelivery(확정/미확정)·postflightHeld/applyPostflightHold(보류 강등+판단 관문)·finishVerifyRun(보류=proof·체크포인트 미기록+HOLD_EXIT_CODE 4 비0
  종결)·resume/new 분기 배선·finishVerifyRun 상태 줄·flagVerdict 행 `delivery` ·
  `src/extension.ts` 카드 제목 병기+개요 배지(같은 상태 줄) · `tests/directive-delivery.test.js` 10단언.
- **§7 4-1 구현(2026-09-01 · 개선 3+P6+P10)**: `verify-cap-handoff.js` REPORT_SECTIONS·reportShapeCheck·reportShapeInstruction·lastAssistantText·lastCodexAssistantText·
  roundFigureMismatch(머리 포함·인접 키워드) 회차 대조·capHandoffInstruction 회차/세 칸 안내 · `verify-guard.js`/`codex-hook.js` reportOk 종료 조건(검증 모드 off 포함)·
  마감 수락 조건(마지막 답)·차단 사다리·epoch ·
  `contract-lib.js` REISSUE_REASON_LABEL·reissueFileFor/writeReissue/readReissue/clearReissue/reissueSectionFor · `codex-bridge.js` machineFindingsLayer 기록/소거·
  v2DynamicData 동봉 · `tests/report-shape.test.js` 6단언(종료 훅 실행 반례·off 모드·P10 반례 꼴 포함)·verify-sequential 마감 픽스처 R3 앞붙임+마지막 답 반례 2종.
- **§7 3-2b 구현(2026-08-31 · 개선 4 (e)(d)+§4-B ①③ Claude 쪽)**: `contract-lib.js` verifyDirectivePieces/buildVerifyDirective(=slim+static)/
  buildVerifyDirectiveSlim/buildVerifyDirectiveStatic·DIRECTIVE_MOVED·CLAUDE_STATIC_KEYS·claudeStaticParts/claudeStaticGen/claudeStaticBlock/
  claudeDeliveryPlan/claudeDeliveryStatusLine·implementerEnvelopeInjectParts(ab 정적/dyn 동적 — implementerEnvelopeInject는 바이트 동일 join) ·
  `contract-inject.js` 앵커 directive 보존→계획→정적 블록/slim/상태 줄→앵커 기록 확정(writeAnchors) · `session-start.js`(신규 SessionStart 훅:
  앵커 directive=null+directiveReset{source,ts}) · `codex-bridge.js` rejudgeTailFor(판정 꼬리 포인터) · `install.js`/`src/hook-setup.ts` OUR_HOOKS·
  BRIDGE_SCRIPTS·OUR_SCRIPT_NAMES 6종 · `src/extension.ts` movedRules 상태+접힘 · `tests/claude-directive-delivery.test.js` 9단언·hook-setup/p12 핀 갱신.
- **§7 3-2a 구현(2026-08-31 · 개선 4 (a)(b)(c)+§4-B ④)**: `contract-lib.js` askSectionBody·selectorUnion(over 표시·실패 없음)·FINDINGS_ROWS_MAX 40(too-many-rows)·
  ENVELOPE_OOS_TITLE_MAX 60(제외 칸 참조)·REF_BUDGET/REF_ORDER/HEAD_BUDGET/headBudgetTotal/applyReferenceBudgets · `map-reader.js` seedText=[직접 범위] 절·
  반환 `parts{provenance,map,coupling,scout}`(text 무회귀) · `codex-bridge.js` CONTRACT_INJ_MAX=HEAD_BUDGET.contract·HEAD_SOFT_LIMIT=headBudgetTotal()·
  envelopeSliceFor selOver(정보 행)·withContract 참고 예산 적용(carrier.attachParts·refClipped)·측정=열린 지적 제외+무결성 경보 head-budget·판정 하단 [서고
  수칙] 정보 행 · `ask-job-worker.js` 초과=실패 아님 · `tests/head-budget.test.js` 7단언·envelope-selector/withcontract 핀 갱신.
- 재판단 설계 상세=`docs/REJUDGE-AUTHORITY-DESIGN.md` v6 §3-A·B(1차 범위) · 선행 정본=`docs/VERIFY-GOVERNANCE.md` §1·§3.3·§7.
