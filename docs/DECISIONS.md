# 설계 결정 색인 (Design-Provenance Index)

> 결정 1건 = 항목 1개(원자 단위). 형식·규칙은 docs/MAP-PROVENANCE-DESIGN.md §1.
> 문체: 이 문서는 사람이 읽고 식별·판단하는 문서다 — '결정'은 겪은 상황부터
> ("무슨 일이 있었고 → 그래서 무엇을 정했다") 기술용어 없이 상황예시로 쓴다.
> 코드 식별자·정확한 지점은 '정본' 줄에만 둔다(자동 실효 감지용). 문체는 기계가
> 검사하지 않는다 — 파서는 형식만 본다(문체 검사 규칙 추가 금지: 경직성 유발).
> '찾는말'에는 규약 어휘(지적 목록 마커 등)를 쓰지 않는다 — 파서가 항목을 거부한다.
> 항목 확정은 사람 몫(자동 승격 없음). 후보 나열: `node scripts/provenance-scan.js`.

## D-2026-07-24-intent-auto — MAP 대기 변경은 승인 카드 대신 자동화
- 날짜: 2026-07-24 · 종류: 개정(ⓒ 확정) · 상태: 유효
- 결정: 지도가 새 발견을 올릴 때마다 '이걸 반영할까요?' 승인 카드가 쌓여 사람이 일일이
  눌러야 했다 — 사용자가 "후보마다 묻지 말고 자동화하라"고 지시해, 정책이 걸리지 않은
  일반 제안은 검증 통과·반박·결함 확인 결과로 자동 반영하고 그 승인 카드는 폐기했다
  (되살리지 않음). 사람에게 묻는 것은 '정책끼리 충돌할 때' 한 종류만 남는다.
- 왜: 사용자 지시(2026-07-24) "MAP은 후보마다 승인 묻지 말고 검증 통과·반박·결함 확인으로
  자동화하라".
- 결과: '재사용 정책' 숫자가 0으로 오래 머무는 저장소는 고장이 아니라 이 자동화의 잔여
  표시(최초 정책을 만들 카드 자체가 없음). 대시보드는 2026-08-17부터 결정 장부의 자동화
  실적을 대신 표시.
- 정본: docs/MAP-V2-DESIGN.md `P9 v12 소규모 개정`~`사용자 지시 부기` 절.
- 찾는말: 정책 0, 자동 승인, 카드 없음, 왜 정책이 안 생기나, 재사용 정책, intent policy, 자동화 결정

## D-2026-08-09-verdict-colors — 판정·상태 의미색은 테마 무관 고정
- 날짜: 2026-08-09(칩)·2026-08-17(통계 확장) · 종류: 확정 · 상태: 유효
- 결정: 대시보드 색상 테마를 파스텔 등으로 바꾸면 '통과 초록·실패 빨강' 같은 판정 색까지
  테마 색조에 물들어 무엇이 실패했는지 한눈에 안 보이게 됐다 — 그래서 판정·상태를 뜻하는
  색 4종(통과 초록·보완 노랑·보류 주황·실패 빨강)은 어떤 테마를 골라도 절대 물들지 않는
  고정 색으로 유지한다.
- 왜: 테마 적용이 의미색을 눌러 대화 판정 칩(08-09)·검증 통계 그래프(08-17)에서 구분이
  사라진 사용자 실보고 2건.
- 결과: 판정 칩은 배경까지 칠하는 고정 색 쌍, 통계 4색은 테마 재정의가 닿지 않게 원색을
  미리 떠서 사용.
- 정본: src/extension.ts — 판정 칩 고정 색 선언부(".app.themed .vchip.pass" 등 4종)와
  통계 4색 원색 캡처("--chPass" 선언·차트 사용부). 보조 맥락: 기억 노트 2026-08-09·08-17.
- 찾는말: 판정 색, 테마 색 구분 안 됨, 통계 회색, 의미색, 완전통과 색, 색이 물듦

## D-2026-08-12-enrich-selfheal — 자동 보강 보류는 스스로 회복해야 한다
- 날짜: 2026-08-12 · 종류: 개정(8왕복+도장) · 상태: 유효
- 결정: 확장을 업데이트하거나 새로 설치할 때마다 '자동 보강: 보류됨' 경고가 다시 떠서
  사람이 '다시 점검'을 눌러야만 풀리곤 했다 — 보류는 사람 클릭이 아니라, 멈추게 했던
  조건이 회복되는 순간 시스템이 스스로 재개해야 한다.
- 왜: 업데이트·설치마다 같은 보류+수동 해제가 반복된 사용자 실보고. 진짜로 사람 결정이
  필요한 보류만 2026-08-16부터 개요 '지금 정할 것'에 상시 표시.
- 결과: 설정이 실제로 바뀌었는지 내용 지문으로 자동 재점검하고, 준비가 회복되면 보류를
  자동 재개(전역 장부가 단일 권위·지문별 증명 이력 보존).
- 정본: bridge/map-enrich.js — 준비 회복 자동 재개 지점('route: "ready-heal"' 기록)과
  내용 지문 자동 재점검 게이트. 상시 표출 판정: src/extension.ts enrichHumanActionState.
  보조 맥락: 기억 노트 08-12·08-16.
- 찾는말: 보강 보류, 자동 재개, 다시 점검, 사람 조치, 왜 멈춰 있나, 업데이트 후 경고

## D-2026-07-22-open-search-bounded-blocker — 검증자는 어디든 살펴보되, "실패" 도장은 사용자가 정한 범위 안에서만
- 날짜: 2026-07-22 · 종류: 확정 · 상태: 유효
- 결정: 채점자(검증자)가 답안 밖 문제까지 트집 잡아 아홉 번씩 다시 고치게 만드는 일이 있었다 — 그래서 검증자의
  탐색 범위는 막지 않되, "실패"로 만들 수 있는 권한만 프로젝트가 방어하기로 선언한 세계(전제·금지·제외)와 이번
  작업이 선언한 구역 안으로 묶는다. 범위 밖 발견은 버리지 않고 메모(주의·보관함·범위 확장 요청)로 흐른다.
- 왜: 사용자 실측 2026-07-22 — 희귀 경합까지 무조건 실패로 몰아 왕복이 폭주(정직 명문: 폭주를 없애는 게 아니라 줄이는 구조).
- 결과: 수칙서(검증 경계) 세 칸과 입장 심사(기계 강등)가 생겼고, 범위 밖 지적은 실패 사유가 아니라 표시로 남는다.
- 정본: docs/VERIFY-GOVERNANCE.md `## 1. 원칙` · `## 2. Verification Envelope` · `### 3.2 입장 심사`.
- 찾는말: 열린 탐색, 차단 권한, 왜 범위 밖은 실패가 아닌가, 검증 경계, 수칙서 세 칸, 입장 심사, 강등, 왕복 폭주

## D-2026-07-24-cap-rulebook-candidates — 검증 왕복이 바닥나면 화면이 수칙서 후보를 직접 내민다
- 날짜: 2026-07-24 · 종류: 확정(설계) · 상태: 유효
- 결정: 사용자가 정한 왕복 상한(프로젝트마다 다르고 무제한도 가능)을 다 쓰고 턴이 강제로 끝날 때, 사용자가 원인을 손수 파헤쳐 수칙을 적어야 했다 — 그래서 소진 보고
  화면이 "이 상황을 앞으로 방어할지/치워둘지" 후보를 상황예시로 직접 제시하고, 후보가 없으면 없다고 적는다(침묵 금지).
- 왜: 사용자 지시 2026-07-24 — 사용자는 방향을 정하는 존재이지 원인 조사원이 아니다.
- 결과: 후보 원천 4종(반복 강등·기계 승격·반복 실패 계보·보관함 이관)과 후보마다 4요소 설명 계약.
- 정본: docs/VERIFY-GOVERNANCE.md `## 7. 상한 소진 시 수칙서 후보 제시`.
- 찾는말: 상한 소진, 수칙서 후보, 왜 후보를 제시하나, 왕복 상한, 소진 화면, 후보 원천, 침묵 생략 금지

## D-2026-07-24-plain-language-choices — 사용자에게 고르라고 할 때는 기술용어 없이 "고르면/안 고르면"까지
- 날짜: 2026-07-24 · 종류: 확정(전역 계약) · 상태: 유효
- 결정: 구조 지도 결정권 질문이 내부 분류명으로 제시돼 사용자가 뜻을 모른 채 골랐다 — 그래서 하네스·구현자가 사용자에게
  선택이나 판단을 요구하는 모든 출력은 ①기술용어 금지 ②상황예시 ③각 선택지에 "고르면 무엇이 달라지나/안 고르면 무엇이
  유지되나" ④권장+근거 한 줄을 갖춘다.
- 왜: 2026-07-24 첫 실사고(같은 날 쉬운 말로 다시 물어 재확인).
- 결과: 선택지 도구·마감 보고·수칙서 후보·대시보드 카드 문구가 이 계약을 따른다.
- 정본: docs/VERIFY-GOVERNANCE.md `## 8. 사용자 대면 선택 출력의 표현 계약`.
- 찾는말: 기술용어 금지, 상황예시, 고르면 안 고르면, 권장 근거, 사용자 선택 출력, 왜 쉬운 말로 묻나, 표현 계약

## D-2026-08-29-rulebook-is-boundary — 수칙서는 지킬 규칙집이 아니라 시험 범위표이자 되받아칠 근거다
- 날짜: 2026-08-29 · 종류: 확정(정의) · 상태: 유효
- 결정: 하네스는 채점자에게 범위표를 건네는 앞쪽만 있었고, 답안 쓴 사람이 그 범위표로 "이건 범위 밖"이라고 되받아치는
  뒤쪽은 706번 중 1번만 작동했다 — 그래서 수칙서를 "채점자가 미리 알고 채점하는 시험 범위표 + 구현자가 범위 밖 지적을
  되받아칠 때 드는 근거"로 정의하고, 개선은 뒤쪽 고리를 잇는 최소 구조로 한다.
- 왜: 장부 전수 실측 2026-08-29(되받아침 1/706·검증자 범위 밖 표시 0/1057).
- 결과: 되받아침 재료가 판정 도착 자리에 오고, 되받아침은 제외 칸 번호 하나로 성립한다.
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `## 0. 한 줄 결론` · `## 1. 수칙서란 무엇인가` · docs/REJUDGE-AUTHORITY-DESIGN.md `## 헌법`.
- 찾는말: 수칙서 정의, 시험 범위표, 되받아침, 왜 되받아치지 못했나, 범위 밖 근거, 뒤쪽 고리, 헌법

## D-2026-08-29-rebuttal-oos-only — 되받아침은 제외 칸 번호 하나로 성립하고, 되살리려면 검증자가 반증을 내야 한다
- 날짜: 2026-08-29 · 종류: 확정 · 상태: 유효
- 결정: 구현자가 "이 지적은 우리가 안 다루기로 한 n번 상황"이라고 한 줄로 되받아치면 그 지적은 강등된다. 검증자가 같은
  지적을 다시 올리려면 "우리 세계 안에서도 이 문제가 난다"는 구체 경로(반증)를 내야 하고, 반증 없이 되살아난 지적은
  자동으로 다시 강등된다. 되받아침만으로는 "통과"가 생기지 않는다(강등이지 삭제가 아님).
- 왜: 반박을 수용만큼 싸게 만들면서도 통과 위조 통로는 열지 않기 위해(원칙 P2·P3·P6).
- 결과: 두 번 되살아나면 "둘이 다툰다" 표시가 붙고 구현자 판단이 기록되기 전에는 턴을 끝내지 못한다.
- 정본: docs/REJUDGE-AUTHORITY-DESIGN.md `## §2 방향` · `### B. '범위 밖' 인용 반박 채널` · docs/HARNESS-REALIGNMENT-2026-08-29.md `**개선 1. 되받아침 고리 잇기`.
- 찾는말: 되받아침, 제외 칸 번호, 반증, 재소환, 강등, 왜 반박만으로 통과가 안 되나, 분쟁 표시, 판단 관문

## D-2026-08-29-curation-independent — 수칙을 넣고 빼자고 제안하는 쪽은 답안 쓴 사람이 아니라 독립 실행이다
- 날짜: 2026-08-29 · 종류: 확정(2차 범위) · 상태: 유효
- 결정: 자기 반박 근거가 될 경계를 답안 쓴 사람이 제안하는 것은 이해충돌이다 — 그래서 유지/추가/삭제와 "항상 붙일지·
  관련 작업에만 붙일지"는 검증 경로 밖에서 도는 독립 실행이 장부(무엇이 반복 강등됐나·무엇이 되받아침에 쓰였나)를 보고
  제안하고, 사용자는 승인·빼기만 한다. 수칙 한 줄마다 항상/관련은 사용자 속성이며 새 수칙 기본값은 "관련될 때".
- 왜: 사용자 지적 2026-08-29(19줄 첫 장이 "임의로 고정된 헌법"이었음).
- 결과: 구현자 제안 채널은 잔존하되 "구현 담당 제안" 표기, 큐레이션 자체는 보류(§8 기본값)이며 나중 증분.
- 정본: docs/REJUDGE-AUTHORITY-DESIGN.md `### D. 항상/관련 = 사용자 속성` · `## §8 결정 항목` · docs/HARNESS-REALIGNMENT-2026-08-29.md `## 1. 수칙서란 무엇인가` 누가 넣고 빼나.
- 찾는말: 큐레이션, 독립 실행, 이해충돌, 항상 관련, 사용자 속성, 왜 구현자가 수칙을 제안하면 안 되나, 승인 빼기

## D-2026-08-29-defaults-close-decisions — 기본값이 있으면 구현자가 정하고 보고만 한다, 사용자에게 묻는 항목은 늘리지 않는다
- 날짜: 2026-08-29 · 종류: 확정(운영 규칙) · 상태: 유효
- 결정: 설계마다 "사용자 결정 항목" 목록이 자라 사용자가 매번 4~5개를 골라야 했다 — 그래서 합리적 기본값이 있는 항목은
  구현자가 기본값으로 닫고 보고만 하며, 재진입 조건(실측)이 생기기 전에는 다시 묻지 않는다. 사용자에게 올리는 것은
  범위표·제품 방향·위험 감수·외부 결정처럼 정말 사용자 몫인 것뿐이다.
- 왜: 재판단 설계의 결정 4항이 전부 기본값으로 닫혔음(2026-08-29 범위 축소).
- 결과: 결정 장부의 "구현자가 못 정하는 이유" 칸이 필수가 됐고, 산문 "정해 주세요"는 관문이 받지 않는다.
- 정본: docs/REJUDGE-AUTHORITY-DESIGN.md `## §8 결정 항목 — 기본값으로 닫음` · docs/HARNESS-REALIGNMENT-2026-08-29.md `**개선 2. 결정 장부`.
- 찾는말: 기본값, 사용자 결정 항목, 재진입 조건, 왜 안 묻나, 못 정하는 이유, 결정 최소화

## D-2026-08-30-decision-ledger — 사용자가 정할 것은 장부 항목으로만 존재한다(글로 "정해 주세요"는 무효)
- 날짜: 2026-08-30 · 종류: 확정 · 상태: 유효
- 결정: 상한 마감 보고서의 "정해 주세요" 칸에 제가 산문으로 질문을 적고 지나가는 일이 반복됐고 사용자는 무엇을 답해야
  하는지 알 수 없었다 — 그래서 결정은 생기는 순간 장부 항목(무엇을·왜·못 정하는 이유·고르면 어떻게·권장·답하는 명령)으로
  남기고, 화면과 명령줄은 그 장부를 양식대로 출력만 한다. 장부 항목이 없으면 "사용자 판단 필요"는 기록되지 않는다.
- 왜: 사용자 실보고 2026-08-29~30(결정 칸이 제 글이었고 답 받을 통로가 없었음).
- 결과: 마감 보고서 결정 칸=장부 렌더 블록만, 대시보드 첫 화면 "지금 정할 것"에 장부 항목 합류(2026-09-02), 답은 명령줄에서
  "고르기" 또는 "네가 정해라"로 남긴다(화면은 읽기 전용).
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `**개선 2. 결정 장부` · bridge/contract-lib.js `function openDecision` · `function renderDecisionBlock`.
- 찾는말: 결정 장부, 정해 주세요, 사용자 판단 필요, 왜 산문 질문이 안 되나, 지금 정할 것, 고르기, 네가 정해라

## D-2026-08-30-report-three-sections — 파일을 바꾼 턴의 마지막 답은 세 칸(무엇이 바뀌었나/이런 상황이 이렇게 됨/다음에 할 일)
- 날짜: 2026-08-30(설계)·2026-09-01(구현)·2026-09-03(폐지) · 종류: 확정→폐지 · 상태: 폐지(사용자 규칙 칸으로 이전 — 하네스는 세 칸을 강제하지 않음, 아래 D-2026-09-03-rule-self-check 참조)
- 결정: 보고에 설계 문서의 항목명과 코드 이름을 그대로 옮겨 사용자가 "다시 설명해 달라"는 턴이 반복됐다 — 그래서 파일을
  바꾼 턴과 상한 마감 턴의 마지막 답은 세 칸 제목을 갖춰야 종료되며, 종료 장치가 빠진 칸을 돌려보낸다(내용은 검사하지
  않음·사용자가 적은 규칙은 그대로).
- 왜: 사용자 선호 2026-08-30(보고는 쉬운 말 절에서만 가져올 것).
- 결과: Claude·구현 Codex 양쪽 종료 장치에 같은 검사, 마감 보고서 수락 조건에도 세 칸.
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `**개선 3. 사용자 보고 양식` · bridge/verify-cap-handoff.js `REPORT_SECTIONS`.
- 찾는말: 보고 양식, 세 칸, 무엇이 바뀌었나, 이런 상황이 이렇게 됨, 다음에 할 일, 왜 보고가 막히나, 경비원

## D-2026-08-30-once-per-session-directives — 매 턴 같은 지시문을 다시 보내지 않는다: 세션 1회 전달+세대 결속
- 날짜: 2026-08-30(원칙)·2026-08-31(Claude·검증자)·2026-09-01(구현 Codex) · 종류: 확정(원칙) · 상태: 유효
- 결정: 매 턴 8천 자 넘는 같은 규약이 반복 주입돼 프로젝트가 오래갈수록 글이 자랐다 — 그래서 글을 다섯 성격으로 나누고,
  매번 같은 정적 지시는 세션에 한 번만(첫 턴·세션 시작/재개/압축·규약 세대가 바뀔 때만 전문) 보내고 매 턴은 한 줄 상태
  표시와 명령 줄만 보낸다. 강제 가능한 규칙은 문장이 아니라 종료 장치가 막고, 판단 재료는 요청 뼈대에 결속해 예산 안에서만
  싣는다(권위 자료는 자르지 않고 참고 자료만 자름).
- 왜: 사용자 지시 2026-08-30 — "매일 아침 사규 전문을 낭독하는 회사"가 아니라 "입사 때 한 번 읽고 출입문이 막는 회사"로.
- 결과: 검증자·Claude·구현 Codex 세 쪽 모두 전달 기록(세대 G)과 상태 줄을 갖고, 압축 감지는 세션 기록 파일의 압축 표시로.
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `## 4-B. 원칙` · bridge/contract-lib.js `function claudeDeliveryPlan` · `function codexDeliveryPlan` · `function deliveryPlanFor`.
- 찾는말: 규약 전달, 세션 1회, 세대, 왜 매 턴 안 보내나, 글 다이어트, 상태 줄, 압축 감지, 정적 지시

## D-2026-08-31-rules-all-included — 관련 수칙이 정상 범위를 넘어도 한 항목도 빼지 않고 전량 싣는다(시작 거부 없음)
- 날짜: 2026-08-31 · 종류: 개정 · 상태: 유효
- 결정: 보관 수칙이 96항까지 늘어 "이번 작업 관련"이 13항이면 검증이 시작조차 안 되던 설계였다 — 사용자가 승인한 절대
  차단 수칙은 권위 자료이므로 12항·4,000바이트를 실패 조건이 아니라 "정상 범위" 표시로 낮추고, 넘친 판은 정보 행과
  대시보드 서고 카드에 "정상 범위 초과 · 서고 정리 권장"을 띄우며 참고 자료 예산만 그만큼 줄인다.
- 왜: "권위 자료 때문에 하네스가 멈추는" 바로 그 구조가 4-B 원칙 위반.
- 결과: 선별기는 순위를 내지 않으므로 "상위만 싣기" 규칙은 두지 않고, 서고를 줄이는 것은 사용자의 정리 결정으로 남긴다.
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `**개선 4` (b)·(c) 선별 초과 절 · bridge/contract-lib.js `function selectorUnion`.
- 찾는말: 선별 초과, 전량 동봉, 정상 범위, 서고 정리 권장, 왜 검증이 시작 안 됐나, 12항 4000바이트, 서고 카드

## D-2026-09-01-anchor-guards — "이 대화의 폴더" 표시는 진짜 훅 입력에만 쓰고, 바뀌면 검증을 멈추고, 설정 없는 폴더는 알린다
- 날짜: 2026-09-01 · 종류: 확정(P7) · 상태: 유효
- 결정: 훅 스크립트를 시험 삼아 불러온 것만으로 "이 대화의 폴더"가 옆 프로젝트로 덮여 채점표 4장이 남의 공책에 적혔다
  (아무도 알려주지 않음) — 그래서 훅은 표준 입력에 이벤트 이름과 세션 id가 함께 있을 때만 폴더를 기록하고, 세션 도중
  폴더가 바뀌면 기록을 남겨 검증 시작을 거부하며(명시 플래그로만 진행), 프로필·상한 설정이 없는 폴더는 "기본값으로 돈다"를
  요청과 판정 양쪽에 한 줄로 알린다.
- 왜: 2026-08-28 실사고(ab-1 — 다른 프로젝트 장부 오귀속·기본값 계약이 조용히 동작).
- 결과: 로드 시험 재현 시험이 앵커 미기록을 고정하고, 거부 문구가 두 폴더를 함께 적는다.
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `**P7.` · bridge/contract-lib.js `function isRealHookInput` · `function folderChangeOf` · `function contractDefaultsNotice`.
- 찾는말: 앵커 이탈, 이 대화의 폴더, 폴더 변경, 왜 검증이 거부됐나, 기본값 고지, 훅 입력, 폴더 변경 확인 플래그

## D-2026-09-01-round-count-ledger — "몇 회차"는 장부가 센다, 제가 적은 번호는 권위가 없다
- 날짜: 2026-09-01 · 종류: 확정(P10) · 상태: 유효
- 결정: 요청문 제목에 "검증 5회차(최종)"처럼 번호를 손으로 적고 왕복 줄이 사라진 것도 모른 채 마감문에 "5/5"라고 썼다 —
  그래서 회차는 하네스가 턴+프로젝트 단위 장부로 세고, 마감 보고서에 적힌 회차 숫자는 장부 값과 다르면 받아 주지 않는다
  (숫자를 안 쓰면 검사 없음·일반 분수는 회차 아님).
- 왜: 사용자 지적 2026-08-29(손셈 회차)와 P7 실사고의 후속.
- 결과: 화면 회차도 장부 서수 단일 원천, 장부에 못 오른 검증은 권위 없는 진행 표시.
- 정본: docs/HARNESS-REALIGNMENT-2026-08-29.md `**P10.` · bridge/verify-cap-handoff.js `function roundFigureMismatch`.
- 찾는말: 회차, 장부, 손셈, 왜 마감문이 거부됐나, 5/5, 회차 대조, 왕복 줄

## D-2026-09-03-rule-self-check — 사용자가 적은 규칙은 구현자가 답을 끝낼 때 규칙마다 스스로 대조해 적어야 한다(하네스는 내용을 판정하지 않음)
- 날짜: 2026-09-03 · 종류: 확정 · 상태: 유효
- 결정: 사용자가 규칙 칸에 적은 문장(예: 상황예시로 설명하라)을 구현자가 자꾸 빼먹었고, 하네스가 세 칸 제목을 강제하는 것은 특정 사용자 선호를
  전역 양식으로 굳힌 잘못된 출발점이었으며, 채점자(검증자)에게 규칙 판정을 넘기는 안은 책임 전가라 기각됐다 — 그래서 답을 끝내려면 규칙 번호마다
  "지켰다/못 지켰다/해당없음 — 어디서" 한 줄을 적어야 하고, 안 적으면 접수 창구가 규칙 원문을 다시 보여 주며 돌려보낸다. 하네스는 그 줄이 있는지만 보고
  내용은 판정하지 않으며, 못 지켰다고 적는 것도 허용된다(장부에 남아 사용자가 본다). 옵션은 사용자가 켜고 끈다.
- 왜: 사용자 지시 2026-09-02(구현자 책임·검증자 전가 기각·불특정 사용자용 하네스) — 규칙을 어긴 방식이 판단 착오가 아니라 "답 쓸 때 규칙이 시야에 없음"이었기에.
- 결과: 턴 중 규칙이 바뀌면 새 규칙 원문으로 다시 적게 하고, 반복해서 빼먹으면 무한 차단 대신 "미기재"로 장부·노랑 경보에 남는다. 세 칸 제목 상수는 폐지.
- 정본: docs/RULE-COMPLIANCE-DESIGN.md `## 헌법` · `### B. 종료 훅의 구조 검사` · bridge/contract-lib.js `function effectiveRuleCheck` · `function ruleCheckVerdict`.
- 찾는말: 규칙 자가점검, 계약점검, 체크리스트 강제, 왜 세 칸을 폐지했나, 사용자 규칙, 규칙 지문, 미기재, 구현자 책임

## D-2026-09-03-curation-independent — 수칙을 넣고 빼자는 제안은 장부를 읽는 독립 실행이 소수만 올리고, 사용자는 승인·안 올림만 한다
- 날짜: 2026-09-02(설계)·2026-09-03(1·2단계 구현) · 종류: 확정 · 상태: 유효
- 결정: 서고가 비어 있던 것은 정리 담당이 필요 없어서가 아니라 공급이 꺼져 있어서였다(8월 26일 자동 상신이 74항으로 불려 끈 뒤 남은 경로는 구현자 수동 상신과
  약속 포착뿐). 그래서 답안을 쓴 사람이 아닌 독립 실행이 대출 기록(반복 강등·계보 반복·한 번도 선별되지 않은 보관 수칙·되받아침에 쓰인 제외 칸·결정 장부)을 읽고
  "이 책은 앞줄로, 이건 창고로" 식으로 회당 3건·미승인 6건 이내로만 제안한다. 제안 자격은 고쳐진 이력이 아니라 프로젝트를 관통하는 성격이고, 기존 항목과 같은
  문안은 거부된다. 사용자는 기존 제안함에서 승인/안 올림만 하며, 승인은 기존 초안·도장 절차를 그대로 탄다(빼기는 제안 시점 번호·문안 지문·대상 세대 셋이 지금과
  같을 때만). 검증은 이 실행을 기다리지도 참조하지도 않는다.
- 왜: 사용자 지시 2026-09-02 "수칙 후보로 올리는 것은 내가 아닌 하네스의 몫" + 헌법(구현자가 자기 답을 수칙으로 올리는 이해충돌 차단·소수정예).
- 결과: 같은 장부 상태에 두 번 물어도 같은 제안 id가 나오고(멱등), 같은 상태에 다른 답이 오면 첫 답이 권위이며 경보로 남는다. 실행 실패는 영수증과 노랑 경보 하나로
  끝나고 검증에는 영향이 없다. 자동 실행 시점(캠페인 10건·신호 임계)과 대시보드 버튼은 다음 단계.
- 정본: docs/CURATION-DESIGN.md `## 헌법` · `### D. 커밋` · bridge/curation.js `function runCuration` · bridge/contract-lib.js `function draftCuratorCandidate`.
- 찾는말: 큐레이션, 정리 제안, 독립 실행, 왜 서고가 비었나, 회당 3건, 미승인 6건, 빼기 3중 대조, 승인 시 변환, applied 종결, 자동 상신 폐지

## D-2026-09-04-round-refund — 검증자가 답을 한 글자도 내지 못한 호출은 왕복으로 세지 않는다(캠페인당 2회까지)
- 날짜: 2026-09-04 · 종류: 확정 · 상태: 유효
- 결정: 2026-09-03 큐레이션 검증에서 Codex 서버가 404를 세 번 돌려주어 답을 못 받았는데도 회차 3·4·5가 소모돼 상한에 닿았고, 사용자가 "상한 뒤에도
  추가 질문 흔적"을 발견했다(실제로는 상한 전의 실패 호출 3건이 검증자 대화창에 질문만 남기고 답이 없던 것). 전화를 걸었는데 신호가 안 간 것은
  통화 횟수가 아니므로, 답 0자 실패 호출은 방금 잡은 회차를 돌려준다. 다만 장애가 계속될 때 무한히 공짜로 다시 걸지 못하게 캠페인당 2회까지만
  돌려주고 그 뒤에는 종전처럼 소모한다. 답이 온 판(보류·실패 판정 포함)은 판정이므로 돌려주지 않는다.
- 왜: 왕복 상한은 "검증자 판정의 수"를 묶는 장치이지 네트워크 장애를 벌하는 장치가 아니다. 그러나 무제한 환급은 장애 중 재시도 폭주(비용·대화창 오염)를 부른다.
- 결과: 실패 호출 뒤 사용자에게 "[회차 환급] … 이 캠페인 환급 n/2회 · 사용 회차 N/M"이 보이고 작업 파일에 roundRefunded가 남는다. 캠페인을 오가며 다른 작업을 하다 돌아와도 환급 누계는 그대로 이어진다(교대 복원 시 함께 복원). 검증자 대화창에 답 없는 질문이 남는 것(Codex CLI가
  전송 전에 기록)은 하네스 밖이라 그대로다 — 연속 실패 시 doctor 확인 후 재시도가 권장 절차.
- 정본: bridge/contract-lib.js `function refundVerifyCampaignRound` · bridge/codex-bridge.js `Codex resume 실패` 분기 · tests/verify-refund.test.js.
- 찾는말: 회차 환급, 404, 답 없는 실패, 왕복 상한, 무한 재시도, VERIFY_REFUND_MAX

## D-2026-09-04-curation-auto-trigger — 정리 제안은 세 갈래로만 시작한다(버튼·마감 캠페인 10건·반복 신호 임계)이고 같은 신호 상태에 두 번 묻지 않는다
- 날짜: 2026-09-04 · 종류: 확정 · 상태: 유효
- 결정: 독립 정리 담당은 ①사용자가 대시보드 버튼을 눌렀을 때 ②마지막 정리 이후 검증 캠페인이 10건 마감됐을 때 ③장부 신호가 임계에 닿았을 때(같은 제외 칸 강등 2회·같은 지적 계보 3회·보관 수칙이
  30일간 한 번도 선별되지 않음·선별 초과 3회) 중 하나에서만 시작한다. 대화 창구(훅)는 작은 상태 파일과 잠금만 보고 판정 일꾼을 뒤에서 따로 띄우며, 판정은 지금 승인된 수칙서 세대의 열린 지적만 세고
  마감 캠페인 수는 오래된 이력이 지워져도 잊지 않도록 따로 누적한다. 판정 결과의 지문이 마지막 실행과 같으면 다시 묻지 않는다(10분 최소 간격·실행 중이면 무실행).
- 왜: 8월 26일 자동 상신으로 서고가 74항으로 불어난 사고의 재발 방지 — 검증마다 묻는 방식은 비용과 서고 비대를 부르고, 사용자 버튼만으로는 공급이 비어 있던 원인(§0)을 못 채운다. 세 갈래는 "관장이 부르거나·대출 기록이 쌓이거나·반납 창구에서 같은 소리가 반복될 때만 사서가 제안하는" 규칙이다.
- 결과: 검증 경로는 이 실행을 기다리지도 참조하지도 않는다. 수칙 카드 1줄에 마지막 정리 시각·트리거·제안·미승인·채택률·서고 크기·선별 초과가 보이고, 실패는 노랑 경보 1종으로만 나타난다.
- 정본: docs/CURATION-DESIGN.md `### A. 트리거` · `## §9 구현 기록` · bridge/curation.js `function curationTickJudge` · `function curationHookTick`.
- 찾는말: 정리 제안, 큐레이션 트리거, 자동 실행, 마감 캠페인 10건, 신호 임계, 같은 신호 상태, 정리 제안 받기, 채택률

## D-2026-09-05-ledger-repo-partition — 지적 장부의 활성 판독은 "이 저장소 표식의 행"만 본다(옛 무표식 행은 이력)
- 날짜: 2026-09-05 · 종류: 확정 · 상태: 유효
- 결정: 같은 작업 폴더에서 정찰 대상 저장소를 A→B→A로 오가면, 캠페인 번호·수칙서 세대가 같아도 "누구 것인지"를 캠페인 번호로는 가릴 수 없었다(창 두 개가 같은 캠페인을 이어가면 한 캠페인이 두 저장소에 걸친다). 그래서 지적·등장·종결·승격·처분·판단·결정 행에는 기록 시점(검증은 시작 시점)의 저장소 표식을 심고,
  판 유형·라운드 번호·열린 지적·되받아침·시작 관문·검증자 프롬프트 주입·판단 명령(유효성 표시·fix-gap 누계 포함)·결과에 덧붙는 부가 보고(원인 분해·후보 재료·재심 재료)·수칙 상신(rule-propose)·지도 수확기·정리 제안 신호는 **표식이 일치하는 행만** 읽는다. 업그레이드 이전의 표식 없는 행은 어느 저장소 것인지 모르므로 새 판정에서 제외하고 이력으로만 남긴다(고쳐 쓰지 않음).
- 왜: 확인검증 4판 연속 반례(2026-09-05) — B 저장소의 통과 판이 A의 판 유형을 바꾸고, B의 열린 지적을 A 통과가 닫고, B의 되받아침이 A의 같은 제목 지적을 강등하고, 옛 무표식 행을 포함하면 그 종결이 다시 생기는 것이 실측됐다. "옛 행은 연속성을 위해 포함"은 저장소 오귀속과 양립하지 않았다.
- 결과: 업그레이드 직후 진행 중이던 캠페인의 옛 열린 지적은 새 통과 판이 자동으로 닫지 않고, 관문도 그 지적으로 시작을 막지 않는다(사용자 흐름 무중단). 저장소 키를 모르는 경로만 종전 전체 판독으로 축퇴한다. 판단 관문 마커도 검증 시작 저장소를 기억하며, 표식 없는 옛 마커의 판단은 현재 저장소로 꾸며 찍지 않고 "소속 불명" 그대로 남긴다(이력만). 사용자 결정(escalate)은 같은 저장소에서 만든 항목만 묶는다. 결정 장부의 목록·선택·화면·마감 동봉도 현재 정찰 대상 저장소의 항목만 보이고, 다른 저장소의 결정은 그 저장소를 대상으로 연 창에서만 답할 수 있다(항목 번호에도 저장소가 들어가 같은 질문이 두 저장소에서 따로 존재).
- 정본: bridge/contract-lib.js `function ledgerRowsForRepo` · `function repoKeyNow` · `function appendFindingsLedger` · `function fixGapCount` · `function ruleProposeCandidate` · `function resolveJudgeRequired` · `function readDecisions` · `function decisionIdFor` · `function resolveDecision` · bridge/codex-bridge.js `function machineFindingsLayer` · `function implementerRebuttalsFor` · `function v2DynamicData` · `function findingDispositionGate` · `function breakdownNoticeFor` · bridge/map-provenance.js `function harvestFromResolvedFinding` · docs/CURATION-DESIGN.md `## §9 구현 기록`.
- 찾는말: 저장소 표식, 장부 분할, 옛 행 제외, 캠페인 번호로 못 가른다, 되받아침 격리, 관문 저장소, 주입 저장소, 창 두 개

## D-2026-09-06-closeout-reject-reason — 마감문이 되돌아오면 "어느 절·몇 번째 줄·어느 칸이 왜"를 관문이 말해 준다
- 날짜: 2026-09-06 · 종류: 확정 · 상태: 유효
- 결정: 검증 상한이 다 찬 뒤 쓰는 마감 보고가 종료 관문에서 두 번 되돌아왔는데, 되돌아온 안내는 매번 "회차 숫자를 쓰면 장부 값 그대로"라는 같은
  첫 줄만 보여 진짜 이유(한 칸에 파일명 같은 확인 가능한 근거가 없었음)를 알 수 없었고, 구현자가 추측으로 고치다가 결국 검사 함수를 손수 돌려 통과시켰다 —
  사용자가 "구현자가 배우는 게 아니라 하네스가 그 상황을 만든 것 자체가 문제"라고 지적해, 관문 거부문 첫 줄에 실패한 절·줄·칸과 필요한 조건을
  그대로 적기로 했다. 구현자의 기억이나 사전 검사 습관에 기대지 않는다.
- 왜: 사용자 지적 2026-09-06(같은 마감 보고가 화면에 3번 남음). 되돌림은 설계대로였지만 이유 없는 되돌림은 재시도만 늘린다.
- 결과: 검사기가 첫 실패 지점을 문장으로 돌려주고, Claude·Codex 두 종료 훅의 거부문이 그 문장을 첫 줄에 붙인다. 승인·거부 판정 자체는 바뀌지 않는다.
- 정본: bridge/verify-cap-handoff.js `function validateCapHandoff` · `function capHandoffInstruction` · `function categoryLines` · tests/cap-closeout-detail.test.js.
- 찾는말: 마감문 거부, 왜 되돌아왔나, 거부 사유, 마감 관문, 형식 검사, 세 칸 근거, 같은 보고 반복

## D-2026-09-06-curator-arm-follows-scout — 정리 담당은 탐색 담당과 같은 회사가 답하고, 재료가 없으면 부르지 않으며, 대시보드 버튼은 결과 보기뿐이다
- 날짜: 2026-09-06 · 종류: 개정(P4·§3 A·§3 B) · 상태: 유효
- 결정: 정리 담당(수칙 넣기/빼기 제안)이 "구현 턴과 같은 회사"로 답하던 규칙을 바꿔 **탐색 담당과 같은 회사**가 답하게 한다(기본 정찰=Claude, Codex 정찰=Codex의 정찰 두뇌 설정, DeepSeek 정찰=DeepSeek).
  이유는 사용자가 정한 것 — Claude 쪽은 추론 강도를 정할 수단이 없고 하네스가 모델도 넘기지 않아 실제로는 명령줄이 고른 모델(sonnet-4-5)이 답하고 있었는데, 두뇌를 정할 수 있는 쪽은 탐색 담당(Codex 두뇌 설정·DeepSeek)뿐이며
  "도우미 두뇌 설정"을 새로 만드는 것은 설정 파편화라 거부됐다. 정리 담당이 보내는 재료(승인 수칙 문안·보관 서고 전체·장부 집계·결정 질문)는 정찰 꾸러미와 다른 범주이고 결정 질문은 저장소 밖 자료이므로, 이 사실을
  PRIVACY.md·정찰 카드 FAQ에 명시하고 "탐색 담당 선택=동의"로 둔다(키 등록=동의 모델과 같음). 이는 하네스 작성자의 설계 결정이며 사용자별로 되풀이 승인하는 절차는 만들지 않는다. 함께 정한 것: 재료(신호)가 0이면 첫 실행이라도 담당을 부르지 않는다,
  근거(refs) 없는 제안은 후보로 받지 않는다, 코드 울타리 한 겹은 벗겨 읽는다(2026-09-03 1회차 검증의 "울타리=손상" 결정을 이 한 겹에 한해 개정), 대시보드 버튼은 "마지막 정리 결과 보기"로 바꾸고 실행은 자동 트리거와 명령줄만 남긴다.
- 왜: 사용자 실보고·설계 질의 2026-09-05~06 — 버튼 첫 클릭이 재료 0인데 담당을 불러 울타리 답을 거짓 실패로 경보했고, 둘째 클릭은 무알림 생략이었으며, "정리 담당은 원래 탐색자의 추가 역할"이라는 원 설계(HARNESS-REALIGNMENT §1)에서
  "구현 쪽 같은 회사"로 흘러간 경위가 기록에 없었고, 별도 두뇌 설정 제안은 파편화였다.
- 결과: 서고 선별 담당은 종전(구현 턴 provider 고정 — 원문 스냅샷 경계) 그대로이고 선별 실행기(selector-runner)에는 손대지 않는다(교차 팔 없음 소스 계약 유지 — DeepSeek 호출은 정리 담당 전용 실행기가 따로). 설정 항목은 하나도 늘지 않는다. 검증 경로 무접촉.
- 정본: bridge/curation.js `function selectorArmForCuration` · `function curationSkip` · `function parseCurationOutput` · bridge/curation.js `function runCurationDeepseekPage` · bridge/deepseek-bridge.js `page` 명령 · src/extension.ts `curationShow` · PRIVACY.md 정리 담당 실행 시 절 · docs/CURATION-DESIGN.md `## §9 구현 기록` ㉑.
- 찾는말: 정리 담당 두뇌, 탐색 담당과 같은 회사, 정리 재료 전송, 재료 없으면 안 부름, 울타리 허용, 마지막 정리 결과 보기, 왜 버튼이 사라졌나, 설정 안 늘림

## D-2026-09-06-evidence-read-form-drift — 검증자가 파일을 읽었는지 보는 감시 창구는 함수 이름이 아니라 "명령을 넘기는 호출 모양"과 "리터럴 목록 반복"으로 알아본다
- 날짜: 2026-09-06 · 종류: 봉합(경보 축 한정) · 상태: 유효
- 결정: 검증 답이 인용한 파일을 검증자가 그 판에서 실제로 읽었는지 확인하는 탐지기가, 검증자 CLI의 실행 함수 이름(tools.shell_command)만 알아보다가 2026-08-20 개명(tools.exec_command) 뒤 배열 판독을 통째로 놓쳐 검증마다 "근거의심" 경보와 재확인 호출이 났다(실측 09-06: 검증 14회 중 경보 10회·재확인 발송 5회·나머지는 사용자가 확인 클릭으로 지움).
  ① 실행 호출은 이름 목록이 아니라 계약 형태로 판정한다 — 도구 이름공간 호출(`tools.<함수>(`)이면서 인수 구간에 명령 키(cmd|command·단축 속성 포함)를 넘기는 것. 이름 목록을 늘리는 방식은 다음 개명에 또 깨지고, 명령 키만 보면 미실행 사용자 함수(`stash({cmd:cmds})`)가 흔적을 위조하므로(1판 blocker ab-3) 둘을 함께 요구한다. 배열이 '쓰였다'는 판정은 **명령 값의 데이터 흐름**으로만: 명령 키의 값 식이 배열 원소를 직접 참조하거나(cmds[i]) 호출을 감싸는 `for (const X of cmds)` 본문·`cmds.map(X => …)` 콜백 안에서 그 반복 변수를 참조할 때(2판 blocker ab-3: `max_output_tokens:1000+cmds.length`처럼 무관한 자리의 이름은 사용이 아니다).
  ② PowerShell 리터럴 배열/해시 + foreach + Get-Content 형태는 같은 호출 텍스트 안에서 foreach 앞의 **마지막** 대입이 리터럴 배열일 때만, foreach **본문 안에서만** 원소별로 풀어 '약한 흔적'으로 인정한다(재대입=값 불명·비리터럴 원소·본문 밖 참조·64 변형 초과=미인정 — 1판 blocker ab-3 반례 2건 반영). 주석(`# …`·`<# … #>`)은 지우고 읽으며 중괄호 깊이 0(최상위)의 대입·foreach만 실행된 것으로 본다(블록 안 대입은 값 불명 — 2판 blocker ab-3: 주석·`if($false){…}` 속 대입 위조). 변수명은 PowerShell 의미대로 대소문자를 구분하지 않는다(2판 보완).
  ③ 같은 실측에서 드러난 별개 결함: 인라인 명령 문자열에 이스케이프 따옴표(\")가 있으면(rg -n "패턴" 파일 같은 가장 흔한 판독) 정규화본에서 문자열 경계가 깨져 명령 전체가 사라졌다 → 중첩 호출 추출은 원시 인수에서 한다(정규화본 폴백은 원시 문자열이 없을 때만).
  ④ 임의 코드 실행기(node -e 등) 제외와 신뢰 등급 승격(strong=하네스 기록 인수만) 규칙은 그대로 — 이 봉합은 경보 축만 넓힌다.
  ⑥ 4판 반영 — 배열 경로는 fail-closed 허용목록: 코드 전용 본문(주석·문자열 자리 공백)에서만 선언·사용·호출을 보고, `tools`가 멤버 접근 외 자리에 나오면(그림자 가능) 전체 미인정, 배열 이름의 모든 등장이 읽기 전용 허용 형태(`ARR[식]` 읽기·`ARR.length`·순회 콜백·`of ARR`)여야 하며(재대입·원소 대입·변이 메서드·별칭·다른 함수 인수=미인정), 실행 호출에 명령 키가 둘 이상이거나 계산된 키가 있으면 그 호출 미인정. 5판 반영: 쓰기 판정은 대입 계열 연산자 전체(`=`·복합·논리·비트·증감·`delete`·구조 분해)를 **문장 단위**로 보고(배열 이름이 든 문장에 쓰기 연산이 하나라도 있으면 미인정), `tools` 멤버 변이도 같은 집합으로 막으며, 배열 원소는 코드 본문상 순수 문자열 리터럴 자리에서만 읽고(주석·객체·중첩 배열이 섞이면 배열 전체 미인정), 상수 첨자면 그 원소만·반복 변수면 전 원소·변수 첨자는 미인정, 반복 변수의 범위 안 그림자 선언(함수/화살표 매개변수·const/let/var·catch)은 미결속.
  ⑦ **사용자 결정 73edb5984bc045e8(2026-09-07 · 위험 감수)**: 6판 연속 새 구문 반례(유니코드 철자 변수·중복 키·템플릿 문자열·메서드 그림자·some 조기 종료·PowerShell '' 이스케이프)가 나와 "정형 문장만 인정"으로 확정 — JS 명령 배열 경로는 스크립트 전체가 정형 S0(리터럴 배열 선언)+S1(for-of 루프)/S2(Promise.all|allSettled(map))/S3(상수 첨자 호출)/S4(명령이 문자열 리터럴인 호출)+선택적 text(…)로만 이루어졌을 때만 인정하고, 그 밖의 글자가 하나라도 있으면 통째로 미인정(`templateArrayReads`). 7판 반영: 도구 함수명은 실측 2종(tools.exec_command|shell_command)으로 고정(상속 속성 tools.constructor 차단·개명 시 경보가 남는 쪽이 안전) · S0 원소의 JS 이스케이프는 \" \' \\ 만 허용(\n·\uXXXX 등은 실행값이 달라져 배열 미인정). 8판 반영(2026-09-07): 정형은 **스크립트 전체**에 적용 — 인라인 리터럴 명령(S4)·객체 배열(`[{cmd|command:"…", <옵션>*}]`)·이름 붙은 객체(`const o={…}; await tools.f(o)`)도 정형 안에서만 인정하고 별도 인라인 추출 경로(nestedShellCalls·'괄호 안 객체=쓰임' 판정)는 폐지(8판 blocker ab-3: 정형이 거부한 스크립트에서 인라인 추출이 유니코드 철자 중복 키 호출의 첫 cmd를 흔적으로 되살림·지역 tools 그림자 동일) · 문자열 리터럴의 JS 이스케이프는 실행 의미 그대로 복원(\n·\t·\uXXXX·\xHH 포함 — 7판 허용목록은 위치별 검사 오류로 정상 Windows 경로(`\\`)를, 목록 자체로 여러 줄 PowerShell 리터럴(`\n`)을 거부 — 8판 blocker; 8진·줄 이음·알 수 없는 이스케이프=미인정) · 서로 다른 workdir 리터럴 2개 이상=통째 미인정(종전 잠금 승계) · 같은 옵션 키 반복=미인정 · 마무리 text(R)·R.forEach(x=>text(x)) 허용. 9판 반영(2026-09-07): 약한 축(경보) 해제 조건=성공 호출+**출력에 그 파일의 실제 줄 하나 이상**(공백 제거 8자 기준·긴 줄이 3개 미만인 작은 파일은 4자 이상·출력 줄의 흔한 접두 벗김·작은 출력은 부분 문자열) — `Get-Content foo -TotalCount 0; Write-Output done`처럼 다른 문장의 출력으로 해제되던 통로 차단(9판 blocker ab-3). 대가: 개수·해시만 출력한 판독은 흔적이 아니다(2026-07-29 '개수·해시도 다룬 흔적' 처방 철회 — 모델이 내용을 받지 못한 판독은 인용 근거일 수 없음). 10판 반영(2026-09-07): **출력 소유권** — 한 외부 호출의 출력은 여러 판독이 구분 머리 없이 합쳐진 것이라(실측 S5 `outputs.forEach(r=>text(r.output))`) 파일 F의 흔적으로 세는 줄은 같은 호출이 글자로 지목한 다른 실재 파일들(`callMentionedFiles`: 명령 글자의 경로 토큰 전부)에는 없는 줄이어야 한다(10판 blocker ab-3: foo -TotalCount 0 + bar 판독의 공통 줄로 foo 해제). 예외=출력 줄에 F의 이름 접두(rg/Select-String)·앞선 구역 표시 줄(`@@ 파일명`)이 F를 적음. 대가: 내용이 완전히 같은 두 파일을 이름 표시 없이 한 호출에서 읽으면 둘 다 흔적이 아니다. 정형에 S5(`Promise.all([tools.f({cmd:"…"}),…])`) 추가(오늘 실측 52건 중 9건이 이 형태). 11판(2026-09-07): 마무리(출력 전용) 문장은 모양 열거 대신 **검증기**로 — 원문에 tools·await 없음, 대입·증감·delete·new·function·class·var/let·while 없음, 식별자는 내장·결과 변수·반복/화살표 매개변수·`text(` 호출만, 선언 배열은 `NAME[정수]` 읽기만, `${…}` 안도 동일(오늘 미인식 20건 중 정직한 11건이 `text(r.output); text(\`EXIT ${r.exit_code}\`)`·`for (const r of results) text(JSON.stringify({…}))`·`if (r.session_id) text(…)` 같은 마무리 변형). 문장은 깊이 0의 `;`/개행으로 나눠 선언/호출/마무리로 분류한다. 11판 D(구현자 자가 검증에서 발견): 읽지 않고(-TotalCount 0) 파일 실제 줄을 `text("…")` 리터럴로 찍으면 약한 축이 거짓 해제됐다 → 약한 축 출력 대조의 제외 집합에 **스크립트 자신의 문자열 리터럴 줄**(`scriptLiteralLines`)을 더한다(검증자가 내용을 타이핑할 수 있다는 것은 이미 안다는 것 — DECISIONS ⑤ 지식 경계 · 정직한 판독은 `text(r.output)`처럼 결과 변수를 찍지 내용을 적지 않음). RCE류(tail의 `constructor.constructor(…)()`·`Object.defineProperty`로 명령 변이·문자열 첨자)는 마무리 검증기가 이미 통째 미인정(직접 실측 확인). 강한 축(승격)은 스크립트 글자로 못 여는 계약 불변. 11판 확인검증(Codex 확인형 요청 통과·blocker 2 발견): ⓐ마무리에서 Object.defineProperty/assign로 선언 명령을 변이하는 통로 → 마무리 허용 내장에서 Object/Array 제거(순수 판독·출력만·변이 통로 차단) ⓑ파일명 소유 예외가 출력 줄 본문 아무 데서나 발동 → 판독 도구가 붙이는 경로 접두(줄 시작 경로 토큰+콜론)·구역 표시 줄(`@@ 파일명`)일 때만으로 고정. 시험 (tp20a~d)(tp21a~d). 11판 확인검증 2차(같은 원칙으로 일반화·blocker 2): ⓒ선언 원소에 메서드 호출(`cmds[0].__defineGetter__(…)` 등)로 파싱 뒤 명령을 바꾸는 통로 → 마무리에서 선언 데이터는 '읽기'만(NAME[정수] 뒤 메서드 호출 금지)+dunder(`__…__`) 어디에도 금지 ⓓ파일명 소유 표식을 판독 도구가 붙이는 것으로 한정(줄 시작 경로 토큰+콜론이나 `@@ 파일명`만 — 콜론/@@ 없는 줄 시작 파일명은 소유 아님). 시험 (tp22a~d)(tp23a~c). 11판 확인검증 3차(blocker 2·모두 봉합): ⓔ선언 원소는 값 읽기만(접근 사슬 `.prop`/`[정수]`을 소비한 뒤 호출 `(`이 오면 미인정 — `cmds[0].cmd.toString()` 같은 사슬 메서드도 차단)·마무리 for는 for-of만(C 스타일 `for(;;)` 미인정) ⓕ소유 표식 경로 접두에 Windows 절대 경로(드라이브 콜론) 허용 — 정직한 절대 경로 판독이 경보로 남지 않게(콜론/@@ 없는 언급은 여전히 소유 아님). 시험 (tp24a~c)(tp25a~d). 11판 확인검증 4차(blocker 3·모두 봉합): ⓖ선택적 호출(`?.()`)·괄호로 감싼 값 호출(`(expr)()`)도 마무리 금지(사슬 호출 우회 차단) ⓗ소유 표식 경로 토큰이 공백을 허용(콜론 앞까지가 경로 — `D:\My Project\foo.ts:2:` 등 정직한 공백 경로 판독이 경보로 남지 않게) ⓘ callMentionedFiles가 따옴표로 묶인 공백 경로를 통째 토큰으로 인식(공백 경로의 다른 지목 파일이 제외 집합에서 누락돼 대상 파일이 거짓 해제되던 ab-3 경로 차단). 시험 (tp26a~c)(tp27a~e). **사용자 결정 8f2908ced14fc9a2(2026-09-09 · 마감)**: 9/7 실측(경보 12건→3건·검증자 스크립트 138건 중 정직한 미인식 8건)에서 6건이 검증자가 실제로 쓰는 `for (let i = 0; i < results.length; i++) text(…)` 마무리인데 12판 ⓔ 'for는 for-of만'(검증자의 '무한 루프도 인정된다' 지적 수용)이 함께 차단한 것으로 확인 → 결과 목록 첨자 순회 for 머리는 for-of 동치로 인정(첨자·결과 변수 검사 동일), 무한 루프 `for(;;)`·그 밖의 C 스타일 for는 계속 미인정. 무한 루프는 경보를 잘못 끄는 길이 아니라 검증자를 멈추게 할 뿐이므로 원래 blocker 사유가 아니었음(경계 기록). 나머지 정직한 미인식 2건(숫자 범위로 명령 조립 `ranges.map(([a,b]) => …)`)은 6판 결정대로 정형 밖=경보 유지. 이 결정으로 캠페인 마감(추가 판은 확인형 1회 후 결과 무관 종결). 시험 (tp28a~c). 이름 목록·데이터 흐름·허용목록 판정 헬퍼(execLikeCalls·arrayUsageAllowed·arrayBoundIdents)는 폐기. 정형 안에서는 다른 문장이 없어 명령 리터럴이 그대로 실행되므로 '인정=실행됨'이 글자 수준에서 닫힌다. 대가: 정형 밖 모양으로 읽은 판은 의심 경보가 남고 재확인 절차가 돈다(실측 3형태는 정형 안). PowerShell 작은따옴표 `''`는 따옴표 한 글자로 읽는다.
  ⑤ 약한 축의 경계(3판에서 명문화): 스크립트 '글자'는 어느 줄이 실행됐는지 증명하지 못한다(2026-07-28 결정과 동일). 그래서 글자로 판별 가능한 구조만 인정한다 — 명령 값 식은 `ARR[첨자]` 또는 배열에서 묶인 반복 변수 **그 자체**만(조건식·연산·호출·문자열 결합이 섞이면 미인정), 반복 변수는 호출 전 재대입이 없어야 하고 `reduce`의 첫 매개변수(누산기)는 원소가 아니며, PowerShell 스캔은 주석과 **문자열 내부**를 지운 본문에서 최상위 대입·foreach만 본다. 이 밖의 실행 의미론(런타임 값·죽은 분기)은 약한 축의 범위 밖이며, 그 통로로 남는 것은 검증자 자신이 의도적으로 죽은 코드를 쓰는 경우뿐이고 그 경우에도 판정·승격은 영향받지 않는다(ab-3의 '보조 장치 우회' 제외 조항 — 경보는 보조 장치).
- 왜: 사용자 실보고 "근거의심 경고가 매 턴 여러 건, 여러 번 고쳤는데 계속됨"(2026-09-06). 앞선 세 봉합(08-21 미ack 반복 억제·08-27 지문 억제·no-dispatch 자동 확인)은 '같은 의심의 반복'만 다뤄 근본(판독 형태 미인식)을 손대지 않았다. 재확인 파일 상한 8 때문에 9개 이상 인용은 지문 억제가 시도조차 안 되고, 보류 판 뒤에는 재확인 발송이 없어 억제 근거도 안 남는다.
- 결과: 검증자가 현재 쓰는 세 판독 형태 중 둘(명령 배열+실행 호출·PowerShell 리터럴 목록 반복)과 따옴표가 든 인라인 명령이 흔적으로 잡힌다. 작은 스크립트(node -e)로만 읽은 파일은 설계대로 여전히 경보 대상(위조 방지 경계).
- 정본: bridge/codex-bridge.js `function templateScriptCommands` · `function psLiteralForeachVariants` · `function toolReadParts`(스크립트=정형 일치기 하나 · 함수 호출 인수=강한 축) · `function outputContainsFileLine`·`function callMentionedFiles`(약한 축 해제=출력 내용 대조+출력 소유권) · tests/evidence-unseen.test.js [3-7].
- 찾는말: 근거의심 반복, 매 턴 경고, 읽은 흔적 없음, exec_command, shell_command 개명, PowerShell foreach Get-Content, 재확인 호출 비용

## D-2026-09-14-decision-supersede — 선택지 밖 답이 오면 새 항목을 올리되 원항목은 '대체됨'으로 닫는다
- 날짜: 2026-09-14 · 종류: 확정 · 상태: 유효
- 결정: 사용자가 결정 장부 항목에 선택지 밖의 답을 하면(예: "압축 무반응") 구현자는 그 답을 담은 새 항목을 올리고, 원항목에는 같은 호출로 '대체됨(대체 항목 id)' 행을 남긴다(`decisions raise --supersedes <원항목>` 또는 `decisions supersede <원항목> --by <대체 항목>`). 대체된 항목은 열린 목록·개요 '지금 정할 것'·마감문에서 빠지고 이력으로만 남는다(삭제 없음 · ab-5).
- 왜: 2026-09-09·09-11에 선택지 밖 답을 새 항목으로만 기록해 원항목 2건이 열린 채 남았고, 사용자가 "결정 장부에 있는 사항을 왜 못 봤나"를 물었다 — 실제로는 둘 다 답한 사안의 낡은 중복이었다. 장부에 철회·대체 행이 없어 구현자가 닫을 길이 없었던 것이 구조 원인.
- 결과: 낡은 열린 항목이 '지금 정할 것'에 쌓이지 않는다. 대체 행은 원항목·대체 항목이 같은 저장소 장부에 실존할 때만 기록된다(자기 자신·타 저장소·미실존 거부).
- 정본: bridge/contract-lib.js `function supersedeDecision` · `function openDecision`(spec.supersedes) · bridge/codex-bridge.js `decisions supersede` · tests/decisions.test.js '대체(superseded) 행'.
- 찾는말: 결정 장부, 열린 결정이 남음, 선택지 밖 답, supersede, 지금 정할 것 낡은 항목, decisions raise --supersedes

## D-2026-09-14-backlog-groups — 보관함 카드는 개요와 같은 세 묶음(검토 기한·여유·백로그)으로 나눠 보이고, 개요 버튼은 그 묶음으로 바로 간다
- 날짜: 2026-09-14 · 종류: 확정 · 상태: 유효
- 결정: 개요의 '보관함 검토 기한 n'·'여유 m'을 눌러 보관함 카드로 오면 어느 항목이 검토 기한이고 어느 것이 여유인지 구별되지 않았다(사용자 실보고). 카드는 검토 기한(주의+오래됨/자주 재발견 — 개요 합산)·여유(주의·기한 없음 — 합산 밖)·백로그(범위 밖 제안 — 합산 밖) 세 묶음 제목 아래에 항목을 나열하고, 항목 배지도 묶음 이름으로 통일하며, 요약 줄은 개요와 같은 어휘로 쓴다. 개요 버튼은 카드 전체가 아니라 해당 묶음 제목으로 이동해 비춘다.
- 정본: src/extension.ts `computeBacklogView`(group·later) · 보관함 카드 렌더(`blGroupDue`·`blGroupLater`·`blGroupBacklog`) · 개요 `decideActs`/여유 줄 이동 · tests/p12-backlog.test.js RB-7~11·CB-V7 · tests/sidebar-shell.test.js.
- 찾는말: 보관함, 검토 기한, 여유, 백로그, 지금 정할 것 이동, 구별 안 됨

## D-2026-09-14-kernel-refactor-policy — 중심 3파일(커널)은 갈아엎지 않고, 다음 수정이 그 자리를 건드릴 때만 경계가 보이는 묶음을 밖으로 뺀다
- 날짜: 2026-09-14 · 종류: 확정 · 상태: 유효
- 결정: 몇 주 동안 기능이 붙는 사이 브릿지의 규칙·장부·지도·검증 살림이 파일 세 개에 모여 버려서, 기능 하나를 고칠 때 다른 장부와 엮이는 지점을 사람이 머릿속으로 따라가기 어려워졌다. 그렇다고 지금 그 세 파일을 통째로 다시 짜면(릴리스 직후·두 모드×두 언어 정상인 상태) 검증 비용만 쓰고 사용하는 사람이 느끼는 변화는 없다. 그래서 세 파일은 지금 옮기지 않는다. 대신 ① 새 기능·수정이 중심 파일에 함수를 새로 넣어야 하면 별도 파일로 만든다(선례: 인용 대조·정리 담당·지도 회전·수칙 흐름 — 중심 파일을 건드리지 않고 시험 가능) ② 기존 함수는 '그 작업 때문에 어차피 고치는 함수'만 같이 빼되 순수 계산(입력→출력만)부터, 잠금·파일 쓰기가 섞인 것은 뒤로 ③ 한 번에 한 묶음만 옮기고 검증으로 닫는다(통과까지 필요한 왕복 수는 지적에 따라 달라진다 — 왕복 수를 미리 정하지 않는다) ④ 폴더 재배치(kernel/verification/governance/… 같은 목표 구조)를 목표로 삼지 않는다 — 그 그림대로 옮기는 것은 대수술이며 지켜야 할 것은 '새 코드를 중심 파일에 더 쌓지 않는다' 한 줄이다 ⑤ 분리를 시작하기 전 할 수 있는 무비용 준비는 '서로만 부르고 밖에서 안 부르는 함수 묶음' 목록을 문서 한 장으로 남기는 것뿐이다.
- 왜: 외부 AI 리뷰(ChatGPT 공유 대화, 2026-09-14)가 v0.1.101 코드를 읽고 '논리 구조는 정교하나 물리적 코드 경계가 뒤늦게 비대해진 구조화된 스파게티 — 대수술 금지·절개선이 보일 때만 분리'로 진단했고, 사용자가 대체로 타당하다고 판단해 참고용으로 남기기로 했다. 크기 실측(2026-09-14 HEAD): 계약 라이브러리 bridge/contract-lib.js 6,976줄·이름 있는 함수 421·공개 함수 498(맨 아래 묶음 내보내기 367 + 그 뒤 낱개 내보내기 131)·브릿지 모듈 36개 중 29개가 직접 불러 씀 / 명령줄 진입 bridge/codex-bridge.js 5,421줄 / 확장 본체 src/extension.ts 9,814줄. 직접 실측으로 뒷받침되는 근거: 8월 이후 커밋 270건 중 21건이 '다른 저장소 장부가 섞임·옛 세대 상태 재사용·옛 행이 새 판정에 살아남음' 같은 교차축 결함 봉합이었고, 새 브릿지 파일 하나를 추가하면 배포 목록 3곳과 시험 개수 핀을 같이 맞춰야 하며, 지도 정본은 확장 쪽·브릿지 쪽 사본 동기 체인이 따로 있다. 단 '스파게티'보다 '커널 비대'가 맞는 진단이다 — 누가 어떤 상태를 소유하는지는 코드 안에서 명확하고(결정 장부·검증 봉투·지도·보관함), 분리 단위가 이미 보이므로 분리 자체는 위험하지 않다. 리뷰의 마지막 권고(새 대형 기능보다 다른 장기 프로젝트 하나를 몇 주 실제 운영) 에 동의 — 구조 부채는 그 운영 중 실제로 걸리는 지점에서만 갚는다.
- 결과: 다음 코드 수정 시기가 오면 이 항목을 참고만 한다(자동 실행·의무 아님). 지금 당장의 리팩터링 작업은 없다.
- 정본: bridge/citation-check.js · bridge/curation.js · bridge/map-rotation.js · bridge/rules-flow.js(중심 파일 밖으로 낸 선례) · bridge/contract-lib.js `module.exports`(내보내기 표면) · install.js(배포 목록).
- 찾는말: 리팩터링, 커널 비대, 중심 파일, 모듈 분리, 스파게티, 대수술 금지, 절개선, contract-lib 크기, 유지보수 부채, 외부 리뷰

## D-2026-09-16-scout-layer-deploy — 정찰(3트랙) 실행층은 마켓 설치본에도 실리는 브릿지 모듈이고, 원래 자리에는 위임만 하는 얇은 껍데기를 남긴다
- 날짜: 2026-09-16 · 종류: 확정 · 상태: 유효
- 결정: 마켓에서 확장을 설치한 사람이 정찰(3트랙)을 켜면 "소스 저장소에서 이 스크립트를 실행하라"는 안내를 받는데 그 저장소가 없어 따라 할 수 없었고, GitHub 전체 설치 묶음을 겹쳐 깔아도 다음 마켓 업데이트 때 브릿지만 새 세대로 바뀌어 정찰 스크립트와 어긋날 수 있었다(2026-09-16 사용자 질문·검증 4판). 제외의 뿌리는 설계가 아니라 2026-06-22 패키징 정리 때 scripts 폴더 전체를 개발용으로 뺀 뒤 정찰 실행기가 그 폴더에 추가된 이력이다. 그래서 정찰 실행층 12파일(실행기 3종·공급자·꾸러미 수집·보관·대상 지정·게이트·지도 명령·일지 이관/백필/메모)을 브릿지 배포 모듈로 옮기고, 원래 자리에는 같은 이름 본체를 부르기만 하는 얇은 껍데기를 남겨 저장소 사용자 명령은 그대로 둔다. 꾸러미 수집 본체가 쓰는 컴파일 산출물 3개는 실행 파일과 이름이 겹치지 않게 '-core' 이름의 배포 사본으로 두고 지도 코어와 같은 패리티 검사로 잠근다. 배포 목록 3사본과 설치·제거·전환 목록은 15개를 함께 늘린다.
- 왜: 정찰 층은 외부 패키지 의존이 없고 부모 폴더 상대경로만이 저장소 밖 실행을 막고 있었다(원천 불가 아님). 지도 런타임·의미 보강기가 같은 이유로 먼저 브릿지로 옮겨진 선례가 있다. 검증자가 잡은 세 가지를 범위에 넣었다: 실행 파일과 컴파일 코어의 이름 충돌, 저장소 상대 실행 안내를 내는 파일의 전수 치환(다음 묶음), 코덱스-코덱스 모드의 기본 정찰 담당이 Claude 명령줄을 요구하는 문제(다음 묶음).
- 결과: 묶음 (가)=이관·배포·시험(이 항목), (나)=안내 문구·README 한/영, (다)=모드별 기본 담당·준비 계약. (가) 뒤에는 설치본 평면 폴더만으로 정찰 공급자·보관·꾸러미 수집이 로드된다(tests/scout-deploy.test.js [5]).
- 정본: bridge/scout-providers.js · bridge/scope-package.js `function cliMain` · bridge/scope-package-core.js · scripts/sync-map-core.js `const PAIRS` · install.js `BRIDGE_SCRIPTS` · src/hook-setup.ts `BRIDGE_SCRIPTS` · bridge/map-cutover.js `EXPECTED_DEPLOY_FILES` · tests/scout-deploy.test.js.
- 찾는말: 마켓 설치 정찰 안 됨, 3트랙 마켓, scripts 폴더 없음, 정찰 스크립트 이관, 얇은 래퍼, 코어 사본, 세대 어긋남 zip 재설치

## D-2026-09-16-bridge-home-hints — 사용자에게 보이는 실행 안내는 브릿지 홈 절대 경로 한 곳에서 만든다(소스 저장소 전제 문구 폐기)
- 날짜: 2026-09-16 · 종류: 확정 · 상태: 유효
- 결정: 플랜 게이트·자동 지시·대시보드·명령줄이 "codex-peek 소스 저장소에서 node scripts/… 를 실행하라"고 안내했는데, 마켓에서 설치한 사람은 그 저장소가 없어 따라 할 수 없었다. 정찰 실행층이 브릿지 홈에 배치된 뒤(D-2026-09-16-scout-layer-deploy)에는 안내가 그 실물을 가리켜야 한다. 그래서 실행 안내 문자열은 브릿지(contract-lib bridgeCmd)와 확장(bridgeCmd·웹뷰 BC9) 각 한 곳에서만 만들고, 저장소 전제 문구는 없앤다. 이관된 명령줄 도구의 usage 는 실제로 불린 경로(설치본이든 래퍼든)를 그대로 보여 준다. 개발용 도구(회고·재조정·동기화·이미지)는 저장소 전용이라 바꾸지 않는다.
- 왜: 검증자가 `node scripts/` 문자열만 바꾸면 `node bridge/…` 예시와 '소스 저장소에서' 문구가 남아 여전히 따라 할 수 없다고 잡았고, 개발용 도구까지 바꾸면 존재하지 않는 명령을 안내한다고 잡았다(2026-09-16 4판).
- 결과: 런타임 노출 18파일(브릿지 7·확장 1·이관 도구 10)의 안내가 브릿지 홈 경로로 통일. README 한/영의 설치 절·한계 고지·'수동 설치 존중' 문장을 현 동작(같은 세대면 관리 모드 승격·다음 업데이트부터 자동 동기화)에 맞게 정정.
- 정본: bridge/contract-lib.js `function bridgeCmd` · src/extension.ts `const bridgeCmd` · src/extension.ts `function BC9` · bridge/scout-gate.js · bridge/codex-hook.js · tests/scout-gate.test.js · tests/scout-arm.test.js · tests/cli-bilingual.test.js.
- 찾는말: 소스 저장소에서 실행, node scripts 안내, 브릿지 홈 경로, 플랜 게이트 안내, 마켓 설치 정찰 안내, README 한계 문구

## D-2026-09-16-release-assets-record — 릴리스 자산 구성 절차는 저장소 스크립트로 두고, 작업 폴더 잔재가 컴파일을 깨지 못하게 한다
- 날짜: 2026-09-16 · 종류: 확정 · 상태: 유효
- 결정: v0.1.101 릴리스 자산(설치 zip·vsix·체크섬)을 임시 폴더의 스크립트로 만들면서 작업 폴더 out/pkg 를 남겼고, 그 안의 소스 사본이 다음 컴파일에 잡혀 전체 시험 체인이 첫 단계에서 멈췄다. 절차를 조수의 기억에 두지 않고 저장소에 남긴다: scripts/release-assets.sh(끝나면 out/pkg 삭제·zip 은 bsdtar 로 만들어 리눅스·맥에서도 바르게 풀림)와 tsconfig.json 의 exclude 에 out 을 추가해 잔재가 있어도 컴파일이 깨지지 않게 한다.
- 왜: 사용자 지적 — "기억하지 말고 기록을 남겨야 함. 지금 만드는 것이 그걸 기억하기 위한 하네스 그 자체"(2026-09-16).
- 결과: 다음 릴리스는 release.js 완주 뒤 `bash scripts/release-assets.sh` → gh release 순서로 저장소 안 절차만 따른다.
- 정본: scripts/release-assets.sh · tsconfig.json `"exclude"` · scripts/release.js.
- 찾는말: 릴리스 자산, 설치 zip 만들기, out/pkg, tsc rootDir 오류, Compress-Archive 백슬래시, bsdtar

## D-2026-09-17-claim-abort-release — 명시 abort 로 끝난 지도 변경의 선점 표시는 그 실행기 pid 가 살아 있어 보여도 풀어 준다
- 날짜: 2026-09-17 · 종류: 확정 · 상태: 유효
- 결정: 자동 의미 보강이 지도에 변경을 적용하다가 정본 쓰기가 끝나지 못한 흔적(정본은 859인데 860 임시 파일과 진행 중 장부만 남음 — 형태상 이름 바꾸기 실패로 보이나 반복별 사유는 장부에 남지 않아 확정은 아님)을 남기고 다섯 번 곧바로 되풀이한 끝에 '재시도 횟수를 다 썼어요'로 멈췄다. 복구 명령이 그 진행 중 장부를 '기준이 이미 바뀌어 폐기 권고'로 판정해 폐기(abort)했더니, 그 변경의 선점 표시(어느 실행기가 잡고 있는지)는 그대로 남았고, 그 프로세스 번호가 지금은 다른 프로그램(tsserver)에 붙어 있어 — 번호 재사용 가능성 — '살아 있는 남의 선점'으로 보여 다시 시도해도 곧바로 같은 보류로 돌아갈 상태였다(2026-09-17 사용자 질문·검증자 지적). 그래서 적용기가 선점 표시를 볼 때, 그 선점의 장부가 폐기 폴더(wal-aborted)에 있으면 프로세스 생존과 무관하게 끝난 것으로 보고 다시 잡게 한다(내구 산출물 부재 검사는 그대로 선행).
- 왜: 프로세스 번호 생존만으로 '진행 중'을 판정하면 번호 재사용에 속는다. 폐기 폴더의 존재는 '그 트랜잭션은 사람이 끝냈다'는 명시 증거라 더 강하다.
- 결과: 폐기 뒤 '다시 시도'가 같은 변경을 다시 잡아 적용한다(tests/map-pipeline.test.js [claim-abort]). 남은 과제(사용자 판단): 일시 실패 재시도에 대기가 없어 짧은 파일 잠금 창에도 상한이 즉시 소진되는 점, 소스 편집 중 보강이 한 번을 소진하는 점, 실패 반복별 사유가 장부에 남지 않는 점.
- 정본: bridge/map-pipeline.js `const abortedWal` · tests/map-pipeline.test.js `[claim-abort]` · bridge/map-runtime.js `cmd === "recover"`·`cmd === "abort"`.
- 찾는말: 자동 보강 보류, 재시도 횟수 소진, retry-exhausted, claim-busy, PID 재사용, wal-aborted, abort 뒤 재시도 안 됨, topology tmp 잔재

## D-2026-09-17-scout-arm-readiness — 정찰 담당의 기본값은 모드를 따르고, 담당의 명령줄이 없으면 '준비 안 됨'으로 닫고 실행을 요구하지 않는다
- 날짜: 2026-09-17 · 종류: 확정 · 상태: 유효
- 결정: 코덱스-코덱스 모드로 마켓에서 설치한 사람은 Claude 명령줄이 없을 수 있는데, 정찰 담당의 기본값이 늘 '기본 정찰(Claude)'이라 플랜 게이트가 실행할 수 없는 명령으로 막고 자동 지시가 같은 명령을 요구했다(2026-09-16 검증자 반례). 그래서 ① 담당이 미지정이면 모드 기본을 쓴다 — 클로드-코덱스는 Claude 정찰, 코덱스-코덱스는 Codex 정찰(명시 선택은 그대로, 키 없는 DeepSeek 은 모드 기본으로 강등) ② 정찰 전용 준비 점검을 둔다 — 담당의 명령줄이 PATH 에 실제로 있고 실행 가능한지(또는 DeepSeek 키)만 보고 실행 파일을 띄우지 않는다(의미 보강 준비 점검과는 별개 축) ③ 미준비면 실행기는 호출을 시작하지 않고 '준비 실패'로 닫고, 두 플랜 게이트는 차단 직전에 판정해 지도 요구 없이 통과하며(세션 상한 미소모·신선한 지도의 정상 통과엔 잡음 없음), 자동 지시는 실행 명령 대신 사유와 해결책을 말한다 ④ 대시보드 담당 선택 줄에 '준비 안 됨'과 사유를 표시하고 모드 기본을 표기한다.
- 왜: 따라 할 수 없는 지시로 막는 것은 안내가 아니라 덫이다(fail-open 원칙). 준비 점검을 spawn 없이 두어야 매 턴 불러도 비용이 없다. 절대 경로로 지정한 실행 파일도 '있다'만으로는 부족해 실행 가능까지 본다.
- 결과: 마켓 설치본만 가진 코덱스-코덱스 사용자도 3트랙이 돈다(Codex 정찰). Claude 명령줄이 없는 환경에서 플랜 게이트는 통과하고 대시보드가 사유를 보여 준다. 정리 담당은 탐색 담당의 유효 팔을 따르므로(D-2026-09-06-curator-arm-follows-scout) 코덱스-코덱스에서 담당 미지정이면 정리 담당도 Codex 가 된다 — 그 항목의 '미설정=Claude' 예시는 이 결정으로 모드 기본을 따르는 것으로 개정.
- 정본: bridge/contract-lib.js `function defaultScoutArmFor` · `function cliOnPath` · `function scoutArmReadiness` · bridge/scout-providers.js `preflight` · bridge/scout-gate.js `passIfScoutNotReady` · bridge/codex-hook.js `scoutGate` · src/extension.ts `function scoutArmViewExt` · tests/scout-arm.test.js `[4c]` · tests/scout-gate.test.js · tests/p3b-stage1.test.js.
- 찾는말: 정찰 담당 기본값, 코덱스-코덱스 정찰, claude 명령줄 없음, 정찰 준비 안 됨, 플랜 게이트 통과, cli-not-found, scoutArmReadiness

## D-2026-09-18-enrich-followup — 자동 보강이 멈출 때는 '왜'를 구조로 남기고, 편집 중 충돌은 담당 실패로 세지 않으며, 같은 잠금을 연달아 두드리지 않는다
- 날짜: 2026-09-18 · 종류: 확정 · 상태: 유효
- 결정: (1) 인용 불일치로 답을 버릴 때 어떤 정규화(줄 끝·줄 앞뒤 공백·공백 전체)면 맞았을지, 발췌 안인지, 답을 기다리는 사이 파일이 바뀌었는지를 시도 기록(failureDetail.kind=evidence-mismatch)에 남긴다. 대조 규칙(원문 그대로 포함)은 바꾸지 않는다. (2) 파일이 바뀐 경우는 담당 실패(precision-failed 등)로 세지 않고 '소스 변경(source-changed)'으로 세워 두며, 다음 실행이 사람 없이 재개한다(자동재시도 1회 예산·승격 미소모). (3) 잠금·정본 쓰기의 일시 실패 재시도는 250ms 부터 두 배씩(4초 상한) 쉬고 다시 두드린다. (4) 정본 임시 파일(<이름>.<pid>.tmp)은 복구 때 죽은 pid 의 것만 지우고, 산 pid 는 30분 나이 상한까지 둔다. (5) 대시보드는 인용 불일치의 세 갈래를 따로 말한다.
- 왜: 2026-09-17 실사고 — 두 시도가 '인용 불일치'로만 남아 원인을 알 수 없었고(진단 호출에서는 30/30 일치 → 간헐), 첫 시도는 구현자가 같은 파일을 고치는 중이라 어긋난 것이 담당 실패로 기록돼 자동재시도 예산을 썼으며, 잠금 실패를 같은 초에 5회 되풀이해 곧장 '재시도 소진'으로 멈췄고, 임시 파일 topology.json.17696.tmp 가 남았다. 정규화 대조로 관문을 느슨하게 하면 근거 위조 차단(ab-3)이 약해지므로 관문은 두고 '실패 뒤 진단'만 더한다. 민감 경로는 존재 여부 오라클이 되므로 진단을 남기지 않는다(ab-7).
- 결과: 다음에 같은 멈춤이 오면 시도 기록 한 줄로 갈래가 보인다. 편집 중 충돌은 사용자 개입 없이 다음 실행에서 풀린다. 잠금 경합으로 인한 즉시 소진이 사라진다. 임시 파일 잔재가 복구 시 정리된다.
- 정본: bridge/map-enrich.js `function evidenceMismatchDetail` · `function validFailureDetail` · `function retryPauseMs` · driveAttempts `_sourceChanged` 분기 · 자기치유 `source-changed` 블록 · bridge/map-pipeline.js `function sweepStaleTmp` · src/extension.ts `failureText` evidence-mismatch 분기 · PARK_REASONS `source-changed` · tests/p8-enrich-run.test.js [7c][7d] · tests/map-pipeline.test.js [tmp-sweep]
- 찾는말: 자동 보강 보류, 인용 불일치, evidence-mismatch, source-changed, 편집 중 충돌, 재시도 소진, retry-exhausted, 재시도 간격, 임시 파일 청소, sweepStaleTmp, failureDetail

## D-2026-09-18-enrich-item-gate — 자동 보강의 첫 관문은 항목 단위로 거르고, 담당에게 되돌려 달라던 대조값(expect)은 실행기가 호출 스냅샷에서 채운다
- 날짜: 2026-09-18 · 종류: 확정(사용자 결정) · 상태: 유효
- 결정: (1) 답이 도착하면 형식·대상 실존·근거 인용 실증을 항목마다 따로 판정해 결함 항목만 사유(색인·단계 shape/id/evidence/cap·진단)와 함께 제외(attempt.droppedItems)하고 정상 항목만 적용 단계로 넘긴다. 전부 결함일 때만 종전처럼 시도 실패(담당 실패)로 기록한다. 변환 단계(패치 형태·변환 시점 재실증)의 결함도 같은 규칙으로 그 항목만 제외(stage convert)하고, 변환에서 전부 제외돼 적용이 0이면 시도 실패로 기록한다. 근거 위조 차단(ab-3)은 항목마다 그대로다. 장부 재판독·시험이 쓰는 strict 검증(validateEnrichResult 기본)은 바꾸지 않고 perItem 옵션으로 분기한다. (2) set_state·rewrite_label 의 expect(현재 값)는 담당의 메아리를 버리고 실행기가 프롬프트 조립 시점 topology(호출 스냅샷)에서 채운다. 적용 시점 CAS 는 그대로 스냅샷 대 현재를 비교한다. (3) 대시보드는 '답 중 결함 항목 N건은 사유와 함께 버리고 나머지만 썼어요'를 보인다.
- 왜: 2026-09-17~18 실사고 — 파이프라인 정밀 호출 5회 중 4회가 항목 1개의 결함(인용 한 글자 차이·근거 누락)으로 답 전체가 버려져 '보류'가 세대마다 반복됐고 세대마다 자동 2회 과금이 이어졌다. 또 프롬프트가 라벨을 120자로 잘라 보이므로(현 지도 61 노드 중 22) 라벨 수정 제안의 expect 는 맞을 수 없는 값이라 그 항목이 늘 조사 대기로 빠졌다. expect 는 근거가 아니라 대조값이므로 담당이 아니라 실행기가 채우는 것이 맞다.
- 결과: 결함 1개가 정상 9개를 죽이지 않는다. 사고 유형별 사유가 항목 단위로 장부에 남아 다음 사고는 추정이 아니라 확인이 된다. 라벨 수정 제안이 절단 때문에 조사 대기로 빠지는 일이 사라진다.
- 정본: bridge/map-enrich.js `validateEnrichResult(obj, topo, ctx, { perItem })` · `fillExpectFromSnapshot` · runAttempt 1단계 관문 블록 · `dropItemAtConvert`(변환 단계 제외 — 교체 표식 보상·원래 색인) · attempt `sourceIdx`(압축 배열→원래 응답 색인) · `DROP_STAGES` · validateJob droppedItems 합타입 · `enrichOutcomeSummary.dropped` · src/extension.ts enrich.dropped 문구 · bridge/enrich-providers.js 프롬프트 expect 안내 · tests/p8-enrich-run.test.js [8][8b][8c][8d]
- 찾는말: 자동 보강 보류 반복, 항목 단위 관문, droppedItems, perItem, expect 자체 채움, fillExpectFromSnapshot, 라벨 절단, 전체 거부, 부분 채택

## D-2026-09-19-bridge-stale-banner — 창이 옛 브릿지 판으로 읽고 있으면 화면이 스스로 그 사실과 조치(창 다시 로드)를 말하고, 그동안은 '손상'이라 단정하지 않는다
- 날짜: 2026-09-19 · 종류: 확정(사용자 결정 — 범위 ①만) · 상태: 유효
- 결정: (1) 확장 호스트가 require 로 캐시한 브릿지 모듈(브릿지 홈 바로 아래 .js)마다 처음 관측한 파일 지문(mtime+size)을 기억하고 매 렌더마다 디스크와 비교해, 달라지면 bridgeStale 로 표시한다(src/bridge-stale.ts `observeBridgeStale`, 특정 파일·특정 장부 키에 매이지 않는 일반 규칙). (2) stale 이면 대시보드 첫머리에 배너와 [창 다시 로드] 버튼을 두고 같은 변경 묶음에 대해 경고 알림을 1회 띄운다(Codex 훅 재로드 안내 선례와 같은 방식). (3) stale 동안 브릿지 판독이 'damaged' 로 나오면 '이 창의 판독기가 이전 판이라 읽지 못함 — 재로드 뒤 다시 확인'으로만 말하고, 재로드 뒤에도 못 읽을 때 '손상 여부 확인 필요'로 말한다. (4) '자동 실행 정지' 문구는 폐기한다 — damaged 는 실행기 spawn 을 막지 않으며 실행기는 자기 판독으로 계속 시도한다.
- 왜: 2026-09-19 실사고 — install.js 로 브릿지를 갱신한 뒤 창을 다시 로드하지 않은 창이 옛 판독기로 새 형식 장부(sourceIdx)를 읽어 '작업 기록 손상 — 자동 실행 정지'를 보였고, 사용자는 원인도 조치도 알 수 없어 구현자에게 물어야 했다. 배포 단계(deployBridgeRuntime)는 stamp version·드리프트를 검사하지만 이미 떠 있는 창의 캐시는 아무도 보지 않았다. 손상 상태 자체에 사용자 행동(복구 버튼)을 두는 것(범위 ②)은 원본 기록 삭제가 얽혀 별도 결정으로 미룬다.
- 결과: 브릿지 갱신 뒤 사용자는 화면에서 바로 원인과 버튼을 보고 스스로 조치한다. 형식 변경이 또 있어도 같은 안내가 자동으로 나온다. 한계: 로드와 첫 관측 사이에 파일이 바뀐 한 번은 놓칠 수 있다(창이 뜬 직후·매 렌더 관측으로 창은 좁다).
- 정본: src/bridge-stale.ts `observeBridgeStale` · src/extension.ts `bridgeStaleView`·`promptBridgeReload`·BridgeState.bridgeStale·웹뷰 `bridgeStaleBanner`·메시지 `reloadWindow`·자동 보강 상태 문구(damaged 두 갈래) · tests/bridge-stale.test.js
- 찾는말: 브릿지 갱신, 창 다시 로드, 옛 판독기, 작업 기록 손상, 자동 실행 정지, bridgeStale, require 캐시, 대시보드 배너

## D-2026-09-20-enrich-ledger-quarantine — 자동 보강의 진행 장부가 깨지면 실행기가 스스로 치우고(보존) 알린 뒤 새로 시작한다 — 사용자 복구 버튼을 두지 않는다
- 날짜: 2026-09-20 · 종류: 확정(사용자 결정) · 상태: 유효
- 결정: (00) 동의·확인 대기 장부 잠금도 작업 장부 잠금과 같은 절차로 죽은 보유자(ESRCH 확정)의 잔존 잠금과 '형식 불명' 잔존 잠금(빈 파일·조각 — 잠금 생성과 토큰 기록 사이 강제 종료의 실물, 단 생성 창 LOCK_WRITE_SETTLE_MS 를 지난 것만·창 안은 쓰는 중으로 보고 회수하지 않음)을 회수한다(run-lock 아래 격리 개명·보존 — 강제 종료 뒤 격리·동의 켜기/끄기·확인 대기 기록이 영구 잠금 실패로 멈추지 않도록). 그 선행 조건인 실행 잠금(run-lock) 자체의 빈 파일·조각 JSON 잔존도 같은 규칙(창 안=쓰는 중으로 물러남·창을 지난 것만 격리 개명 회수·옮겨 온 원문 동일성 재검증)으로 회수해 자기치유 전체가 영구 정지하지 않는다. (0) 재개(resumeJob)도 동결 주체로 동의를 재대조해 동의가 없으면(격리로 빈 장부·철회) 저장된 결과라도 반영하지 않고 consent-stale 로 세운다 — 동의 장부 격리 시 새 장부의 세대 번호를 작업이 본 세대 이상으로 심어(단조 불변식 유지) 재동의가 새 세대로 인식돼 사람 없이 자동 재개된다. (1) 작업 기록·동의 기록·확인 대기 기록은 실행 시작(복구 경로보다 앞)에서, 동의 기록은 동의 판독·기록(켜기/끄기)에서 손상이 발견되면 실행기가 `<파일>.broken-<시각>`으로 이름을 바꿔 보존하고(삭제 금지·ab-5) 무결성 채널에 1건(enrich-quarantined) 남긴 뒤 새 장부로 진행한다(`quarantineLedger`). 동의 기록은 새 동의가 필요하므로 무동의 정지로 이어지고 화면의 기존 '자동 보강 켜기'가 그 경로다. 확인 대기 기록을 치우면 그 장부만 알던 '검증자 확인 대기' pending 은 아무도 다시 찾지 못하므로(스윕 불침·시간 경과로도 정리 안 됨) 격리 직후 이 지도의 pending 중 '현재 작업 기록에서 결정론 patchId(detPatchId)로 소유가 증명되고' lifecycle proposed|classified · classification verifier-resolved|auto(강등 에스컬레이션) · 작업 커서가 이미 지나간 항목(index < cursor.nextIndex — 커서가 아직 그 항목에 있으면 재개 실행이 이어 처리하므로 제외)인 것만 즉시 정리(expired·deferred-quarantined)하고 건수를 알림에 적는다 — 연산명·분류만으로 추정하지 않아 수동·정책 위임 제안은 보존되고, 사용자 결정 대기 항목(needs-investigation·intent-choice)은 건드리지 않으며, 작업 기록이 없으면 정리하지 않는다(보수) — 다음 실행에서 다시 제안될 수 있다(레코드 재구성은 하지 않음). 격리는 종류별 잠금(작업=job lock·동의=consent lock·확인 대기=deferred lock) 안에서 다시 읽어 여전히 깨져 있을 때만 수행한다 — 다른 창이 그 사이 저장한 정상 장부를 치우지 않는다(ab-2). (2) 확장의 실행기 spawn 게이트는 동의 기록을 정상으로 읽었고 이 창이 옛 브릿지 판(bridgeStale)이 아닐 때만 동의를 검사하며, 판독 불가·stale 이면 판단을 실행기(설치본 최신 판)에게 넘긴다. (3) 경보 배너·상태바는 자동 보강 알림(enrich-parked·enrich-quarantined)을 '근거 의심'과 갈라 '자동 보강 알림'으로 센다.
- 왜: 사용자 지적(2026-09-20) — 하네스는 보류 사유별 자기치유·지도 저널 자동 복구·임시 파일 청소·수칙서 제안→승인→적용까지 사람 없이 굴러가는데, 진행 장부 손상만 '손상·수동 복구 필요'로 방치하고 사용자에게 버튼을 요구하려 한 것은 자동화 철학에 어긋난다. 진행 장부는 정본(지도)이 아니므로 안내형 확인 없이 치워도 된다(보존이 곧 되돌림 수단). 옛 판독기 오판은 실행기 쪽엔 없다(실행기=설치본 최신 판) — 그 문제는 확장 게이트가 캐시 판독으로 실행기 시작을 막는 경로이므로 게이트를 넘기게 했다.
- 결과: 장부가 깨져도 다음 실행에서 스스로 정리되고 알림이 남는다. 사용자가 손댈 것은 동의 재설정뿐(버튼 기존). 창을 다시 로드하기 전에도 실행기는 시작될 수 있다.
- 정본: bridge/map-enrich.js `quarantineLedger`·`notifyEnrichQuarantined`·runEnrichLocked 시작 자기치유·동의 판독/grant/revoke 자기치유 · src/extension.ts maybeSpawnEnrichExt 게이트·배너/상태바 nEnrich · tests/p8-enrich-run.test.js [10] · tests/bridge-stale.test.js [4]
- 찾는말: 장부 손상, job-damaged, consent-damaged, deferred-damaged, 격리, quarantine, broken, 자기치유, 자동 보강 알림, spawn 게이트

## D-2026-09-20-enrich-next-axes — 자동 보강 남은 세 축의 방향(발췌·호출 표시·되묻기) — 사용자 확정
- 날짜: 2026-09-20 · 종류: 확정(사용자 결정 · 계획 docs/ENRICH-NEXT-AXES-PLAN-2026-09-20.md v1.4 §0) · 상태: 유효
- 결정: D1 담당 답의 인용문은 '우리가 보낸 발췌 안'에 있어야만 통과(파일 전체 대조 폐기 — 발췌 밖 인용은 그 항목만 제외) · D2 발췌는 파일 첫머리가 아니라 이번에 바뀐 부분 주변(git 없거나 새 파일이면 첫머리 폴백+제목에 표기) · D3 발췌 양은 프로젝트별 옵션(프리셋 가벼움 8·권장 10·대형 15·최대 20파일, 기본값 권장 10 — 수치는 실사용 카드 측정을 보고 사용자가 조정, 벤치 아님) · D4 정밀 담당 되묻기는 새 호출 없이 기존 재호출(자동 재시도·기본형 재개·사용자 재시도 모두)에 지난 답이 버려진 이유를 기계 분류로만 동봉(D4-2 사용자 재시도에도 동봉) · D5 호출 횟수는 카드 한 줄(요금·절대 상한 표시 안 함) · D6 순서=관찰(호출 표시+인용 측정)→읽기 강제→옵션→되묻기.
- 왜: 사용자 방향(2026-09-20) — '보낸 부분을 읽었다'는 보증이 없던 발췌 검사, 변경과 무관한 파일 머리 발췌, 호출 수·재시도 이력이 화면에 없던 점을 사용자가 정한 철학(읽게 만드는 장치·프로젝트별 옵션·추가 호출 없는 되묻기)으로 해소한다. 전제: 프로젝트별 분리·한/영 쌍·사용자=방향, 구현자=세부.
- 결과: 묶음 1(관찰)부터 순서대로 구현한다. 프리셋 수치는 임시 출발값이며 옵션 칸에서 언제든 편집된다.
- 정본: docs/ENRICH-NEXT-AXES-PLAN-2026-09-20.md §0·§2~§4 · 후속 결정 D-2026-09-20-enrich-calls-observe(묶음 1)
- 찾는말: 발췌, 인용 결속, 바뀐 부분 주변, 발췌 옵션, 프리셋, 호출 횟수, 되묻기, priorRejection, 사용자 결정 D1~D6

## D-2026-09-20-enrich-calls-observe — 호출 횟수·재시도 이력·인용 측정은 작업 장부만 재료로 '관찰'만 한다(묶음 1)
- 날짜: 2026-09-20 · 종류: 구현(D-2026-09-20-enrich-next-axes 의 묶음 1) · 상태: 유효
- 결정: (1) 재개 사건 `job.resumes[{kind, at, fromAttempts}]`(kind 닫힌 열거 input-doc-only|not-ready|source-changed|auto-retry|manual)를 retryFrom 을 옮기는 다섯 경로가 같은 잠금 갱신 안에서 덧붙인다(공통 헬퍼 reopenForRetry — 확장의 수동 재시도 포함). 선택 필드·추가만·strict 검사(kind·ISO·fromAttempts 단조·상한). (2) 통과 항목의 인용마다 '실제로 보낸 발췌 안인가'를 세어 `attempt.citation{total,inExcerpt,outside,filesCited,filesSent}` 로 남긴다(정규화 quoteMatchAfter 단일 정본 — 불일치 진단과 공유). 검사·거부는 바꾸지 않는다(관찰 전용). (3) 화면 요약은 순수 모듈 bridge/enrich-calls.js 가 작업 장부만 읽어 계산한다: 답 받은 호출(applying/done 또는 실패 단계 response|validation|conversion)·호출 실패(call)·불확실(running 잔존·parked/uncertain-call)·단계 미상(옛 기록)·자동 재시도 남음/사용됨/없었음/해당 없음/unknown(옛 기록)·사용자 재시도·편집 중 충돌 재개·자동 재개. 비용 장부·경로 로그·실행 사건은 best-effort 라 재료로 쓰지 않는다. 요금·절대 상한은 적지 않고, 모드 안내 패널의 '사람 없이 이어지는 호출' 문구는 상수(UNATTENDED_CALL_RULE)에서 만든다.
- 왜: 계획 검토(2026-09-20, 5판)에서 확정된 사실 — 비용 장부는 프로세스 기동만 기록해 요금 권위가 아니고, retryFrom 은 다섯 경로가 같은 형태로 옮겨 원인을 구별할 수 없으며, 2·2·4 는 소스 불변 구간에서만 성립한다(편집 중 충돌 자기치유가 다시 부른다). 사용자 결정 D5·D6.
- 결과: 현황 카드에 '이번 작업: 답 받은 호출 n회(담당별) · 호출 실패 · 자동 재시도 상태 · 사용자 재시도 · 편집 중 충돌로 다시'와 '마지막 답: 인용 n건 중 보낸 발췌 안 m건 · 인용 파일 a / 보낸 파일 b' 두 문구가 보인다. 옛 기록은 '재시도 이력 없음(이전 판 기록)'으로 0과 구분한다. 이 수치가 D3 프리셋과 D1 전환의 기준선이다.
- 정본: bridge/map-enrich.js RESUME_KINDS·CITATION_KEYS·reopenForRetry·quoteMatchAfter·citationSummaryFor·validateJob(resumes·citation) · bridge/enrich-calls.js · src/extension.ts retryEnrichFromUi·computeState job.calls·웹뷰 상태 줄·openMapModeGuide · 배포 목록 3사본(install.js·hook-setup.ts·map-cutover.js) · tests/enrich-calls.test.js · tests/p8-enrich-run.test.js [10b]
- 찾는말: 호출 횟수, 재시도 이력, resumes, reopenForRetry, citation, 인용 측정, enrich-calls, 답 받은 호출, 불확실 호출, 자동 재시도 남음

## D-2026-09-20-enrich-excerpt-binding — 자동 보강 발췌는 '바뀐 부분 주변'을 보내고, 근거 인용은 '보낸 발췌 안'의 원문만 인정한다(묶음 2 · D1·D2)
- 날짜: 2026-09-20 · 종류: 구현(D-2026-09-20-enrich-next-axes 의 묶음 2) · 상태: 유효
- 결정: (1) 발췌 판독 `excerptBodyFor(repo, f, {baseRef})`는 git 기준 큐면 소화 기준점 커밋(없으면 실행 시작에 한 번 캡처한 HEAD 커밋 id — 별칭 "HEAD" 금지)~작업 트리의 `git diff -U0` hunk 줄 범위에 앞뒤 40줄 창을 붙여 병합·글자 상한(파일당 4,000자) 안에서 싣고, 제목에 위치를 적는다(`### 파일 (변경 주변 L110–192)`). git 없음·새 파일·hunk 없음·git 실패(5초 제한)는 종전 첫머리로 폴백하되 제목에 `(앞부분)` 표기 — 폴백은 침묵하지 않는다. 관문(answerableInput)·호출 직전 스냅샷(preBody/preLabel)·프롬프트(어댑터 입력 excerptBodies/excerptLabels)·인용 결속·인용 측정이 같은 판독을 쓴다. (2) 인용 결속: 모든 항목의 evidence·claims 인용은 파일 실존 검사(종전) 뒤에 '보낸 파일 ∧ 보낸 발췌 본문 안'(줄 구조 보존 단계 exact·crlf·trim 만 — 공백 접기 가짜 일치 배제)이어야 통과. 아니면 그 항목만 `evidence-outside-excerpt`(detail evidence-outside{sent, excerpt 위치 표기, quoteHead})로 제외, 전부 제외면 종전대로 시도 실패(자동 재시도 대상 유지). 출력 계약 문구에 이 규칙을 고지(기계가 강제하는 규칙을 답하는 쪽이 알게 — 어휘 드리프트 실사고 관례). (3) 기준점은 항상 '한 번 해석한 커밋 id'다(확인 검증 2판 blocker): 소화 기준점이 없을 때 별칭 "HEAD" 를 넘기면 git 호출마다 다시 풀려, 답을 기다리는 사이(또는 결과 영속 뒤 죽어 있는 사이) 사용자가 그 변경을 커밋하면 응답 뒤 add_node 앵커 재검사·변환 재검사가 새 HEAD(=변경 없음)를 봐 첫머리 폴백→빈 본문으로 정상 제안을 제외한다. 실행 시작에 캡처한 HEAD(`st.srcHead` — 입력 계산·소화 기준점 전진과 같은 판독)를 우선 쓰고 없을 때만 rev-parse 1회, 해석 실패=null(첫머리). 발송 시점 값은 시도에 `excerptBase`(커밋 id|null · 옛 기록 부재 허용 · strict)로 영속되어 변환 재검사(재개 포함)가 그 값을 쓴다 — 관문·스냅샷·프롬프트·재검사·변환이 같은 커밋을 본다.
- 왜: 사용자 결정(2026-09-20) — 종전 검사는 파일 전체 대조라 '보낸 부분을 읽었다'는 보증이 없었고(담당은 발췌 밖을 볼 통로가 없어 그런 인용은 우연 일치거나 지어낸 것), 발췌는 변경과 무관한 파일 머리(주석·import 28%)였다. '무엇을 보내느냐'가 곧 '무엇을 읽게 하느냐'가 되게 한다.
- 결과: 담당은 이번 변경 주변만 받고 그 안에서만 인용해야 한다. 카드의 제외 사유는 '보낸 범위 밖(범위 표기)'과 '보내지 않은 파일'을 갈라 보인다(ko/en). 묶음 1 인용 측정은 통과 항목 기준이므로 inExcerpt=total 이 정상이 되고, 제외 건수는 droppedItems 에 남는다.
- 정본: bridge/enrich-providers.js parseHunkRanges·changedRangesFor·windowedBody·EXCERPT_PAD_LINES·excerptBodyFor(opts)·buildEnrichPrompt(제목 표기·계약 고지) · bridge/map-enrich.js excerptBaseRefFor(opts.head·별칭 없음)·answerableInput(opts)·runAttempt(baseRef9·excerptBase 영속·preLabel·결속 관문)·convertItem(a.excerptBase)·ATTEMPT_KEYS excerptBase·quoteInExcerpt·FAILURE_CODES evidence-outside-excerpt·validFailureDetail evidence-outside · src/extension.ts failureText/FAIL_TEXT·lastFailure 투영 · tests/enrich-excerpt.test.js · tests/p8-enrich-run.test.js [10b]
- 찾는말: 발췌, 바뀐 부분 주변, hunk, 창, 첫머리 폴백, 인용 결속, evidence-outside-excerpt, 보낸 발췌, 위치 표기, quoteInExcerpt, 기준점 별칭, excerptBase, 호출 중 커밋

## D-2026-09-20-enrich-excerpt-options — 자동 보강이 담당에게 보내는 발췌 범위(파일 수·파일당 글자)는 프로젝트별 옵션이고 기본값은 권장 10파일·3,000자다(묶음 3 · D3)
- 날짜: 2026-09-20 · 종류: 구현(D-2026-09-20-enrich-next-axes 의 묶음 3) · 상태: 유효
- 결정: (1) 옵션의 정본은 프로젝트(워크스페이스)별 계약 파일의 `mapExcerpt: { files, charsPerFile }` 한 곳(`contracts/<ws키>.json`, 현재 언어 슬롯) — 저장은 기존 단일 관문(`patchContractRetryExt`)의 exact patch 로 이 키만 쓰고 다른 필드는 보존한다(ab-2). 값의 정규화·프리셋·상한은 새 모듈 `bridge/enrich-excerpt-cfg.js` 한 곳(화면·실행기·프롬프트가 같은 상수 — 복제 드리프트 금지). (2) 프리셋 4종=가벼움 8/2,000 · 권장 10/3,000 · 대형 15/4,000 · 최대 20/4,000(종전 상한). **기본값(저장값 없음)=권장 10/3,000**(사용자 결정 2026-09-20 — 수치는 벤치가 아니라 현황 카드의 인용 측정을 보고 사용자가 조정). 하드 상한은 종전 그대로 20파일·4,000자(옵션은 그 안에서만 줄인다). (3) 저장 관문은 상한 밖·정수 아님을 사유와 함께 거부한다(저장 안 함 — 검증 왕복 상한과 같은 관례) · 판독은 이형·상한 밖 값을 미지정(기본값)으로 본다(비슷한 값으로 고쳐 쓰지 않음). 실효 뷰는 mapMode 와 동형: 현재 슬롯 명시값 → 반대 언어 슬롯 상속 → 기본값. (4) 실행기: 새 작업은 호출자(ws·slot)의 계약, 재개 작업은 작업에 동결된 주체(configWs·slot)의 계약(ab-1)을 `excerptCfgFor` 한 곳에서 읽어 관문 4곳·호출 직전 스냅샷·어댑터 입력·응답 검증(발췌 파일 집합·add_node 앵커 재검사)이 같은 값을 쓰고, 시도에 `excerptCfg`(ATTEMPT_KEYS·strict 선택 필드·닫힌 모양)로 영속해 변환 재검사(재개 포함)가 발송 시점 값을 본다. cfg 없이 부르는 판독(옛 시도 기록의 재검사·직접 호출)은 옵션 도입 전 상수(20/4,000)로 — 옛 기록이 실제로 받은 발췌보다 좁게 재검사돼 정상 항목이 제외되는 일이 없게. (5) 프롬프트: 옵션 상한에 밀려 안 실린 후보가 있으면 자료 줄로 고지한다(침묵 상한 금지 · 지시문 아님). 파일당 글자 절단은 종전대로 제목의 '(일부)'. (6) 화면: 검증 설정 탭 의미 보강 담당 줄 아래 "발췌 범위" 줄(파일 수·파일당 글자 입력 + 저장 + '권장 기본값으로'(권장값을 이 슬롯에 명시 저장 — 키 삭제면 반대 언어 슬롯 값이 상속돼 버튼의 약속과 실효값이 갈린다) + 프리셋 버튼=자동 입력 · 실효값과 출처 표시 · 미저장 입력은 15초 상태 갱신에도 웹뷰 초안으로 유지되고 저장이 확인될 때만 비워짐 · 저장 슬롯=표시 데이터의 슬롯(언어 전환 보류 중 다른 슬롯을 덮지 않음)) ko/en 쌍 · 모드 안내 패널과 README ko/en 에 한 문단(상수에서 문장 생성).
- 왜: 사용자 결정 D3(2026-09-20) — 발췌 양은 프로젝트마다 다르게 두되 옵션은 보조이고 D1·D2(읽기 강제)가 본체. 기본값을 권장으로 두는 이유는 종전 상한(20파일·4,000자)이 "무엇을 보내느냐=무엇을 읽게 하느냐" 아래서 너무 넓고, 수치의 근거는 벤치가 아니라 실사용 측정이라 사용자가 조정할 여지를 남긴다.
- 결과: 프로젝트마다 담당에게 보내는 양이 달라지고 그 값이 시도 기록에 남아 감사·재개에서 같은 값을 본다. 옵션 상한에 밀린 파일은 보내지 않았으므로 그 인용은 D1 규칙대로 제외된다(sent:false).
- 정본: bridge/enrich-excerpt-cfg.js(상수·validExcerptCfg·normMapExcerpt·parseExcerptInput·presetKeyFor·mapExcerptView·effectiveExcerptCfg·cfgOrLegacy·excerptCfgText) · bridge/enrich-providers.js excerptSelectionFor(cfg)·excerptFilesFor(cfg)·excerptBodyFor(opts.charsMax)·buildEnrichPrompt(ctx.excerptCfg·자료 줄) · bridge/map-enrich.js excerptCfgFor·answerableInput(opts.cfg)·runAttempt(cfg9·excerptCfg 영속)·validateEnrichResult(ctx.excerptCfg)·toPatchV2(ctx.excerptCfg)·convertItem(a.excerptCfg)·ATTEMPT_KEYS·validateJob · src/extension.ts setMapExcerptFromUi·setMapExcerpt 핸들러·computeState mapExcerpt·웹뷰 발췌 범위 줄·openMapModeGuide 문단 · README.md·docs/README.en.md · tests/enrich-excerpt-cfg.test.js · 배포 목록 3사본(install.js·src/hook-setup.ts·bridge/map-cutover.js) 53파일
- 찾는말: 발췌 범위, mapExcerpt, 프리셋, 파일 수, 파일당 글자, 기본값 권장 10, excerptCfg, enrich-excerpt-cfg, 옵션, 동결 주체, cfgOrLegacy

## D-2026-09-20-enrich-prior-rejection — 다시 묻는 호출에는 지난 답에서 제외된 항목의 '기계 분류'만 붙인다(묶음 4 · D4 B · D4-2 ㄱ)
- 날짜: 2026-09-20 · 종류: 구현(D-2026-09-20-enrich-next-axes 의 묶음 4) · 상태: 유효
- 결정: (1) 새 호출을 만들지 않는다 — 이미 있던 재호출(유료 담당 자동 재시도 1회·기본형 재개 경로 재호출·사용자 '다시 시도')의 프롬프트에 자료 절 하나를 붙일 뿐이다(호출 횟수·재시도 횟수 불변). (2) 동봉 판정은 경로가 아니라 호출 직전 작업 장부 사실로: 새 시도를 만드는 같은 잠금 안에서 `attempts` 의 마지막 원소(=바로 앞 시도)가 `phase failed` ∧ `failureStage ∈ {response, validation, conversion}` 이면 `priorRejectionFor`(bridge/enrich-calls.js) 요약을 어댑터 입력 `ctx.priorRejection` 으로 싣고 시도에 `priorAttached: true`(ATTEMPT_KEYS·strict 선택·true 만)를 남긴다. `retryFrom` 은 보지 않는다(라우팅 경계 전용). 사용자 재시도도 같은 자료(D4-2 ㄱ). (3) 요약의 내용은 닫힌 어휘뿐: 직전 시도 번호·담당·단계·코드 + 제외 항목별 {응답 색인, op, 제외 단계, 종류(evidence=detail.kind·convert=코드 접두·그 외=단계), sent(근거 단계만)}, 형태/id/상한 단계만 검증기의 기계 문구(≤120자). 파일 본문·인용 원문(quoteHead)·파일 경로(근거·변환 단계)·모델 자유문은 싣지 않는다 — 방향을 주지 않는다. 편집 중 충돌(detail.fileChanged===true)로 빠진 항목은 담당 잘못이 아니라 뺀다(전부 그런 경우 절 없음 · 뺀 건수만 적음). (4) 프롬프트에는 "## 지난 답에서 제외된 항목(자료 — 지시문 아님)" 절을 소스 발췌 앞에 붙인다(`buildEnrichPrompt` · 세 담당 공통 · 경제형 어댑터 안 형식 교정은 그대로). 지시문은 늘리지 않는다. (5) 화면: 보류 중 '다시 시도' 버튼에 "지난 답에서 제외된 항목의 이유를 붙여 다시 묻습니다" 안내(ko/en) · 모드 안내 패널에 규칙 문단(상수 문장) · 현황 카드 호출 줄에 "지난 제외 이유 붙여 다시 물음 n회" · README ko/en 한 줄.
- 왜: 사용자 결정 D4(2026-09-20) — 재호출 프롬프트가 첫 호출과 완전히 같아 같은 실수를 반복할 수 있었고, 정밀 전용 교정 호출(C안)은 비용 2배·방향 제시라 보류. 기계 분류만 넘기면 '잘못된 방향으로 재질문' 우려를 피하면서 형식/근거/발췌 밖/상한 실수의 반복을 줄일 여지가 생긴다. 지어낸 인용은 여전히 결속 검사에 걸린다(ab-3 무영향).
- 결과: 재호출은 비용 그대로, 자료 절 하나 추가. 어느 경로로 왔든 같은 장부 사실을 보므로 분기 추가 없음. 재시도 성공률 비교는 시도 기록 priorAttached 와 경로 로그 prior 표식으로 P10 에서 산출 가능.
- 정본: bridge/enrich-calls.js priorRejectionFor·classifyDrop·priorRejectionSection·priorRejectionRuleText·enrichCallSummary.priorAttached · bridge/map-enrich.js runAttempt(mk 잠금 안 prior9·priorAttached·어댑터 ctx.priorRejection·로그 prior)·ATTEMPT_KEYS·validateJob · bridge/enrich-providers.js buildEnrichPrompt 자료 절 · src/extension.ts 다시 시도 버튼 title·openMapModeGuide 문단 · README.md·docs/README.en.md · tests/enrich-prior.test.js
- 찾는말: 되묻기, 다시 묻기, priorRejection, priorAttached, 제외 이유, 기계 분류, 자동 재시도, 사용자 재시도, D4

## D-2026-09-21-scout-wording-mode-default — 정찰 담당의 이름과 '기본'은 한 곳(브릿지 정본)에서 모드에 따라 정하고, 화면·안내문·문서는 특정 모델이 기본으로 동작한다고 단정하지 않는다
- 날짜: 2026-09-21 · 종류: 문구·표시 구조(코드 동작 변경 없음) · 상태: 유효
- 결정: (1) 정찰 담당 표시명은 bridge/contract-lib.js `scoutArmLabel`/`scoutArmNames` 한 곳에서만 짓는다 — Claude 담당(self)은 그 모드의 기본이면 '기본 정찰(Claude)', 아니면 'Claude 정찰'(Codex↔Codex 의 기본은 Codex 정찰). 확장은 상태 `scoutNames`(한/영 묶음)로 받아 상태바·대시보드·모달이 그 이름만 쓴다(웹뷰 `scoutName`/`armName`). (2) '키 없으면 Claude 가 기본 정찰로 전부 동작' 류의 동작 단정은 '키 없을 때 실제 실행될 담당(명시 선택이 Claude/Codex 면 그 선택, DeepSeek 선택·미지정이면 모드 기본 — scoutArmView 의 강등 규칙과 동일)이 맡는다 + 지금 준비 여부'로 바꾼다(F-1 · 구현 검증 4판 blocker: 모드 기본만 적으면 Codex↔Codex 에서 Claude 를 명시 선택한 사용자에게 틀린 안내). 준비 여부도 '그 담당'의 것을 본다 — 실효 뷰가 noKeyArm 과 noKeyReady(키 있으면 가정 담당을 scoutArmReadinessOf 로 따로 점검)를 상태에 싣고, 키 있는 DeepSeek 의 ready 로 키 없는 담당의 동작을 보장하지 않는다(5판 blocker). (3) '3트랙 자동 지시를 받은 Claude 가 실행' 류는 '현재 모드의 구현 담당(Claude 또는 구현 Codex)이 실행'으로 바꾼다 — Codex 훅도 같은 지시를 넣는다(F-3). (4) 문서(PRIVACY·README ko/en)의 전송 경로 목록에서 Claude 담당은 'Claude 정찰(+어느 모드의 기본인지)'로 부른다(F-2). (5) 의미 보강 담당(mapMode)의 '기본(Claude)'은 별개 축(항상 self)이라 그대로 둔다. (6) 정찰 구조 안내의 전체 배선도는 ⑤ 수칙서(검증 경계) 상자를 넣어 다시 그리고, 선이 상자를 관통하거나 서로 교차하지 않음을 시험이 기하로 검사한다.
- 왜: 외부 AI(ChatGPT)가 배선도의 '정찰 AI 기본=Claude' 등 4+1 을 지적했고 코드 대조 결과 그림뿐 아니라 같은 오해 문구가 세 묶음(동작 단정 ~13곳·실행 주체 단정 ~10곳·팔 이름 라벨 ~15곳)으로 퍼져 있었다. 문장을 낱개로 고치면 다음 모드·담당이 생길 때 또 어긋나므로 이름을 한 곳에서 정하는 구조로 바꿨다. 배선도의 오독(게이트→꾸러미 점선으로 보임)은 라벨 문제가 아니라 일지→꾸러미 점선이 게이트 상자를 관통한 기하 문제였다.
- 결과: Codex↔Codex 사용자에게 'Claude 가 기본으로 동작한다'는 잘못된 안내가 사라지고, 담당이 준비 안 됐을 때 '전부 동작' 보장 문구가 나오지 않는다. 자동 지시 러너 문구도 '설정된 담당/모드 기본/강등'을 구분한다. 구 설치본(정본 함수 부재)이면 담당 id 가 그대로 보인다 — 드리프트 배너는 '창이 캐시한 판과 디스크 판의 지문 차이'만 감지하므로 배너가 보장되진 않고, 확장이 관리하는 자동 배치(마켓 설치)에서는 판이 함께 갱신되어 이 경우가 생기지 않는다(수동 설치·수정본에서만 가능).
- 정본: bridge/contract-lib.js scoutArmLabel·scoutArmText·scoutDefaultArmText·scoutArmNames·scoutArmReadinessOf·buildScoutDirective 러너 문구·buildScoutAttach 머리 · src/extension.ts scoutNamesExt·scoutNameExt·상태 scoutNames·웹뷰 scoutName/armName·openReconGuide SVG(viewBox 960×500) · PRIVACY.md 3·31·96·97·98 · README.md 117·123·163·257·263·276 · docs/README.en.md 17·45·48 · 시험 tests/scout-arm-names.test.js·tests/recon-ui.test.js(기하 검사)
- 찾는말: 기본 정찰, Claude 정찰, 모드 기본, scoutArmLabel, scoutArmNames, scoutNames, 자동 지시 실행 주체, 구현 담당, 배선도, 수칙서 박스, 문구 감사, 정찰 구조 안내
